// Outage Management - External Contexts
external context SCADASystem as split is {
  outlet SCADASystemRepliesOut is result FeederStatus
 with {
    briefly "What this service answers, on its way back"
  }
  type SCADASystemEvent is one of {
    FeederIsolated or OutageDetected
  } with {
    briefly "Everything this service tells the model, unasked"
  }
  outlet SCADASystemEventsOut is type SCADASystemEvent
 with {
    briefly "What this service tells the model, unasked"
  }
  type SCADASystemRequest is one of {
    IsolateFeeder or GetFeederStatus
  } with {
    briefly "Everything this service can be asked"
  }
  inlet SCADASystemRequests is type SCADASystemRequest with {
    briefly "What this context asks the service to send"
  }
  handler SCADASystemBoundary is {
    on query GetFeederStatus is {
      let askAnswer: type FeederStatus = prompt("look up what GetFeederStatus asks for")
      reply askAnswer
    }
    on command IsolateFeeder is {
      let feederIsolated: type FeederIsolated = prompt("the feeder isolated this produces")
      send feederIsolated to outlet SCADASystem.SCADASystemEventsOut
      do "deliver it to the recipient"
    }
    on other is {
      error "Unexpected message for external context SCADASystem"
    }
  } with {
    briefly "SCADASystem boundary"
  }
  event OutageDetected is {
    feederId: FeederId with {
      briefly "Feeder"
      described as {
        |Feeder ID.
      }
    }
    location: String(1,300) with {
      briefly "Location"
      described as {
        |Outage location.
      }
    }
    detectedAt: TimeStamp with {
      briefly "Detected"
      described as {
        |Detection time.
      }
    }
    severity: String(1,50) with {
      briefly "Severity"
      described as {
        |Outage severity.
      }
    }
  } with {
    briefly "Outage detected"
    described as {
      |Emitted when SCADA detects a power outage.
    }
  }
  query GetFeederStatus replies result FeederStatus is {
    feederId: FeederId with {
      briefly "Feeder"
      described as {
        |Feeder ID.
      }
    }
  } with {
    briefly "Get feeder status"
    described as {
      |Gets current feeder circuit status.
    }
  }
  result FeederStatus is {
    feederId: FeederId with {
      briefly "Feeder"
      described as {
        |Feeder ID.
      }
    }
    feederStatus: String(1,50) with {
      briefly "Status"
      described as {
        |Feeder status.
      }
    }
    voltage: Decimal(8,2) with {
      briefly "Voltage"
      described as {
        |Current voltage reading.
      }
    }
    lastReading: TimeStamp with {
      briefly "Reading"
      described as {
        |Last reading time.
      }
    }
  } with {
    briefly "Feeder status"
    described as {
      |Current feeder circuit status.
    }
  }
  command IsolateFeeder is {
    feederId: FeederId with {
      briefly "Feeder"
      described as {
        |Feeder ID.
      }
    }
    reason: String(1,500) with {
      briefly "Reason"
      described as {
        |Isolation reason.
      }
    }
  } with {
    briefly "Isolate feeder"
    described as {
      |Isolates a feeder circuit for safety.
    }
  }
  event FeederIsolated is {
    feederId: FeederId with {
      briefly "Feeder"
      described as {
        |Feeder ID.
      }
    }
    isolatedAt: TimeStamp with {
      briefly "Isolated"
      described as {
        |Isolation time.
      }
    }
  } with {
    briefly "Feeder isolated"
    described as {
      |Emitted when a feeder is isolated.
    }
  }
} with {
  briefly "External SCADA system"
  described as {
    |Supervisory control and data acquisition system for grid monitoring.
  }
}
external context CrewDispatchSystem as split is {
  outlet CrewDispatchSystemRepliesOut is result AvailableCrews
 with {
    briefly "What this service answers, on its way back"
  }
  type CrewDispatchSystemEvent is one of {
    CrewAssigned or CrewReleased
  } with {
    briefly "Everything this service tells the model, unasked"
  }
  outlet CrewDispatchSystemEventsOut is type CrewDispatchSystemEvent
 with {
    briefly "What this service tells the model, unasked"
  }
  type CrewDispatchSystemCommand is one of {
    AssignCrew or ReleaseCrew or GetAvailableCrews
  } with {
    briefly "Everything this service can be asked to send"
  }
  inlet CrewDispatchSystemRequests is type CrewDispatchSystemCommand with {
    briefly "What this context asks the service to send"
  }
  handler CrewDispatchSystemBoundary is {
    on query GetAvailableCrews is {
      let askAnswer: type AvailableCrews = prompt("look up what GetAvailableCrews asks for")
      reply askAnswer
    }
    on command AssignCrew is {
      let crewAssigned: type CrewAssigned = prompt("the crew assigned this produces")
      send crewAssigned to outlet CrewDispatchSystem.CrewDispatchSystemEventsOut
      do "deliver it to the recipient"
    }
    on command ReleaseCrew is {
      let crewReleased: type CrewReleased = prompt("the crew released this produces")
      send crewReleased to outlet CrewDispatchSystem.CrewDispatchSystemEventsOut
      do "deliver it to the recipient"
    }
    on other is {
      error "Unexpected message for external context CrewDispatchSystem"
    }
  } with {
    briefly "CrewDispatchSystem boundary"
  }
  query GetAvailableCrews replies result AvailableCrews is {
    location: String(1,300) with {
      briefly "Location"
      described as {
        |Outage location.
      }
    }
    priority: OutagePriority with {
      briefly "Priority"
      described as {
        |Outage priority.
      }
    }
  } with {
    briefly "Get available crews"
    described as {
      |Finds available crews near a location.
    }
  }
  result AvailableCrews is {
    crews: CrewAssignment+ with {
      briefly "Crews"
      described as {
        |Available crews.
      }
    }
    estimatedArrival: Decimal(8,2) with {
      briefly "ETA"
      described as {
        |Estimated arrival in minutes.
      }
    }
  } with {
    briefly "Available crews"
    described as {
      |Crews available for dispatch.
    }
  }
  command AssignCrew is {
    outageId: OutageEventId with {
      briefly "Outage"
      described as {
        |Outage ID.
      }
    }
    crewId: CrewId with {
      briefly "Crew"
      described as {
        |Crew ID.
      }
    }
    location: String(1,300) with {
      briefly "Location"
      described as {
        |Dispatch location.
      }
    }
  } with {
    briefly "Assign crew"
    described as {
      |Assigns a crew to an outage event.
    }
  }
  event CrewAssigned is {
    outageId: OutageEventId with {
      briefly "Outage"
      described as {
        |Outage ID.
      }
    }
    crewId: CrewId with {
      briefly "Crew"
      described as {
        |Crew ID.
      }
    }
    assignedAt: TimeStamp with {
      briefly "Assigned"
      described as {
        |Assignment time.
      }
    }
  } with {
    briefly "Crew assigned"
    described as {
      |Emitted when a crew is assigned to an outage.
    }
  }
  command ReleaseCrew is {
    crewId: CrewId with {
      briefly "Crew"
      described as {
        |Crew ID.
      }
    }
    outageId: OutageEventId with {
      briefly "Outage"
      described as {
        |Outage ID.
      }
    }
  } with {
    briefly "Release crew"
    described as {
      |Releases a crew from an outage assignment.
    }
  }
  event CrewReleased is {
    crewId: CrewId with {
      briefly "Crew"
      described as {
        |Crew ID.
      }
    }
    releasedAt: TimeStamp with {
      briefly "Released"
      described as {
        |Release time.
      }
    }
  } with {
    briefly "Crew released"
    described as {
      |Emitted when a crew is released from assignment.
    }
  }
} with {
  briefly "External crew dispatch system"
  described as {
    |Field crew management and dispatch system.
  }
}
