// Design Review - Design Review Context
context DesignReviewContext is {
  adaptor FromDocumentStorage from context DocumentStorage is { 
    handler FromDocumentStorageHandler is {
      on event DocumentStorage.DocumentStored is {
        do "the model receives document stored"
      }
      on other is {
        error "Unexpected message for adaptor FromDocumentStorage"
      }
    } with {
      briefly "from document storage"
      described as {
        |Carries messages from the external DocumentStorage.
      }
    }
  } with {
    briefly "DocumentStorage integration"
    described as {
      |The model's side of the DocumentStorage conversation.
    }
  }
  adaptor ToDocumentStorage to context DocumentStorage is { 
    handler ToDocumentStorageHandler is {
      on command DocumentStorage.StoreDocument is {
        do "the model sends store document"
      }
      on other is {
        error "Unexpected message for adaptor ToDocumentStorage"
      }
    } with {
      briefly "to document storage"
      described as {
        |Carries messages to the external DocumentStorage.
      }
    }
  } with {
    briefly "DocumentStorage integration"
    described as {
      |The model's side of the DocumentStorage conversation.
    }
  }
  adaptor FromIdentityService from context IdentityService is { 
    handler FromIdentityServiceHandler is {
      on result IdentityService.ReviewerDetails is {
        do "the model receives reviewer details"
      }
      on other is {
        error "Unexpected message for adaptor FromIdentityService"
      }
    } with {
      briefly "from identity service"
      described as {
        |Carries messages from the external IdentityService.
      }
    }
  } with {
    briefly "IdentityService integration"
    described as {
      |The model's side of the IdentityService conversation.
    }
  }
  adaptor ToIdentityService to context IdentityService is { 
    handler ToIdentityServiceHandler is {
      on query IdentityService.GetReviewerInfo is {
        do "the model sends get reviewer info"
      }
      on other is {
        error "Unexpected message for adaptor ToIdentityService"
      }
    } with {
      briefly "to identity service"
      described as {
        |Carries messages to the external IdentityService.
      }
    }
  } with {
    briefly "IdentityService integration"
    described as {
      |The model's side of the IdentityService conversation.
    }
  }
  adaptor FromNotificationService from context NotificationService is { 
    handler FromNotificationServiceHandler is {
      on event NotificationService.NotificationSent is {
        do "the model receives notification sent"
      }
      on other is {
        error "Unexpected message for adaptor FromNotificationService"
      }
    } with {
      briefly "from notification service"
      described as {
        |Carries messages from the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor ToNotificationService to context NotificationService is { 
    handler ToNotificationServiceHandler is {
      on command NotificationService.SendReviewNotification is {
        do "the model sends send review notification"
      }
      on other is {
        error "Unexpected message for adaptor ToNotificationService"
      }
    } with {
      briefly "to notification service"
      described as {
        |Carries messages to the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor FromProjectService from context ProjectService is { 
    handler FromProjectServiceHandler is {
      on result ProjectService.ProjectDetails is {
        do "the model receives project details"
      }
      on other is {
        error "Unexpected message for adaptor FromProjectService"
      }
    } with {
      briefly "from project service"
      described as {
        |Carries messages from the external ProjectService.
      }
    }
  } with {
    briefly "ProjectService integration"
    described as {
      |The model's side of the ProjectService conversation.
    }
  }
  adaptor ToProjectService to context ProjectService is { 
    handler ToProjectServiceHandler is {
      on query ProjectService.GetProjectInfo is {
        do "the model sends get project info"
      }
      on other is {
        error "Unexpected message for adaptor ToProjectService"
      }
    } with {
      briefly "to project service"
      described as {
        |Carries messages to the external ProjectService.
      }
    }
  } with {
    briefly "ProjectService integration"
    described as {
      |The model's side of the ProjectService conversation.
    }
  }
  include "types.riddl"
  include "Submittal.riddl"
  // Repository for submittal persistence
  repository SubmittalRepository as merge is {
    inlet SubmittalRepositoryFromSubmittal is type SubmittalEvent    inlet SubmittalRepositoryFromReviewAnalytics is type SubmittalEvent    outlet SubmittalRepositoryResponses is type Submittal.SubmittalResult
    record StoredSubmittal is  {
      submittalId: SubmittalId with {
        briefly "submittalId"
        described as {
          |The submittalId column of the stored submittal row.
        }
      }
      info: SubmittalInfo with {
        briefly "info"
        described as {
          |The info column of the stored submittal row.
        }
      }
    } with {
      briefly "Stored submittal row"
      described as {
        |The persistence shape of submittal data as this repository stores it.
      }
    }
    schema SubmittalData is relational
      of submittals as type StoredSubmittal
        index on field StoredSubmittal.submittalId
        index on field StoredSubmittal.info.projectId
 with {
      briefly "Submittal data schema"
      described as {
        |The relational schema holding submittal data for the SubmittalRepository.
      }
    }
    handler SubmittalPersistence is {
      on query GetReviewStats is {
        do "read the review stats projection"
        yield result ReviewStatsView
      }
      on command Submittal.CreateSubmittal is {
        tell event Submittal.SubmittalCreated to entity DesignReviewContext.Submittal
        do "Store submittal record"
      }
      on command Submittal.UpdateSubmittal is {
        tell event Submittal.SubmittalUpdated to entity DesignReviewContext.Submittal
        do "Update submittal info"
      }
      on command Submittal.SubmitForReview is {
        do "Update submittal status to submitted"
      }
      on command Submittal.AssignReviewer is {
        tell event Submittal.ReviewerAssigned to entity DesignReviewContext.Submittal
        do "Store reviewer assignment"
      }
      on command Submittal.RemoveReviewer is {
        tell event Submittal.ReviewerRemoved to entity DesignReviewContext.Submittal
        do "Remove reviewer assignment"
      }
      on command Submittal.AddComment is {
        tell event Submittal.CommentAdded to entity DesignReviewContext.Submittal
        do "Store review comment"
      }
      on command Submittal.ResolveComment is {
        do "Update comment resolution"
      }
      on command Submittal.SubmitRevision is {
        tell event Submittal.RevisionSubmitted to entity DesignReviewContext.Submittal
        do "Store revision record"
      }
      on command Submittal.RecordApproval is {
        tell event Submittal.ApprovalRecorded to entity DesignReviewContext.Submittal
        do "Store approval record"
      }
      on command Submittal.RejectSubmittal is {
        tell event Submittal.SubmittalRejected to entity DesignReviewContext.Submittal
        do "Update submittal to rejected"
      }
      on command Submittal.WithdrawSubmittal is {
        do "Update submittal to withdrawn"
      }
      on command Submittal.AddMilestone is {
        tell event Submittal.MilestoneAdded to entity DesignReviewContext.Submittal
        do "Store milestone record"
      }
      on command Submittal.SignOffMilestone is {
        do "Update milestone sign-off"
      }
    } with {
      briefly "Persists submittal commands"
      described as {
        |Handles command-driven persistence.
      }
    }
  } with {
    briefly "Submittal data persistence"
    described as {
      | The SubmittalRepository provides persistent storage for
      | design submittals, reviews, comments, and approvals.
    }
  }
  // Projector for review analytics
  query GetReviewStats is  {
    reviewStatsKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which review stats to retrieve.
      }
    }
  } with {
    briefly "Get review stats"
    described as {
      |Retrieves the review stats projection.
    }
  }
  result ReviewStatsView is  {
    reviewStats: ReviewAnalytics.ReviewStats with {
      briefly "Review stats"
      described as {
        |The review stats being returned.
      }
    }
  } with {
    briefly "Review stats view"
    described as {
      |Read model view carrying the review stats projection.
    }
  }
  projector ReviewAnalytics as flow is {
    updates repository DesignReviewContext.SubmittalRepository
    inlet ReviewAnalyticsFromSubmittal is type SubmittalEvent    outlet ReviewAnalyticsOut is type SubmittalEvent
    record ReviewStats is  {
      projectId: UUID with {
        briefly "Project"
        described as {
          |Project ID.
        }
      }
      totalSubmittals: Natural with {
        briefly "Submittals"
        described as {
          |Total submittals.
        }
      }
      pendingReviews: Natural with {
        briefly "Pending"
        described as {
          |Pending reviews.
        }
      }
      approvalRate: Decimal(5,2) with {
        briefly "Approval rate"
        described as {
          |Approval percentage.
        }
      }
      averageReviewDuration: Duration with {
        briefly "Duration"
        described as {
          |Average review time.
        }
      }
      openComments: Natural with {
        briefly "Open comments"
        described as {
          |Unresolved comments.
        }
      }
    } with {
      briefly "Review statistics"
      described as {
        |Project review metrics.
      }
    }
    handler AnalyticsHandler is {
      on event Submittal.SubmittalCreated is {
        tell event Submittal.SubmittalCreated to repository DesignReviewContext.SubmittalRepository
        do "Update submittal counts"
      }
      on event Submittal.SubmittalSubmitted is {
        tell event Submittal.SubmittalSubmitted to repository DesignReviewContext.SubmittalRepository
        do "Update pending review counts"
      }
      on event Submittal.CommentAdded is {
        tell event Submittal.CommentAdded to repository DesignReviewContext.SubmittalRepository
        do "Update comment metrics"
      }
      on event Submittal.CommentResolved is {
        tell event Submittal.CommentResolved to repository DesignReviewContext.SubmittalRepository
        do "Update resolved comment metrics"
      }
      on event Submittal.ApprovalRecorded is {
        tell event Submittal.ApprovalRecorded to repository DesignReviewContext.SubmittalRepository
        do "Update approval metrics"
      }
      on event Submittal.SubmittalRejected is {
        tell event Submittal.SubmittalRejected to repository DesignReviewContext.SubmittalRepository
        do "Update rejection metrics"
      }
    } with {
      briefly "Maintains review analytics"
      described as {
        |Projects events into review analytics views.
      }
    }
  } with {
    briefly "Review analytics projections"
    described as {
      |Maintains design review analytics data.
    }
  }
  // ---- Event publication for Submittal ----
  type SubmittalEvent is one of {
    DesignReviewContext.Submittal.SubmittalCreated or DesignReviewContext.Submittal.SubmittalUpdated or DesignReviewContext.Submittal.SubmittalSubmitted or DesignReviewContext.Submittal.ReviewerAssigned or DesignReviewContext.Submittal.ReviewerRemoved or DesignReviewContext.Submittal.CommentAdded or DesignReviewContext.Submittal.CommentResolved or DesignReviewContext.Submittal.RevisionSubmitted or DesignReviewContext.Submittal.ApprovalRecorded or DesignReviewContext.Submittal.SubmittalRejected or DesignReviewContext.Submittal.SubmittalWithdrawn or DesignReviewContext.Submittal.MilestoneAdded or DesignReviewContext.Submittal.MilestoneSignedOff
  } with {
    briefly "Submittal event alternation"
    described as {
      |Union of all Submittal events, for streaming and event-sourced
      |persistence.
    }
  }
  processor SubmittalEventSource as source is {
    outlet SubmittalEvents is type SubmittalEvent
    handler SubmittalSourceHandler is {
      on event Submittal.SubmittalCreated is {
        send event Submittal.SubmittalCreated to outlet SubmittalEvents
      }
      on event Submittal.SubmittalUpdated is {
        send event Submittal.SubmittalUpdated to outlet SubmittalEvents
      }
      on event Submittal.SubmittalSubmitted is {
        send event Submittal.SubmittalSubmitted to outlet SubmittalEvents
      }
      on event Submittal.ReviewerAssigned is {
        send event Submittal.ReviewerAssigned to outlet SubmittalEvents
      }
      on event Submittal.ReviewerRemoved is {
        send event Submittal.ReviewerRemoved to outlet SubmittalEvents
      }
      on event Submittal.CommentAdded is {
        send event Submittal.CommentAdded to outlet SubmittalEvents
      }
      on event Submittal.CommentResolved is {
        send event Submittal.CommentResolved to outlet SubmittalEvents
      }
      on event Submittal.RevisionSubmitted is {
        send event Submittal.RevisionSubmitted to outlet SubmittalEvents
      }
      on event Submittal.ApprovalRecorded is {
        send event Submittal.ApprovalRecorded to outlet SubmittalEvents
      }
      on event Submittal.SubmittalRejected is {
        send event Submittal.SubmittalRejected to outlet SubmittalEvents
      }
      on event Submittal.SubmittalWithdrawn is {
        send event Submittal.SubmittalWithdrawn to outlet SubmittalEvents
      }
      on event Submittal.MilestoneAdded is {
        send event Submittal.MilestoneAdded to outlet SubmittalEvents
      }
      on event Submittal.MilestoneSignedOff is {
        send event Submittal.MilestoneSignedOff to outlet SubmittalEvents
      }
      on other is {
        error "Unexpected message for source SubmittalEventSource"
      }
    } with {
      briefly "Submittal source handler"
      described as {
        |Publishes Submittal events onto the outlet.
      }
    }
  } with {
    briefly "Submittal event source"
    described as {
      |Publishes Submittal entity events into the streaming topology.
    }
  }
  processor SubmittalEventSink as sink is {
    inlet SubmittalInlet is type SubmittalEvent    handler SubmittalSinkHandler is {
      on event Submittal.SubmittalCreated is {
        tell event Submittal.SubmittalCreated to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.SubmittalUpdated is {
        tell event Submittal.SubmittalUpdated to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.SubmittalSubmitted is {
        tell event Submittal.SubmittalSubmitted to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.ReviewerAssigned is {
        tell event Submittal.ReviewerAssigned to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.ReviewerRemoved is {
        tell event Submittal.ReviewerRemoved to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.CommentAdded is {
        tell event Submittal.CommentAdded to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.CommentResolved is {
        tell event Submittal.CommentResolved to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.RevisionSubmitted is {
        tell event Submittal.RevisionSubmitted to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.ApprovalRecorded is {
        tell event Submittal.ApprovalRecorded to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.SubmittalRejected is {
        tell event Submittal.SubmittalRejected to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.SubmittalWithdrawn is {
        tell event Submittal.SubmittalWithdrawn to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.MilestoneAdded is {
        tell event Submittal.MilestoneAdded to repository DesignReviewContext.SubmittalRepository
      }
      on event Submittal.MilestoneSignedOff is {
        tell event Submittal.MilestoneSignedOff to repository DesignReviewContext.SubmittalRepository
      }
      on other is {
        error "Unexpected message for sink SubmittalEventSink"
      }
    } with {
      briefly "Submittal sink handler"
      described as {
        |Persists Submittal events via the repository.
      }
    }
  } with {
    briefly "Submittal event sink"
    described as {
      |Receives Submittal events for persistence.
    }
  }
  connector SubmittalEventFlow is from outlet DesignReviewContext.SubmittalEventSource.SubmittalEvents to inlet DesignReviewContext.SubmittalEventSink.SubmittalInlet with {
    briefly "Submittal event flow"
    described as {
      |Connects the Submittal source to its sink.
    }
  }
  processor SubmittalEventSplit as split is {
    inlet SubmittalEventSplitIn is type SubmittalEvent    outlet SubmittalEventSplitToSubmittalRepository is type SubmittalEvent
    outlet SubmittalEventSplitToReviewAnalytics is type SubmittalEvent
    handler SubmittalEventSplitHandler is {
      on event Submittal.SubmittalCreated is {
        send event Submittal.SubmittalCreated to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.SubmittalCreated to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.SubmittalCreated to entity DesignReviewContext.Submittal
      }
      on event Submittal.SubmittalUpdated is {
        send event Submittal.SubmittalUpdated to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.SubmittalUpdated to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.SubmittalUpdated to entity DesignReviewContext.Submittal
      }
      on event Submittal.SubmittalSubmitted is {
        send event Submittal.SubmittalSubmitted to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.SubmittalSubmitted to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.SubmittalSubmitted to entity DesignReviewContext.Submittal
      }
      on event Submittal.ReviewerAssigned is {
        send event Submittal.ReviewerAssigned to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.ReviewerAssigned to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.ReviewerAssigned to entity DesignReviewContext.Submittal
      }
      on event Submittal.ReviewerRemoved is {
        send event Submittal.ReviewerRemoved to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.ReviewerRemoved to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.ReviewerRemoved to entity DesignReviewContext.Submittal
      }
      on event Submittal.CommentAdded is {
        send event Submittal.CommentAdded to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.CommentAdded to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.CommentAdded to entity DesignReviewContext.Submittal
      }
      on event Submittal.CommentResolved is {
        send event Submittal.CommentResolved to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.CommentResolved to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.CommentResolved to entity DesignReviewContext.Submittal
      }
      on event Submittal.RevisionSubmitted is {
        send event Submittal.RevisionSubmitted to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.RevisionSubmitted to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.RevisionSubmitted to entity DesignReviewContext.Submittal
      }
      on event Submittal.ApprovalRecorded is {
        send event Submittal.ApprovalRecorded to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.ApprovalRecorded to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.ApprovalRecorded to entity DesignReviewContext.Submittal
      }
      on event Submittal.SubmittalRejected is {
        send event Submittal.SubmittalRejected to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.SubmittalRejected to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.SubmittalRejected to entity DesignReviewContext.Submittal
      }
      on event Submittal.SubmittalWithdrawn is {
        send event Submittal.SubmittalWithdrawn to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.SubmittalWithdrawn to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.SubmittalWithdrawn to entity DesignReviewContext.Submittal
      }
      on event Submittal.MilestoneAdded is {
        send event Submittal.MilestoneAdded to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.MilestoneAdded to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.MilestoneAdded to entity DesignReviewContext.Submittal
      }
      on event Submittal.MilestoneSignedOff is {
        send event Submittal.MilestoneSignedOff to outlet SubmittalEventSplitToSubmittalRepository
        send event Submittal.MilestoneSignedOff to outlet SubmittalEventSplitToReviewAnalytics
        tell event Submittal.MilestoneSignedOff to entity DesignReviewContext.Submittal
      }
      on other is {
        error "Unexpected message for processor SubmittalEventSplit"
      }
    } with {
      briefly "Submittal event fan-out handler"
      described as {
        |Delivers each Submittal event to all of its consumers.
      }
    }
  } with {
    briefly "Submittal event fan-out"
    described as {
      |Fans Submittal events out to the repository and projectors.
    }
  }
  connector 'SubmittalEvent Stream' is from outlet DesignReviewContext.Submittal.SubmittalEvents2 to inlet DesignReviewContext.SubmittalEventSplit.SubmittalEventSplitIn with {
    briefly "Submittal events, fanned out to storage and read models"
  }
  connector 'SubmittalEvent Storage' is from outlet DesignReviewContext.SubmittalEventSplit.SubmittalEventSplitToSubmittalRepository to inlet DesignReviewContext.SubmittalRepository.SubmittalRepositoryFromSubmittal with {
    briefly "Submittal events on their way to storage"
  }
  connector 'ReviewAnalytics Feed' is from outlet DesignReviewContext.SubmittalEventSplit.SubmittalEventSplitToReviewAnalytics to inlet DesignReviewContext.ReviewAnalytics.ReviewAnalyticsFromSubmittal with {
    briefly "Submittal events feeding the ReviewAnalytics projection"
  }
  connector 'ReviewAnalytics Storage' is from outlet DesignReviewContext.ReviewAnalytics.ReviewAnalyticsOut to inlet DesignReviewContext.SubmittalRepository.SubmittalRepositoryFromReviewAnalytics with {
    briefly "The ReviewAnalytics projection on its way to storage"
  }
} with {
  briefly "Bounded context for design reviews"
  described as {
    | The DesignReviewContext is the bounded context for design
    | submittal management, review routing, and approval tracking.
  }
}
