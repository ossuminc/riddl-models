// Ad Serving - Ad Entity
entity Advertisement as flow is {
  type AdCommand is one of {
    Advertisement.CreateAd or Advertisement.UpdateTargeting or Advertisement.UpdateBid or Advertisement.ActivateAd or Advertisement.PauseAd or Advertisement.RecordImpression or Advertisement.RecordClick or Advertisement.CompleteAd or Advertisement.ArchiveAd
  } with {
    briefly "Ad command alternation"
    described as {
      |Commands accepted by the Ad entity.
    }
  }
  inlet AdCommands is type AdCommand  outlet AdEvents2 is type AdEvent
  query GetAd replies result AdResult is {
    adId: AdId with {
      briefly "ID"
      described as {
        |Identifier of the Ad to retrieve.
      }
    }
  } with {
    briefly "Get Ad"
    described as {
      |Retrieve Ad data by identifier.
    }
  }
  result AdResult is  {
    advertisementId: AdId with {
      briefly "Advertisement addressed"
      described as {
        |Which advertisement this message concerns. Named directly rather
        |than left to a wrapped record, because a nested record cannot say
        |which instance is addressed.
      }
    }
    adData: Advertisement.ActiveStateData with {
      briefly "Ad data"
      described as {
        |The requested Ad data.
      }
    }
  } with {
    briefly "Ad result"
    described as {
      |Result carrying the requested Ad data.
    }
  }
  command CreateAd is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |New ad identifier.
      }
    }
    campaignId: CampaignId with {
      briefly "Campaign"
      described as {
        |Parent campaign.
      }
    }
    adName: String(1,200) with {
      briefly "Name"
      described as {
        |Ad name.
      }
    }
    creative: AdCreative with {
      briefly "Creative"
      described as {
        |Ad creative.
      }
    }
    targeting: TargetingCriteria with {
      briefly "Targeting"
      described as {
        |Targeting criteria.
      }
    }
    schedule: FlightSchedule with {
      briefly "Schedule"
      described as {
        |Flight schedule.
      }
    }
    bidInfo: BidInfo with {
      briefly "Bid"
      described as {
        |Bidding info.
      }
    }
  } with {
    briefly "Create ad"
    described as {
      |Creates a new ad.
    }
  }
  command UpdateTargeting is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    targeting: TargetingCriteria with {
      briefly "Targeting"
      described as {
        |New targeting.
      }
    }
  } with {
    briefly "Update targeting"
    described as {
      |Updates targeting criteria.
    }
  }
  command UpdateBid is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    bidInfo: BidInfo with {
      briefly "Bid"
      described as {
        |New bid info.
      }
    }
  } with {
    briefly "Update bid"
    described as {
      |Updates bid configuration.
    }
  }
  command ActivateAd is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    activatedAt: TimeStamp with {
      briefly "Activated"
      described as {
        |Activation time.
      }
    }
  } with {
    briefly "Activate ad"
    described as {
      |Activates ad for delivery.
    }
  }
  command PauseAd is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    reason: String(1,200) with {
      briefly "Reason"
      described as {
        |Pause reason.
      }
    }
  } with {
    briefly "Pause ad"
    described as {
      |Pauses ad delivery.
    }
  }
  command RecordImpression is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    impression: ImpressionRecord with {
      briefly "Impression"
      described as {
        |Impression record.
      }
    }
  } with {
    briefly "Record impression"
    described as {
      |Records an ad impression.
    }
  }
  command RecordClick is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    click: ClickRecord with {
      briefly "Click"
      described as {
        |Click record.
      }
    }
  } with {
    briefly "Record click"
    described as {
      |Records an ad click.
    }
  }
  command CompleteAd is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
    completedAt: TimeStamp with {
      briefly "Completed"
      described as {
        |Completion time.
      }
    }
    finalStats: DeliveryStats with {
      briefly "Stats"
      described as {
        |Final delivery stats.
      }
    }
  } with {
    briefly "Complete ad"
    described as {
      |Marks ad as completed.
    }
  }
  command ArchiveAd is  {
    adId: AdId with {
      briefly "Ad ID"
      described as {
        |Ad identifier.
      }
    }
  } with {
    briefly "Archive ad"
    described as {
      |Archives the ad.
    }
  }
  // Events
  event AdCreated is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    campaignId: CampaignId with {
      briefly "Campaign"
      described as {
        |Campaign ID.
      }
    }
    adName: String(1,200) with {
      briefly "Name"
      described as {
        |Ad name.
      }
    }
    createdAt: TimeStamp with {
      briefly "Created"
      described as {
        |Creation time.
      }
    }
  } with {
    briefly "Ad created"
    described as {
      |Emitted when ad is created.
    }
  }
  event TargetingUpdated is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    targeting: TargetingCriteria with {
      briefly "Targeting"
      described as {
        |New targeting.
      }
    }
    updatedAt: TimeStamp with {
      briefly "Updated"
      described as {
        |Update time.
      }
    }
  } with {
    briefly "Targeting updated"
    described as {
      |Emitted when targeting changes.
    }
  }
  event BidUpdated is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    bidInfo: BidInfo with {
      briefly "Bid"
      described as {
        |New bid.
      }
    }
    updatedAt: TimeStamp with {
      briefly "Updated"
      described as {
        |Update time.
      }
    }
  } with {
    briefly "Bid updated"
    described as {
      |Emitted when bid changes.
    }
  }
  event AdActivated is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    activatedAt: TimeStamp with {
      briefly "Activated"
      described as {
        |Activation time.
      }
    }
  } with {
    briefly "Ad activated"
    described as {
      |Emitted when ad activates.
    }
  }
  event AdPaused is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    reason: String(1,200) with {
      briefly "Reason"
      described as {
        |Pause reason.
      }
    }
    pausedAt: TimeStamp with {
      briefly "Paused"
      described as {
        |Pause time.
      }
    }
  } with {
    briefly "Ad paused"
    described as {
      |Emitted when ad is paused.
    }
  }
  event ImpressionRecorded is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    impression: ImpressionRecord with {
      briefly "Impression"
      described as {
        |Impression.
      }
    }
  } with {
    briefly "Impression recorded"
    described as {
      |Emitted when impression served.
    }
  }
  event ClickRecorded is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    click: ClickRecord with {
      briefly "Click"
      described as {
        |Click.
      }
    }
  } with {
    briefly "Click recorded"
    described as {
      |Emitted when click occurs.
    }
  }
  event AdCompleted is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    finalStats: DeliveryStats with {
      briefly "Stats"
      described as {
        |Final stats.
      }
    }
    completedAt: TimeStamp with {
      briefly "Completed"
      described as {
        |Completion time.
      }
    }
  } with {
    briefly "Ad completed"
    described as {
      |Emitted when ad completes.
    }
  }
  event AdArchived is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    archivedAt: TimeStamp with {
      briefly "Archived"
      described as {
        |Archive time.
      }
    }
  } with {
    briefly "Ad archived"
    described as {
      |Emitted when ad archived.
    }
  }
  // States
  record ActiveStateData is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    campaignId: CampaignId with {
      briefly "Campaign"
      described as {
        |Campaign ID.
      }
    }
    adName: String(1,200) with {
      briefly "Name"
      described as {
        |Ad name.
      }
    }
    status: AdStatus with {
      briefly "Status"
      described as {
        |Ad status.
      }
    }
    creative: AdCreative with {
      briefly "Creative"
      described as {
        |Creative.
      }
    }
    targeting: TargetingCriteria with {
      briefly "Targeting"
      described as {
        |Targeting.
      }
    }
    schedule: FlightSchedule with {
      briefly "Schedule"
      described as {
        |Schedule.
      }
    }
    bidInfo: BidInfo with {
      briefly "Bid"
      described as {
        |Bid info.
      }
    }
    currentStats: DeliveryStats? with {
      briefly "Stats"
      described as {
        |Current stats.
      }
    }
    createdAt: TimeStamp with {
      briefly "Created"
      described as {
        |Creation time.
      }
    }
  } with {
    briefly "Active ad state"
    described as {
      |State for active ad.
    }
  }
  record ArchivedStateData is  {
    adId: AdId with {
      briefly "Ad"
      described as {
        |Ad ID.
      }
    }
    adName: String(1,200) with {
      briefly "Name"
      described as {
        |Ad name.
      }
    }
    finalStats: DeliveryStats with {
      briefly "Stats"
      described as {
        |Final stats.
      }
    }
    archivedAt: TimeStamp with {
      briefly "Archived"
      described as {
        |Archive time.
      }
    }
  } with {
    briefly "Archived ad state"
    described as {
      |State for archived ad.
    }
  }
  initial state ActiveState of record Advertisement.ActiveStateData is {
    initial handler ActiveStateInitHandler is {
      on query GetAd is {
        let adResult: type AdResult = prompt("read the ad from the store")
        reply adResult
      }
      on init is {
        set state ActiveState to "initialize Advertisement.ActiveStateData fields from the creating message"
      }
    } with {
      briefly "ActiveState initialization"
      described as {
        |Initializes ActiveState from the message that caused the
        |entity to enter it.
      }
    }
  } with {
    briefly "Active state"
    described as {
      |The active state of the Ad entity, carrying ActiveStateData.
    }
  }
  state ArchivedState of record Advertisement.ArchivedStateData with {
    briefly "Archived state"
    described as {
      |The archived state of the Ad entity, carrying ArchivedStateData.
    }
  }
  handler AdHandler is {
    on init is {
      set state ArchivedState to "initialize Advertisement.ArchivedStateData fields from the creating message"
    }
    on createAd: command CreateAd is {
      morph entity AdContext.Advertisement to state AdContext.Advertisement.ActiveState with record Advertisement.ActiveStateData(adId = createAd.adId, campaignId = createAd.campaignId, adName = createAd.adName, creative = createAd.creative, targeting = createAd.targeting, schedule = createAd.schedule, bidInfo = createAd.bidInfo, status = prompt("the status of this active state data"), currentStats = empty, createdAt = prompt("the created at of this active state data"))
      tell event AdCreated(adId = createAd.adId, campaignId = createAd.campaignId, adName = createAd.adName, createdAt = prompt("the created at of this ad created")) to entity AdContext.Advertisement
    }
    on updateTargeting: command UpdateTargeting is {
      tell event TargetingUpdated(adId = updateTargeting.adId, targeting = updateTargeting.targeting, updatedAt = prompt("the updated at of this targeting updated")) to entity AdContext.Advertisement
    }
    on updateBid: command UpdateBid is {
      tell event BidUpdated(adId = updateBid.adId, bidInfo = updateBid.bidInfo, updatedAt = prompt("the updated at of this bid updated")) to entity AdContext.Advertisement
    }
    on activateAd: command ActivateAd is {
      tell event AdActivated(adId = activateAd.adId, activatedAt = activateAd.activatedAt) to entity AdContext.Advertisement
    }
    on pauseAd: command PauseAd is {
      tell event AdPaused(adId = pauseAd.adId, reason = pauseAd.reason, pausedAt = prompt("the paused at of this ad paused")) to entity AdContext.Advertisement
    }
    on recordImpression: command RecordImpression is {
      tell event ImpressionRecorded(adId = recordImpression.adId, impression = recordImpression.impression) to entity AdContext.Advertisement
    }
    on recordClick: command RecordClick is {
      tell event ClickRecorded(adId = recordClick.adId, click = recordClick.click) to entity AdContext.Advertisement
    }
    on completeAd: command CompleteAd is {
      tell event AdCompleted(adId = completeAd.adId, finalStats = completeAd.finalStats, completedAt = completeAd.completedAt) to entity AdContext.Advertisement
    }
    on archiveAd: command ArchiveAd is {
      morph entity AdContext.Advertisement to state AdContext.Advertisement.ArchivedState with record Advertisement.ArchivedStateData(adId = archiveAd.adId, adName = prompt("the ad name of this archived state data"), finalStats = prompt("the final stats of this archived state data"), archivedAt = prompt("the archived at of this archived state data"))
      tell event AdArchived(adId = archiveAd.adId, archivedAt = prompt("the archived at of this ad archived")) to entity AdContext.Advertisement
    }
      on adActivated: event AdActivated is {
      do "apply AdActivated to the advertisement so it can be persisted and replayed"
    }
    on adArchived: event AdArchived is {
      do "apply AdArchived to the advertisement so it can be persisted and replayed"
    }
    on adCompleted: event AdCompleted is {
      do "apply AdCompleted to the advertisement so it can be persisted and replayed"
    }
    on adCreated: event AdCreated is {
      do "apply AdCreated to the advertisement so it can be persisted and replayed"
    }
    on adPaused: event AdPaused is {
      do "apply AdPaused to the advertisement so it can be persisted and replayed"
    }
    on bidUpdated: event BidUpdated is {
      do "apply BidUpdated to the advertisement so it can be persisted and replayed"
    }
    on clickRecorded: event ClickRecorded is {
      do "apply ClickRecorded to the advertisement so it can be persisted and replayed"
    }
    on impressionRecorded: event ImpressionRecorded is {
      do "apply ImpressionRecorded to the advertisement so it can be persisted and replayed"
    }
    on targetingUpdated: event TargetingUpdated is {
      do "apply TargetingUpdated to the advertisement so it can be persisted and replayed"
    }
} with {
    briefly "Processes ad commands"
    described as {
      |Handles ad lifecycle.
    }
  }
} with {
  briefly "Ad aggregate"
  described as {
    |Manages digital advertisements.
  }
}
