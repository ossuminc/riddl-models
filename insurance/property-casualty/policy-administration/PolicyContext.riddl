// Policy Administration - Policy Context
context PolicyContext as router is {
  inlet PolicyCommandStreamIn is type PolicyCommand  outlet PolicyCommandStreamOut is type PolicyCommand
  inlet PolicyResultStreamIn is result Policy.PolicyResult  outlet PolicyResultStreamOut is result Policy.PolicyResult
  handler PolicyContextBoundary is {
    on issuePolicy: command Policy.IssuePolicy is {
      send issuePolicy to outlet PolicyContext.PolicyCommandStreamOut
    }
    on addEndorsement: command Policy.AddEndorsement is {
      send addEndorsement to outlet PolicyContext.PolicyCommandStreamOut
    }
    on processRenewal: command Policy.ProcessRenewal is {
      send processRenewal to outlet PolicyContext.PolicyCommandStreamOut
    }
    on cancelPolicy: command Policy.CancelPolicy is {
      send cancelPolicy to outlet PolicyContext.PolicyCommandStreamOut
    }
    on reinstatPolicy: command Policy.ReinstatPolicy is {
      send reinstatPolicy to outlet PolicyContext.PolicyCommandStreamOut
    }
    on updateBilling: command Policy.UpdateBilling is {
      send updateBilling to outlet PolicyContext.PolicyCommandStreamOut
    }
    on generateRenewalOffer: command Policy.GenerateRenewalOffer is {
      send generateRenewalOffer to outlet PolicyContext.PolicyCommandStreamOut
    }
    on declineRenewal: command DeclineRenewal is {
      yield event RenewalDeclined(policyId = declineRenewal.policyId, renewalId = declineRenewal.renewalId, declinedAt = prompt("the declined at of this renewal declined"), declineReason = declineRenewal.declineReason)
    }
    on policyResult: result Policy.PolicyResult is {
      send policyResult to outlet PolicyContext.PolicyResultStreamOut
    }
    on other is {
      error "Unexpected message at the PolicyContext boundary"
    }
  } with {
    briefly "Context boundary relay"
    described as {
      |The context is the port at its own boundary: peers connect here, never
      |to a definition inside. Inbound messages are relayed to the definition
      |that owns them; outbound ones are republished from here.
    }
  }
  connector 'PolicyCommand Stream Intake' is from outlet PolicyContext.PolicyCommandStreamOut to inlet PolicyContext.Policy.PolicyCommands  connector 'PolicyResult Stream Egress' is from outlet PolicyContext.PolicyRepository.PolicyRepositoryResponses to inlet PolicyContext.PolicyResultStreamIn  adaptor FromBillingService from context BillingService is { 
    inlet FromBillingServiceIn is type BillingService.BillingServiceEvent    handler FromBillingServiceHandler is {
      on billingSetupComplete: event BillingService.BillingSetupComplete is {
        do "billing is set up; issuing the policy was recorded here"
      }
      on paymentPastDue: event BillingService.PaymentPastDue is {
        do "the premium is late; cancelling for non-payment follows notice and a decision, not the bill"
      }
      on paymentReceivedEvent: event BillingService.PaymentReceived is {
        do "the premium is paid; the policy holds no payment record of its own"
      }
      on other is {
        error "Unexpected message for adaptor FromBillingService"
      }
    } with {
      briefly "from billing service"
      described as {
        |Carries messages from the external BillingService.
      }
    }
  } with {
    briefly "BillingService integration"
    described as {
      |The model's side of the BillingService conversation.
    }
  }
  adaptor ToBillingService to context BillingService as flow is { 
    inlet ToBillingServiceIn is type PolicyEvent    outlet ToBillingServiceOut is type BillingService.BillingServiceCommand
    handler ToBillingServiceHandler is {
      on policyIssued: event Policy.PolicyIssued is {
        let setup: type BillingService.SetupBilling = prompt("translate the policyIssued event into a SetupBilling starting the billing arrangement for the policy just put on risk")
        send setup to outlet PolicyContext.ToBillingService.ToBillingServiceOut
      }
      on endorsementAdded: event Policy.EndorsementAdded is {
        let change: type BillingService.AdjustBilling = prompt("translate the endorsementAdded event into an AdjustBilling, since an endorsement changes the coverage and therefore the premium billed")
        send change to outlet PolicyContext.ToBillingService.ToBillingServiceOut
      }
      on policyCancelled: event Policy.PolicyCancelled is {
        let stop: type BillingService.SuspendBilling = prompt("translate the policyCancelled event into a SuspendBilling, there being nothing further to bill once cover ends")
        send stop to outlet PolicyContext.ToBillingService.ToBillingServiceOut
        let refund: type BillingService.ProcessRefund = prompt("translate the policyCancelled event into a ProcessRefund returning the unearned premium the cancellation releases")
        send refund to outlet PolicyContext.ToBillingService.ToBillingServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToBillingService"
      }
    } with {
      briefly "to billing service"
      described as {
        |Carries messages to the external BillingService.
      }
    }
  } with {
    briefly "BillingService integration"
    described as {
      |The model's side of the BillingService conversation.
    }
  }
  adaptor FromDocumentService from context DocumentService is { 
    inlet FromDocumentServiceIn is event DocumentService.DocumentGenerated    handler FromDocumentServiceHandler is {
      on documentGenerated: event DocumentService.DocumentGenerated is {
        do "the documents exist; the issue, renewal or cancellation that called for them was recorded here"
      }
      on other is {
        error "Unexpected message for adaptor FromDocumentService"
      }
    } with {
      briefly "from document service"
      described as {
        |Carries messages from the external DocumentService.
      }
    }
  } with {
    briefly "DocumentService integration"
    described as {
      |The model's side of the DocumentService conversation.
    }
  }
  adaptor ToDocumentService to context DocumentService as flow is { 
    inlet ToDocumentServiceIn is type PolicyEvent    outlet ToDocumentServiceOut is type DocumentService.DocumentServiceCommand
    handler ToDocumentServiceHandler is {
      on policyIssued: event PolicyIssued is {
        let artifact: type DocumentService.GeneratePolicyDocuments = prompt("translate the policyIssued event into a GeneratePolicyDocuments producing the schedule and wording the insured is entitled to")
        send artifact to outlet PolicyContext.ToDocumentService.ToDocumentServiceOut
      }
      on renewalOfferGenerated: event RenewalOfferGenerated is {
        let artifact: type DocumentService.GenerateRenewalNotice = prompt("translate the renewalOfferGenerated event into a GenerateRenewalNotice putting the offer in writing")
        send artifact to outlet PolicyContext.ToDocumentService.ToDocumentServiceOut
      }
      on policyCancelled: event PolicyCancelled is {
        let artifact: type DocumentService.GenerateCancellationNotice = prompt("translate the policyCancelled event into a GenerateCancellationNotice, cancellation being a thing that must be evidenced")
        send artifact to outlet PolicyContext.ToDocumentService.ToDocumentServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToDocumentService"
      }
    } with {
      briefly "to document service"
      described as {
        |Carries messages to the external DocumentService.
      }
    }
  } with {
    briefly "DocumentService integration"
    described as {
      |The model's side of the DocumentService conversation.
    }
  }
  adaptor FromRatingEngine from context RatingEngine is { 
    inlet FromRatingEngineIn is event RatingEngine.PolicyRated    handler FromRatingEngineHandler is {
      on policyRated: event RatingEngine.PolicyRated is {
        do "the premium is computed; the rating engine owns the number and the policy carries what it is told"
      }
      on other is {
        error "Unexpected message for adaptor FromRatingEngine"
      }
    } with {
      briefly "from rating engine"
      described as {
        |Carries messages from the external RatingEngine.
      }
    }
  } with {
    briefly "RatingEngine integration"
    described as {
      |The model's side of the RatingEngine conversation.
    }
  }
  adaptor ToRatingEngine to context RatingEngine as flow is { 
    inlet ToRatingEngineIn is type PolicyEvent    outlet ToRatingEngineOut is type RatingEngine.RatingEngineCommand
    handler ToRatingEngineHandler is {
      on policyIssued: event Policy.PolicyIssued is {
        let item0: type RatingEngine.RatePolicy = prompt("translate the policyIssued event into a RatePolicy, issuance being what must be priced")
        send item0 to outlet PolicyContext.ToRatingEngine.ToRatingEngineOut
      }
      on endorsementAdded: event Policy.EndorsementAdded is {
        let item0: type RatingEngine.RateEndorsement = prompt("translate the endorsementAdded event into a RateEndorsement, an endorsement changing what must be rated")
        send item0 to outlet PolicyContext.ToRatingEngine.ToRatingEngineOut
      }
      on renewalOfferGenerated: event Policy.RenewalOfferGenerated is {
        let item0: type RatingEngine.RateRenewal = prompt("translate the renewalOfferGenerated event into a RateRenewal, an offer being unmakeable without a price")
        send item0 to outlet PolicyContext.ToRatingEngine.ToRatingEngineOut
      }
      on other is {
        error "Unexpected message for adaptor ToRatingEngine"
      }
    } with {
      briefly "to rating engine"
      described as {
        |Carries messages to the external RatingEngine.
      }
    }
  } with {
    briefly "RatingEngine integration"
    described as {
      |The model's side of the RatingEngine conversation.
    }
  }
  include "types.riddl"
  include "Policy.riddl"
  // Repository for policy persistence
  type PolicyRepositoryCommand is one of {
    PolicyContext.PolicyRepository.PersistBillingUpdated or PolicyContext.PolicyRepository.PersistEndorsementAdded or PolicyContext.PolicyRepository.PersistPolicyCancelled or PolicyContext.PolicyRepository.PersistPolicyIssued or PolicyContext.PolicyRepository.PersistPolicyReinstated or PolicyContext.PolicyRepository.PersistRenewalDeclined or PolicyContext.PolicyRepository.PersistRenewalOfferGenerated or PolicyContext.PolicyRepository.PersistRenewalProcessed
  } with {
    briefly "Anything that writes PolicyRepository"
    described as {
      |The persistence commands PolicyRepository accepts. Entity events never
      |reach it directly; a projector turns each into one of these.
    }
  }
  repository PolicyRepository as flow is {
    inlet PolicyRepositoryFromPolicyMetricsProjector is type PolicyRepositoryCommand    command PersistBillingUpdated is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row BillingUpdated applies to.
        }
      }
    } with {
      briefly "Persist BillingUpdated"
      described as {
        |Instructs this repository to apply BillingUpdated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistRenewalDeclined is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row RenewalDeclined applies to.
        }
      }
    } with {
      briefly "Persist RenewalDeclined"
      described as {
        |Instructs this repository to apply RenewalDeclined to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistRenewalOfferGenerated is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row RenewalOfferGenerated applies to.
        }
      }
    } with {
      briefly "Persist RenewalOfferGenerated"
      described as {
        |Instructs this repository to apply RenewalOfferGenerated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistEndorsementAdded is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row EndorsementAdded applies to.
        }
      }
    } with {
      briefly "Persist EndorsementAdded"
      described as {
        |Instructs this repository to apply EndorsementAdded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPolicyCancelled is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row PolicyCancelled applies to.
        }
      }
    } with {
      briefly "Persist PolicyCancelled"
      described as {
        |Instructs this repository to apply PolicyCancelled to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPolicyIssued is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row PolicyIssued applies to.
        }
      }
    } with {
      briefly "Persist PolicyIssued"
      described as {
        |Instructs this repository to apply PolicyIssued to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPolicyReinstated is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row PolicyReinstated applies to.
        }
      }
    } with {
      briefly "Persist PolicyReinstated"
      described as {
        |Instructs this repository to apply PolicyReinstated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistRenewalProcessed is {
      policyId: PolicyId with {
        briefly "Row addressed"
        described as {
          |Which stored row RenewalProcessed applies to.
        }
      }
    } with {
      briefly "Persist RenewalProcessed"
      described as {
        |Instructs this repository to apply RenewalProcessed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    outlet PolicyRepositoryResponses is result Policy.PolicyResult
    record StoredPolicy is {
      policyId: PolicyId with {
        briefly "policyId"
        described as {
          |The policyId column of the stored policy row.
        }
      }
      details: PolicyDetails with {
        briefly "details"
        described as {
          |The details column of the stored policy row.
        }
      }
    } with {
      briefly "Stored policy row"
      described as {
        |The persistence shape of policy data as this repository stores it.
      }
    }
    schema PolicyData is relational
      of policies as record StoredPolicy
        index on field StoredPolicy.policyId
        index on field StoredPolicy.details.policyNumber
        index on field StoredPolicy.details.insuredName
 with {
      briefly "Policy data schema"
      described as {
        |The relational schema holding policy data for the PolicyRepository.
      }
    }
    handler PolicyPersistence is {
      on persistBillingUpdated: command PersistBillingUpdated is {
        do "apply BillingUpdated to the stored row"
      }
      on persistRenewalDeclined: command PersistRenewalDeclined is {
        do "apply RenewalDeclined to the stored row"
      }
      on persistRenewalOfferGenerated: command PersistRenewalOfferGenerated is {
        do "apply RenewalOfferGenerated to the stored row"
      }
      on persistEndorsementAdded: command PersistEndorsementAdded is {
        do "apply EndorsementAdded to the stored row"
      }
      on persistPolicyCancelled: command PersistPolicyCancelled is {
        do "apply PolicyCancelled to the stored row"
      }
      on persistPolicyIssued: command PersistPolicyIssued is {
        do "apply PolicyIssued to the stored row"
      }
      on persistPolicyReinstated: command PersistPolicyReinstated is {
        do "apply PolicyReinstated to the stored row"
      }
      on persistRenewalProcessed: command PersistRenewalProcessed is {
        do "apply RenewalProcessed to the stored row"
      }
      on query GetPolicyMetricsView is {
        let policyMetricsViewView: type PolicyMetricsViewView = prompt("read the policy metrics view projection")
        reply policyMetricsViewView
      }
      on query GetPortfolioSummary is {
        let portfolioSummaryView: type PortfolioSummaryView = prompt("read the portfolio summary projection")
        reply portfolioSummaryView
      }
    } with {
      briefly "Persists policy commands"
      described as {
        |Handles command-driven persistence for policies.
      }
    }
  } with {
    briefly "Policy data persistence"
    described as {
      | The PolicyRepository provides persistent storage for
      | policy records including endorsements and renewals.
    }
  }
  // Projector for policy metrics
  query GetPolicyMetricsView replies result PolicyMetricsViewView is {
    policyMetricsViewKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which policy metrics view to retrieve.
      }
    }
  } with {
    briefly "Get policy metrics view"
    described as {
      |Retrieves the policy metrics view projection.
    }
  }
  result PolicyMetricsViewView is {
    policyMetricsView: PolicyMetricsProjector.PolicyMetricsView with {
      briefly "Policy metrics view"
      described as {
        |The policy metrics view being returned.
      }
    }
  } with {
    briefly "Policy metrics view view"
    described as {
      |Read model view carrying the policy metrics view projection.
    }
  }
  query GetPortfolioSummary replies result PortfolioSummaryView is {
    portfolioSummaryKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which portfolio summary to retrieve.
      }
    }
  } with {
    briefly "Get portfolio summary"
    described as {
      |Retrieves the portfolio summary projection.
    }
  }
  result PortfolioSummaryView is {
    portfolioSummary: PolicyMetricsProjector.PortfolioSummary with {
      briefly "Portfolio summary"
      described as {
        |The portfolio summary being returned.
      }
    }
  } with {
    briefly "Portfolio summary view"
    described as {
      |Read model view carrying the portfolio summary projection.
    }
  }
  projector PolicyMetricsProjector as flow is {
    updates repository PolicyContext.PolicyRepository
    outlet PolicyMetricsProjectorToRepository is type PolicyRepositoryCommand
    inlet PolicyMetricsProjectorFromPolicy is type PolicyEvent    record PolicyMetricsView is {
      policyId: PolicyId with {
        briefly "Policy"
        described as {
          |Policy ID.
        }
      }
      policyNumber: String(1,20) with {
        briefly "Number"
        described as {
          |Policy number.
        }
      }
      insuredName: String(1,200) with {
        briefly "Insured"
        described as {
          |Insured name.
        }
      }
      status: PolicyStatus with {
        briefly "Status"
        described as {
          |Policy status.
        }
      }
      effectiveDate: Date with {
        briefly "Effective"
        described as {
          |Effective date.
        }
      }
      expirationDate: Date with {
        briefly "Expiration"
        described as {
          |Expiration date.
        }
      }
      totalPremium: Decimal(12,2) with {
        briefly "Premium"
        described as {
          |Total premium.
        }
      }
      endorsementCount: Integer with {
        briefly "Endorsements"
        described as {
          |Endorsement count.
        }
      }
      lastActivityDate: Date with {
        briefly "Last activity"
        described as {
          |Last activity date.
        }
      }
      renewalEligible: Boolean with {
        briefly "Renewal eligible"
        described as {
          |Is eligible for renewal.
        }
      }
    } with {
      briefly "Policy metrics view"
      described as {
        |Denormalized view for policy metrics.
      }
    }
    record PortfolioSummary is {
      totalActivePolicies: Integer with {
        briefly "Active"
        described as {
          |Total active policies.
        }
      }
      totalWrittenPremium: Decimal(14,2) with {
        briefly "Written"
        described as {
          |Total written premium.
        }
      }
      policiesExpiringThisMonth: Integer with {
        briefly "Expiring"
        described as {
          |Policies expiring this month.
        }
      }
      averagePolicyAge: Decimal(5,2) with {
        briefly "Avg age"
        described as {
          |Average policy age in days.
        }
      }
      endorsementsThisMonth: Integer with {
        briefly "Endorsements"
        described as {
          |Endorsements this month.
        }
      }
      cancellationsThisMonth: Integer with {
        briefly "Cancellations"
        described as {
          |Cancellations this month.
        }
      }
      renewalRate: Decimal(5,4) with {
        briefly "Renewal rate"
        described as {
          |Renewal rate percentage.
        }
      }
    } with {
      briefly "Portfolio summary"
      described as {
        |Aggregated metrics across the policy portfolio.
      }
    }
    handler MetricsHandler is {
      on billingUpdated: event Policy.BillingUpdated is {
        tell command PolicyContext.PolicyRepository.PersistBillingUpdated(policyId = billingUpdated.policyId) to repository PolicyContext.PolicyRepository
        do "apply BillingUpdated to the PolicyMetricsProjector projection"
      }
      on renewalDeclined: event Policy.RenewalDeclined is {
        tell command PolicyContext.PolicyRepository.PersistRenewalDeclined(policyId = renewalDeclined.policyId) to repository PolicyContext.PolicyRepository
        do "apply RenewalDeclined to the PolicyMetricsProjector projection"
      }
      on renewalOfferGenerated: event Policy.RenewalOfferGenerated is {
        tell command PolicyContext.PolicyRepository.PersistRenewalOfferGenerated(policyId = renewalOfferGenerated.policyId) to repository PolicyContext.PolicyRepository
        do "apply RenewalOfferGenerated to the PolicyMetricsProjector projection"
      }
      on policyIssued: event Policy.PolicyIssued is {
        tell command PolicyContext.PolicyRepository.PersistPolicyIssued(policyId = policyIssued.policyId) to repository PolicyContext.PolicyRepository
      }
      on endorsementAdded: event Policy.EndorsementAdded is {
        tell command PolicyContext.PolicyRepository.PersistEndorsementAdded(policyId = endorsementAdded.policyId) to repository PolicyContext.PolicyRepository
      }
      on renewalProcessed: event Policy.RenewalProcessed is {
        tell command PolicyContext.PolicyRepository.PersistRenewalProcessed(policyId = renewalProcessed.policyId) to repository PolicyContext.PolicyRepository
      }
      on policyCancelled: event Policy.PolicyCancelled is {
        tell command PolicyContext.PolicyRepository.PersistPolicyCancelled(policyId = policyCancelled.policyId) to repository PolicyContext.PolicyRepository
      }
      on policyReinstated: event Policy.PolicyReinstated is {
        tell command PolicyContext.PolicyRepository.PersistPolicyReinstated(policyId = policyReinstated.policyId) to repository PolicyContext.PolicyRepository
      }
    } with {
      briefly "Maintains policy metrics"
      described as {
        |Projects events into metrics views.
      }
    }
  } with {
    briefly "Policy metrics projector"
    described as {
      | Maintains read-optimized views of policy data for
      | dashboards, reporting, and portfolio analysis.
    }
  }
  streamlet PolicyEventSplit as split is {
    outlet PolicyEventSplitToToRatingEngine is type PolicyEvent
    outlet PolicyEventSplitToToDocumentService is type PolicyEvent
    outlet PolicyEventSplitToToBillingService is type PolicyEvent
    inlet PolicyEventSplitIn is type PolicyEvent    outlet PolicyEventSplitToPolicyMetricsProjector is type PolicyEvent
    handler PolicyEventSplitHandler is {
      on policyIssued: event Policy.PolicyIssued is {
        send policyIssued to outlet PolicyEventSplitToPolicyMetricsProjector
        send policyIssued to outlet PolicyEventSplitToToRatingEngine
        send policyIssued to outlet PolicyEventSplitToToDocumentService
        send policyIssued to outlet PolicyEventSplitToToBillingService
        tell policyIssued to entity PolicyContext.Policy
      }
      on endorsementAdded: event Policy.EndorsementAdded is {
        send endorsementAdded to outlet PolicyEventSplitToPolicyMetricsProjector
        send endorsementAdded to outlet PolicyEventSplitToToRatingEngine
        send endorsementAdded to outlet PolicyEventSplitToToBillingService
        tell endorsementAdded to entity PolicyContext.Policy
      }
      on renewalProcessed: event Policy.RenewalProcessed is {
        send renewalProcessed to outlet PolicyEventSplitToPolicyMetricsProjector
        tell renewalProcessed to entity PolicyContext.Policy by policyId
      }
      on renewalDeclined: event Policy.RenewalDeclined is {
        send renewalDeclined to outlet PolicyEventSplitToPolicyMetricsProjector
        tell renewalDeclined to entity PolicyContext.Policy
      }
      on policyCancelled: event Policy.PolicyCancelled is {
        send policyCancelled to outlet PolicyEventSplitToPolicyMetricsProjector
        send policyCancelled to outlet PolicyEventSplitToToDocumentService
        send policyCancelled to outlet PolicyEventSplitToToBillingService
        tell policyCancelled to entity PolicyContext.Policy
      }
      on policyReinstated: event Policy.PolicyReinstated is {
        send policyReinstated to outlet PolicyEventSplitToPolicyMetricsProjector
        tell policyReinstated to entity PolicyContext.Policy
      }
      on billingUpdated: event Policy.BillingUpdated is {
        send billingUpdated to outlet PolicyEventSplitToPolicyMetricsProjector
        tell billingUpdated to entity PolicyContext.Policy
      }
      on renewalOfferGenerated: event Policy.RenewalOfferGenerated is {
        send renewalOfferGenerated to outlet PolicyEventSplitToPolicyMetricsProjector
        send renewalOfferGenerated to outlet PolicyEventSplitToToRatingEngine
        send renewalOfferGenerated to outlet PolicyEventSplitToToDocumentService
        tell renewalOfferGenerated to entity PolicyContext.Policy
      }
      on other is {
        error "Unexpected message for processor PolicyEventSplit"
      }
    } with {
      briefly "Policy event fan-out handler"
      described as {
        |Delivers each Policy event to all of its consumers.
      }
    }
  } with {
    briefly "Policy event fan-out"
    described as {
      |Fans Policy events out to the repository and projectors.
    }
  }
  connector 'ToRatingEngine Feed' is from outlet PolicyContext.PolicyEventSplit.PolicyEventSplitToToRatingEngine to inlet PolicyContext.ToRatingEngine.ToRatingEngineIn with {
    briefly "The Policy events ToRatingEngine translates, on their way to it"
  }
  connector 'ToDocumentService Feed' is from outlet PolicyContext.PolicyEventSplit.PolicyEventSplitToToDocumentService to inlet PolicyContext.ToDocumentService.ToDocumentServiceIn with {
    briefly "The Policy events ToDocumentService translates, on their way to it"
  }
  connector 'ToBillingService Feed' is from outlet PolicyContext.PolicyEventSplit.PolicyEventSplitToToBillingService to inlet PolicyContext.ToBillingService.ToBillingServiceIn with {
    briefly "The Policy events ToBillingService translates, on their way to it"
  }
  connector 'PolicyEvent Stream' is from outlet PolicyContext.Policy.PolicyEvents2 to inlet PolicyContext.PolicyEventSplit.PolicyEventSplitIn with {
    briefly "Policy events, fanned out to storage and read models"
  }
  connector 'PolicyMetricsProjector Feed' is from outlet PolicyContext.PolicyEventSplit.PolicyEventSplitToPolicyMetricsProjector to inlet PolicyContext.PolicyMetricsProjector.PolicyMetricsProjectorFromPolicy with {
    briefly "Policy events feeding the PolicyMetricsProjector projection"
  }
  connector 'PolicyMetricsProjector Storage' is from outlet PolicyContext.PolicyMetricsProjector.PolicyMetricsProjectorToRepository to inlet PolicyContext.PolicyRepository.PolicyRepositoryFromPolicyMetricsProjector with {
    briefly "PolicyMetricsProjector updates on their way to storage"
  }
} with {
  briefly "Bounded context for policy administration"
  described as {
    | The PolicyContext is the bounded context for policy
    | administration including the Policy aggregate, repository,
    | and projections for metrics.
  }
}
