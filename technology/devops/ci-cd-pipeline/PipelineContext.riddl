// CI/CD Pipeline - Pipeline Context
context PipelineContext is {
  adaptor FromArtifactStorageService from context ArtifactStorageService is { 
    handler FromArtifactStorageServiceHandler is {
      on result ArtifactStorageService.ArtifactDownloadUrl is {
        do "the model receives artifact download url"
      }
      on event ArtifactStorageService.ArtifactUploaded is {
        do "the model receives artifact uploaded"
      }
      on other is {
        error "Unexpected message for adaptor FromArtifactStorageService"
      }
    } with {
      briefly "from artifact storage service"
      described as {
        |Carries messages from the external ArtifactStorageService.
      }
    }
  } with {
    briefly "ArtifactStorageService integration"
    described as {
      |The model's side of the ArtifactStorageService conversation.
    }
  }
  adaptor ToArtifactStorageService to context ArtifactStorageService is { 
    handler ToArtifactStorageServiceHandler is {
      on query ArtifactStorageService.DownloadArtifact is {
        do "the model sends download artifact"
      }
      on command ArtifactStorageService.UploadArtifact is {
        do "the model sends upload artifact"
      }
      on other is {
        error "Unexpected message for adaptor ToArtifactStorageService"
      }
    } with {
      briefly "to artifact storage service"
      described as {
        |Carries messages to the external ArtifactStorageService.
      }
    }
  } with {
    briefly "ArtifactStorageService integration"
    described as {
      |The model's side of the ArtifactStorageService conversation.
    }
  }
  adaptor FromContainerRegistryService from context ContainerRegistryService is { 
    handler FromContainerRegistryServiceHandler is {
      on event ContainerRegistryService.ImagePushed is {
        do "the model receives image pushed"
      }
      on event ContainerRegistryService.ImageScanned is {
        do "the model receives image scanned"
      }
      on other is {
        error "Unexpected message for adaptor FromContainerRegistryService"
      }
    } with {
      briefly "from container registry service"
      described as {
        |Carries messages from the external ContainerRegistryService.
      }
    }
  } with {
    briefly "ContainerRegistryService integration"
    described as {
      |The model's side of the ContainerRegistryService conversation.
    }
  }
  adaptor ToContainerRegistryService to context ContainerRegistryService is { 
    handler ToContainerRegistryServiceHandler is {
      on command ContainerRegistryService.PushImage is {
        do "the model sends push image"
      }
      on command ContainerRegistryService.ScanImage is {
        do "the model sends scan image"
      }
      on other is {
        error "Unexpected message for adaptor ToContainerRegistryService"
      }
    } with {
      briefly "to container registry service"
      described as {
        |Carries messages to the external ContainerRegistryService.
      }
    }
  } with {
    briefly "ContainerRegistryService integration"
    described as {
      |The model's side of the ContainerRegistryService conversation.
    }
  }
  adaptor FromDeploymentOrchestratorService from context DeploymentOrchestratorService is { 
    handler FromDeploymentOrchestratorServiceHandler is {
      on event DeploymentOrchestratorService.DeploymentFailed is {
        do "the model receives deployment failed"
      }
      on event DeploymentOrchestratorService.DeploymentRolledBack is {
        do "the model receives deployment rolled back"
      }
      on event DeploymentOrchestratorService.DeploymentStarted is {
        do "the model receives deployment started"
      }
      on event DeploymentOrchestratorService.DeploymentSucceeded is {
        do "the model receives deployment succeeded"
      }
      on other is {
        error "Unexpected message for adaptor FromDeploymentOrchestratorService"
      }
    } with {
      briefly "from deployment orchestrator service"
      described as {
        |Carries messages from the external DeploymentOrchestratorService.
      }
    }
  } with {
    briefly "DeploymentOrchestratorService integration"
    described as {
      |The model's side of the DeploymentOrchestratorService conversation.
    }
  }
  adaptor ToDeploymentOrchestratorService to context DeploymentOrchestratorService is { 
    handler ToDeploymentOrchestratorServiceHandler is {
      on command DeploymentOrchestratorService.DeployToEnvironment is {
        do "the model sends deploy to environment"
      }
      on command DeploymentOrchestratorService.RollbackDeployment is {
        do "the model sends rollback deployment"
      }
      on other is {
        error "Unexpected message for adaptor ToDeploymentOrchestratorService"
      }
    } with {
      briefly "to deployment orchestrator service"
      described as {
        |Carries messages to the external DeploymentOrchestratorService.
      }
    }
  } with {
    briefly "DeploymentOrchestratorService integration"
    described as {
      |The model's side of the DeploymentOrchestratorService conversation.
    }
  }
  adaptor FromNotificationService from context NotificationService is { 
    handler FromNotificationServiceHandler is {
      on event NotificationService.NotificationSent is {
        do "the model receives notification sent"
      }
      on other is {
        error "Unexpected message for adaptor FromNotificationService"
      }
    } with {
      briefly "from notification service"
      described as {
        |Carries messages from the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor ToNotificationService to context NotificationService is { 
    handler ToNotificationServiceHandler is {
      on command NotificationService.SendBuildNotification is {
        do "the model sends send build notification"
      }
      on other is {
        error "Unexpected message for adaptor ToNotificationService"
      }
    } with {
      briefly "to notification service"
      described as {
        |Carries messages to the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor FromSourceControlService from context SourceControlService is { 
    handler FromSourceControlServiceHandler is {
      on event SourceControlService.CommitStatusSet is {
        do "the model receives commit status set"
      }
      on event SourceControlService.RepositoryFetched is {
        do "the model receives repository fetched"
      }
      on other is {
        error "Unexpected message for adaptor FromSourceControlService"
      }
    } with {
      briefly "from source control service"
      described as {
        |Carries messages from the external SourceControlService.
      }
    }
  } with {
    briefly "SourceControlService integration"
    described as {
      |The model's side of the SourceControlService conversation.
    }
  }
  adaptor ToSourceControlService to context SourceControlService is { 
    handler ToSourceControlServiceHandler is {
      on command SourceControlService.FetchRepository is {
        do "the model sends fetch repository"
      }
      on command SourceControlService.SetCommitStatus is {
        do "the model sends set commit status"
      }
      on other is {
        error "Unexpected message for adaptor ToSourceControlService"
      }
    } with {
      briefly "to source control service"
      described as {
        |Carries messages to the external SourceControlService.
      }
    }
  } with {
    briefly "SourceControlService integration"
    described as {
      |The model's side of the SourceControlService conversation.
    }
  }
  include "types.riddl"
  include "Pipeline.riddl"
  // Repository for pipeline persistence
  repository PipelineRepository as merge is {
    inlet PipelineRepositoryFromPipeline is type PipelineEvent    inlet PipelineRepositoryFromPipelineAnalytics is type PipelineEvent    outlet PipelineRepositoryResponses is type Pipeline.PipelineResult
    record StoredPipeline is  {
      pipelineId: PipelineId with {
        briefly "pipelineId"
        described as {
          |The pipelineId column of the stored pipeline row.
        }
      }
      config: PipelineConfig with {
        briefly "config"
        described as {
          |The config column of the stored pipeline row.
        }
      }
    } with {
      briefly "Stored pipeline row"
      described as {
        |The persistence shape of pipeline data as this repository stores it.
      }
    }
    schema PipelineData is relational
      of pipelines as type StoredPipeline
        index on field StoredPipeline.pipelineId
        index on field StoredPipeline.config.repositoryUrl
 with {
      briefly "Pipeline data schema"
      described as {
        |The relational schema holding pipeline data for the PipelineRepository.
      }
    }
    handler PipelinePersistence is {
      on query GetPipelineStats is {
        do "read the pipeline stats projection"
        reply result PipelineStatsView
      }
      on command Pipeline.CreatePipeline is {
        tell event Pipeline.PipelineCreated to entity PipelineContext.Pipeline
      }
      on command Pipeline.StartPipeline is {
        tell event Pipeline.PipelineStarted to entity PipelineContext.Pipeline
      }
      on command Pipeline.CompleteStage is {
        tell event Pipeline.StageCompleted to entity PipelineContext.Pipeline
      }
      on command Pipeline.StoreArtifact is {
        tell event Pipeline.ArtifactStored to entity PipelineContext.Pipeline
      }
      on command Pipeline.RequestApproval is {
        tell event Pipeline.ApprovalRequested to entity PipelineContext.Pipeline
      }
      on command Pipeline.ApproveDeployment is {
        tell event Pipeline.DeploymentApproved to entity PipelineContext.Pipeline
      }
      on command Pipeline.RejectDeployment is {
        tell event Pipeline.DeploymentRejected to entity PipelineContext.Pipeline
      }
      on command Pipeline.CancelPipeline is {
        tell event Pipeline.PipelineCancelled to entity PipelineContext.Pipeline
      }
      on command Pipeline.CompletePipeline is {
        tell event Pipeline.PipelineCompleted to entity PipelineContext.Pipeline
      }
    } with {
      briefly "Persists pipeline commands"
      described as {
        |Handles command-driven persistence.
      }
    }
  } with {
    briefly "Pipeline data persistence"
    described as {
      | The PipelineRepository provides persistent storage for
      | pipelines, builds, and artifacts.
    }
  }
  // Projector for pipeline analytics
  query GetPipelineStats is  {
    pipelineStatsKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which pipeline stats to retrieve.
      }
    }
  } with {
    briefly "Get pipeline stats"
    described as {
      |Retrieves the pipeline stats projection.
    }
  }
  result PipelineStatsView is  {
    pipelineStats: PipelineAnalytics.PipelineStats with {
      briefly "Pipeline stats"
      described as {
        |The pipeline stats being returned.
      }
    }
  } with {
    briefly "Pipeline stats view"
    described as {
      |Read model view carrying the pipeline stats projection.
    }
  }
  projector PipelineAnalytics as flow is {
    updates repository PipelineContext.PipelineRepository
    inlet PipelineAnalyticsFromPipeline is type PipelineEvent    outlet PipelineAnalyticsOut is type PipelineEvent
    record PipelineStats is  {
      totalPipelines: Natural with {
        briefly "Total"
        described as {
          |Total pipelines.
        }
      }
      totalBuilds: Natural with {
        briefly "Builds"
        described as {
          |Total builds.
        }
      }
      successRate: Decimal(5,4) with {
        briefly "Success rate"
        described as {
          |Build success rate.
        }
      }
      averageDuration: Natural with {
        briefly "Avg duration"
        described as {
          |Average duration seconds.
        }
      }
      deploymentsToday: Natural with {
        briefly "Deployments"
        described as {
          |Deployments today.
        }
      }
    } with {
      briefly "Pipeline statistics"
      described as {
        |CI/CD pipeline metrics.
      }
    }
    handler AnalyticsHandler is {
      on event Pipeline.PipelineStarted is {
        tell event Pipeline.PipelineStarted to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.PipelineCompleted is {
        tell event Pipeline.PipelineCompleted to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.DeploymentApproved is {
        tell event Pipeline.DeploymentApproved to repository PipelineContext.PipelineRepository
      }
    } with {
      briefly "Maintains pipeline analytics"
      described as {
        |Projects events into analytics views.
      }
    }
  } with {
    briefly "Pipeline analytics projections"
    described as {
      |Maintains CI/CD pipeline analytics data.
    }
  }
  // Event streaming infrastructure
  type PipelineEvent is one of {
    PipelineContext.Pipeline.PipelineCreated or PipelineContext.Pipeline.PipelineStarted or PipelineContext.Pipeline.StageCompleted or PipelineContext.Pipeline.PipelineCompleted or PipelineContext.Pipeline.PipelineCancelled
  } with {
    briefly "Pipeline events"
    described as {
      |Union of all Pipeline events for streaming.
    }
  }
  processor PipelineSource as source is {
    outlet PipelineEventOutlet is type PipelineEvent
 with {
      briefly "Event outlet"
      described as {
        |Outlet for Pipeline events.
      }
    }
    handler PipelineSourceHandler is {
      on init is {
        tell command Pipeline.InitializePipeline to entity PipelineContext.Pipeline
      }
      on event Pipeline.PipelineCreated is {
        send event Pipeline.PipelineCreated to outlet PipelineEventOutlet
      }
      on event Pipeline.PipelineStarted is {
        send event Pipeline.PipelineStarted to outlet PipelineEventOutlet
      }
      on event Pipeline.StageCompleted is {
        send event Pipeline.StageCompleted to outlet PipelineEventOutlet
      }
      on event Pipeline.PipelineCompleted is {
        send event Pipeline.PipelineCompleted to outlet PipelineEventOutlet
      }
      on event Pipeline.PipelineCancelled is {
        send event Pipeline.PipelineCancelled to outlet PipelineEventOutlet
      }
      on other is {
        error "Unexpected message for source PipelineSource"
      }
    } with {
      briefly "Source handler"
      described as {
        |Handles Pipeline events for streaming.
      }
    }
  } with {
    briefly "Pipeline event source"
    described as {
      |Publishes Pipeline events to the event stream.
    }
  }
  processor PipelineSink as sink is {
    inlet PipelineEventInlet is type PipelineEvent with {
      briefly "Event inlet"
      described as {
        |Inlet for Pipeline events.
      }
    }
    handler PipelineSinkHandler is {
      on init is { ??? }
      on event Pipeline.PipelineCreated is {
        tell event Pipeline.PipelineCreated to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.PipelineStarted is {
        tell event Pipeline.PipelineStarted to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.StageCompleted is {
        tell event Pipeline.StageCompleted to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.PipelineCompleted is {
        tell event Pipeline.PipelineCompleted to repository PipelineContext.PipelineRepository
      }
      on event Pipeline.PipelineCancelled is {
        tell event Pipeline.PipelineCancelled to repository PipelineContext.PipelineRepository
      }
      on other is {
        error "Unexpected message for sink PipelineSink"
      }
    } with {
      briefly "Sink handler"
      described as {
        |Handles incoming Pipeline events for persistence.
      }
    }
  } with {
    briefly "Pipeline event sink"
    described as {
      |Receives Pipeline events for persistence.
    }
  }
  connector PipelineEventFlow is from outlet PipelineContext.PipelineSource.PipelineEventOutlet to inlet PipelineContext.PipelineSink.PipelineEventInlet with {
    briefly "Pipeline event connector"
    described as {
      |Wires Pipeline source to sink for event flow.
    }
  }
  processor PipelineEventSplit as split is {
    inlet PipelineEventSplitIn is type PipelineEvent    outlet PipelineEventSplitToPipelineRepository is type PipelineEvent
    outlet PipelineEventSplitToPipelineAnalytics is type PipelineEvent
    handler PipelineEventSplitHandler is {
      on event Pipeline.PipelineCreated is {
        send event Pipeline.PipelineCreated to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.PipelineCreated to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.PipelineCreated to entity PipelineContext.Pipeline
      }
      on event Pipeline.PipelineStarted is {
        send event Pipeline.PipelineStarted to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.PipelineStarted to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.PipelineStarted to entity PipelineContext.Pipeline
      }
      on event Pipeline.StageCompleted is {
        send event Pipeline.StageCompleted to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.StageCompleted to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.StageCompleted to entity PipelineContext.Pipeline
      }
      on event Pipeline.ArtifactStored is {
        send event Pipeline.ArtifactStored to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.ArtifactStored to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.ArtifactStored to entity PipelineContext.Pipeline
      }
      on event Pipeline.ApprovalRequested is {
        send event Pipeline.ApprovalRequested to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.ApprovalRequested to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.ApprovalRequested to entity PipelineContext.Pipeline
      }
      on event Pipeline.DeploymentApproved is {
        send event Pipeline.DeploymentApproved to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.DeploymentApproved to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.DeploymentApproved to entity PipelineContext.Pipeline
      }
      on event Pipeline.DeploymentRejected is {
        send event Pipeline.DeploymentRejected to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.DeploymentRejected to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.DeploymentRejected to entity PipelineContext.Pipeline
      }
      on event Pipeline.PipelineCancelled is {
        send event Pipeline.PipelineCancelled to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.PipelineCancelled to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.PipelineCancelled to entity PipelineContext.Pipeline
      }
      on event Pipeline.PipelineCompleted is {
        send event Pipeline.PipelineCompleted to outlet PipelineEventSplitToPipelineRepository
        send event Pipeline.PipelineCompleted to outlet PipelineEventSplitToPipelineAnalytics
        tell event Pipeline.PipelineCompleted to entity PipelineContext.Pipeline
      }
      on other is {
        error "Unexpected message for processor PipelineEventSplit"
      }
    } with {
      briefly "Pipeline event fan-out handler"
      described as {
        |Delivers each Pipeline event to all of its consumers.
      }
    }
  } with {
    briefly "Pipeline event fan-out"
    described as {
      |Fans Pipeline events out to the repository and projectors.
    }
  }
  connector 'PipelineEvent Stream' is from outlet PipelineContext.Pipeline.PipelineEvents to inlet PipelineContext.PipelineEventSplit.PipelineEventSplitIn with {
    briefly "Pipeline events, fanned out to storage and read models"
  }
  connector 'PipelineEvent Storage' is from outlet PipelineContext.PipelineEventSplit.PipelineEventSplitToPipelineRepository to inlet PipelineContext.PipelineRepository.PipelineRepositoryFromPipeline with {
    briefly "Pipeline events on their way to storage"
  }
  connector 'PipelineAnalytics Feed' is from outlet PipelineContext.PipelineEventSplit.PipelineEventSplitToPipelineAnalytics to inlet PipelineContext.PipelineAnalytics.PipelineAnalyticsFromPipeline with {
    briefly "Pipeline events feeding the PipelineAnalytics projection"
  }
  connector 'PipelineAnalytics Storage' is from outlet PipelineContext.PipelineAnalytics.PipelineAnalyticsOut to inlet PipelineContext.PipelineRepository.PipelineRepositoryFromPipelineAnalytics with {
    briefly "The PipelineAnalytics projection on its way to storage"
  }
} with {
  briefly "Bounded context for CI/CD pipelines"
  described as {
    | The PipelineContext is the bounded context for
    | continuous integration and deployment automation.
  }
}
