// Observability - Alert Context
context AlertContext is {
  include "types.riddl"
  include "Alert.riddl"
  // Repository for alert persistence
  repository AlertRepository is {
    schema AlertData is relational
      of alerts as type Alert
        index on field Alert.alertId
        index on field Alert.rule.severity

    handler AlertPersistence is {
      on command Alert.CreateAlert is {
        tell event Alert.AlertCreated to entity AlertContext.Alert
        prompt "Store new alert"
      }
      on command Alert.AcknowledgeAlert is {
        tell event Alert.AlertAcknowledged to entity AlertContext.Alert
        prompt "Update acknowledgment status"
      }
      on command Alert.ResolveAlert is {
        prompt "Update to resolved"
      }
      on command Alert.SilenceAlert is {
        prompt "Update silence status"
      }
      on command Alert.EscalateAlert is {
        prompt "Update escalation level"
      }
      on command Alert.AddComment is {
        tell event Alert.CommentAdded to entity AlertContext.Alert
        prompt "Store alert comment"
      }
      on command Alert.ExpireAlert is {
        prompt "Update to expired"
      }
    } with {
      briefly "Persists alert commands"
      described as {
        |Handles command-driven persistence.
      }
    }
  } with {
    briefly "Alert data persistence"
    described as {
      | The AlertRepository provides persistent storage for
      | alerts, comments, and resolution history.
    }
  }
  // Projector for observability analytics
  projector ObservabilityAnalytics is {
    updates repository AlertContext.AlertRepository
    record ObservabilityStats is  {
      activeAlerts: Natural with {
        briefly "Active"
        described as {
          |Active alerts.
        }
      }
      criticalAlerts: Natural with {
        briefly "Critical"
        described as {
          |Critical alerts.
        }
      }
      alertsToday: Natural with {
        briefly "Today"
        described as {
          |Alerts fired today.
        }
      }
      mttr: Natural with {
        briefly "MTTR"
        described as {
          |Mean time to resolve (minutes).
        }
      }
      acknowledgeRate: Decimal(5,4) with {
        briefly "Ack rate"
        described as {
          |Acknowledgment rate.
        }
      }
    } with {
      briefly "Observability statistics"
      described as {
        |Alert and incident metrics.
      }
    }
    handler AnalyticsHandler is {
      on event Alert.AlertCreated is {
        tell event Alert.AlertCreated to repository AlertContext.AlertRepository
        prompt "Update alert counts"
      }
      on event Alert.AlertAcknowledged is {
        tell event Alert.AlertAcknowledged to repository AlertContext.AlertRepository
        prompt "Update ack metrics"
      }
      on event Alert.AlertResolved is {
        tell event Alert.AlertResolved to repository AlertContext.AlertRepository
        prompt "Update resolution metrics"
      }
    } with {
      briefly "Maintains observability analytics"
      described as {
        |Projects events into analytics views.
      }
    }
  } with {
    briefly "Observability analytics projections"
    described as {
      |Maintains alert and incident analytics data.
    }
  }
  // ---- Event publication for Alert ----
  type AlertEvent is one of {
    AlertContext.Alert.AlertCreated,
    AlertContext.Alert.AlertAcknowledged,
    AlertContext.Alert.AlertResolved,
    AlertContext.Alert.AlertSilenced,
    AlertContext.Alert.AlertEscalated,
    AlertContext.Alert.CommentAdded,
    AlertContext.Alert.AlertExpired
  } with {
    briefly "Alert event alternation"
    described as {
      |Union of all Alert events, for streaming and event-sourced
      |persistence.
    }
  }
  source AlertEventSource is {
    outlet AlertEvents is type AlertEvent
    handler AlertSourceHandler is {
      on event Alert.AlertCreated is {
        send event Alert.AlertCreated to outlet AlertEvents
      }
      on event Alert.AlertAcknowledged is {
        send event Alert.AlertAcknowledged to outlet AlertEvents
      }
      on event Alert.AlertResolved is {
        send event Alert.AlertResolved to outlet AlertEvents
      }
      on event Alert.AlertSilenced is {
        send event Alert.AlertSilenced to outlet AlertEvents
      }
      on event Alert.AlertEscalated is {
        send event Alert.AlertEscalated to outlet AlertEvents
      }
      on event Alert.CommentAdded is {
        send event Alert.CommentAdded to outlet AlertEvents
      }
      on event Alert.AlertExpired is {
        send event Alert.AlertExpired to outlet AlertEvents
      }
      on other is {
        error "Unexpected message for source AlertEventSource"
      }
    } with {
      briefly "Alert source handler"
      described as {
        |Publishes Alert events onto the outlet.
      }
    }
  } with {
    briefly "Alert event source"
    described as {
      |Publishes Alert entity events into the streaming topology.
    }
  }
  sink AlertEventSink is {
    inlet AlertInlet is type AlertEvent
    handler AlertSinkHandler is {
      on event Alert.AlertCreated is {
        tell event Alert.AlertCreated to repository AlertContext.AlertRepository
      }
      on event Alert.AlertAcknowledged is {
        tell event Alert.AlertAcknowledged to repository AlertContext.AlertRepository
      }
      on event Alert.AlertResolved is {
        tell event Alert.AlertResolved to repository AlertContext.AlertRepository
      }
      on event Alert.AlertSilenced is {
        tell event Alert.AlertSilenced to repository AlertContext.AlertRepository
      }
      on event Alert.AlertEscalated is {
        tell event Alert.AlertEscalated to repository AlertContext.AlertRepository
      }
      on event Alert.CommentAdded is {
        tell event Alert.CommentAdded to repository AlertContext.AlertRepository
      }
      on event Alert.AlertExpired is {
        tell event Alert.AlertExpired to repository AlertContext.AlertRepository
      }
      on other is {
        error "Unexpected message for sink AlertEventSink"
      }
    } with {
      briefly "Alert sink handler"
      described as {
        |Persists Alert events via the repository.
      }
    }
  } with {
    briefly "Alert event sink"
    described as {
      |Receives Alert events for persistence.
    }
  }
  connector AlertEventFlow is {
    from outlet AlertContext.AlertEventSource.AlertEvents to
      inlet AlertContext.AlertEventSink.AlertInlet
  } with {
    briefly "Alert event flow"
    described as {
      |Connects the Alert source to its sink.
    }
  }
} with {
  briefly "Bounded context for alerting"
  described as {
    | The AlertContext is the bounded context for
    | alert management and incident tracking.
  }
}
