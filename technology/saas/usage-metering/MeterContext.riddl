// Usage Metering - Meter Context
context MeterContext as router is {
  inlet MeterContextFromToBillingService is command Meter.GenerateBillingRecord with {
    briefly "What ToBillingService makes of the answers it gets"
  }
  connector 'ToBillingService Intake' is from outlet MeterContext.ToBillingService.ToBillingServiceToMeterContext to inlet MeterContext.MeterContextFromToBillingService with {
    briefly "Answers translated by ToBillingService, on their way in"
  }
  inlet MeterContextFromToEventIngestionService is command Meter.AdjustUsage with {
    briefly "What ToEventIngestionService makes of the answers it gets"
  }
  connector 'ToEventIngestionService Intake' is from outlet MeterContext.ToEventIngestionService.ToEventIngestionServiceToMeterContext to inlet MeterContext.MeterContextFromToEventIngestionService with {
    briefly "Answers translated by ToEventIngestionService, on their way in"
  }
  inlet MeterContextFromToSubscriptionService is command Meter.PauseMeter with {
    briefly "What ToSubscriptionService makes of the answers it gets"
  }
  connector 'ToSubscriptionService Intake' is from outlet MeterContext.ToSubscriptionService.ToSubscriptionServiceToMeterContext to inlet MeterContext.MeterContextFromToSubscriptionService with {
    briefly "Answers translated by ToSubscriptionService, on their way in"
  }
  inlet MeterCommandStreamIn is type MeterCommand  outlet MeterCommandStreamOut is type MeterCommand
  inlet MeterQueryResultStreamIn is result Meter.MeterQueryResult  outlet MeterQueryResultStreamOut is result Meter.MeterQueryResult
  handler MeterContextBoundary is {
    on createMeter: command Meter.CreateMeter is {
      send createMeter to outlet MeterContext.MeterCommandStreamOut
    }
    on recordUsageEvent: command Meter.RecordUsageEvent is {
      send recordUsageEvent to outlet MeterContext.MeterCommandStreamOut
    }
    on recordBatchEvents: command Meter.RecordBatchEvents is {
      send recordBatchEvents to outlet MeterContext.MeterCommandStreamOut
    }
    on aggregateUsage: command Meter.AggregateUsage is {
      send aggregateUsage to outlet MeterContext.MeterCommandStreamOut
    }
    on setAlertThreshold: command Meter.SetAlertThreshold is {
      send setAlertThreshold to outlet MeterContext.MeterCommandStreamOut
    }
    on removeAlertThreshold: command Meter.RemoveAlertThreshold is {
      send removeAlertThreshold to outlet MeterContext.MeterCommandStreamOut
    }
    on generateBillingRecord: command Meter.GenerateBillingRecord is {
      send generateBillingRecord to outlet MeterContext.MeterCommandStreamOut
    }
    on adjustUsage: command Meter.AdjustUsage is {
      send adjustUsage to outlet MeterContext.MeterCommandStreamOut
    }
    on pauseMeter: command Meter.PauseMeter is {
      send pauseMeter to outlet MeterContext.MeterCommandStreamOut
    }
    on resumeMeter: command Meter.ResumeMeter is {
      send resumeMeter to outlet MeterContext.MeterCommandStreamOut
    }
    on closeMeter: command Meter.CloseMeter is {
      send closeMeter to outlet MeterContext.MeterCommandStreamOut
    }
    on meterQueryResult: result Meter.MeterQueryResult is {
      send meterQueryResult to outlet MeterContext.MeterQueryResultStreamOut
    }
    on other is {
      error "Unexpected message at the MeterContext boundary"
    }
  } with {
    briefly "Context boundary relay"
    described as {
      |The context is the port at its own boundary: peers connect here, never
      |to a definition inside. Inbound messages are relayed to the definition
      |that owns them; outbound ones are republished from here.
    }
  }
  connector 'MeterCommand Stream Intake' is from outlet MeterContext.MeterCommandStreamOut to inlet MeterContext.Meter.MeterCommands  connector 'MeterQueryResult Stream Egress' is from outlet MeterContext.MeterRepository.MeterRepositoryResponses to inlet MeterContext.MeterQueryResultStreamIn  adaptor FromAnalyticsExportService from context AnalyticsExportService as sink is { 
    inlet FromAnalyticsExportServiceIn is event AnalyticsExportService.ExportCompleted    handler FromAnalyticsExportServiceHandler is {
      on exportCompleted: event AnalyticsExportService.ExportCompleted is {
        do "the export is done; the meter's own state is unchanged"
      }
      on other is {
        error "Unexpected message for adaptor FromAnalyticsExportService"
      }
    } with {
      briefly "from analytics export service"
      described as {
        |Carries messages from the external AnalyticsExportService.
      }
    }
  } with {
    briefly "AnalyticsExportService integration"
    described as {
      |The model's side of the AnalyticsExportService conversation.
    }
  }
  adaptor ToAnalyticsExportService to context AnalyticsExportService as flow is { 
    inlet ToAnalyticsExportServiceIn is type MeterEvent    outlet ToAnalyticsExportServiceOut is command AnalyticsExportService.ExportUsageData
    handler ToAnalyticsExportServiceHandler is {
      on usageAggregated: event Meter.UsageAggregated is {
        let item0: type AnalyticsExportService.ExportUsageData = prompt("translate the usageAggregated event into a ExportUsageData, aggregation being what makes usage worth exporting")
        send item0 to outlet MeterContext.ToAnalyticsExportService.ToAnalyticsExportServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToAnalyticsExportService"
      }
    } with {
      briefly "to analytics export service"
      described as {
        |Carries messages to the external AnalyticsExportService.
      }
    }
  } with {
    briefly "AnalyticsExportService integration"
    described as {
      |The model's side of the AnalyticsExportService conversation.
    }
  }
  adaptor FromBillingService from context BillingService as sink is { 
    inlet FromBillingServiceIn is event BillingService.InvoiceCreated    handler FromBillingServiceHandler is {
      on invoiceCreated: event BillingService.InvoiceCreated is {
        do "the invoice exists; generating the billing record was recorded here"
      }
      on other is {
        error "Unexpected message for adaptor FromBillingService"
      }
    } with {
      briefly "from billing service"
      described as {
        |Carries messages from the external BillingService.
      }
    }
  } with {
    briefly "BillingService integration"
    described as {
      |The model's side of the BillingService conversation.
    }
  }
  adaptor ToBillingService to context BillingService as router is { 
    outlet ToBillingServiceToMeterContext is command Meter.GenerateBillingRecord
    inlet ToBillingServiceIn is type MeterEvent    inlet ToBillingServiceReplies is result BillingService.InvoiceStatusResult    outlet ToBillingServiceOut is type BillingService.BillingServiceRequest
    handler ToBillingServiceHandler is {
      on billingRecordGenerated: event Meter.BillingRecordGenerated is {
        let notice: type BillingService.CreateInvoice = prompt("translate the billingRecordGenerated event into a CreateInvoice for the usage just aggregated and closed into a billing record")
        send notice to outlet MeterContext.ToBillingService.ToBillingServiceOut
      }
      on meterClosed: event Meter.MeterClosed is {
        let askAnswer: type BillingService.InvoiceStatusResult = ask query BillingService.GetInvoiceStatus of context BillingService
        do "closing a meter needing to know whether what it produced has been invoiced"
        when prompt("askAnswer.paymentStatus shows the meter's output not yet invoiced") then
          send command Meter.GenerateBillingRecord(meterId = meterClosed.meterId, billingPeriodStart = prompt("the period's start"), billingPeriodEnd = prompt("the period's end")) to outlet MeterContext.ToBillingService.ToBillingServiceToMeterContext
        end
      }
      on other is {
        error "Unexpected message for adaptor ToBillingService"
      }
    } with {
      briefly "to billing service"
      described as {
        |Carries messages to the external BillingService.
      }
    }
  } with {
    briefly "BillingService integration"
    described as {
      |The model's side of the BillingService conversation.
    }
  }
  adaptor FromEventIngestionService from context EventIngestionService as sink is { 
    inlet FromEventIngestionServiceIn is event EventIngestionService.RawUsageReceived    handler FromEventIngestionServiceHandler is {
      on rawUsageReceived: event EventIngestionService.RawUsageReceived is {
        do "raw usage arrived at ingestion; the meter records events once they are attributed to it"
      }
      on other is {
        error "Unexpected message for adaptor FromEventIngestionService"
      }
    } with {
      briefly "from event ingestion service"
      described as {
        |Carries messages from the external EventIngestionService.
      }
    }
  } with {
    briefly "EventIngestionService integration"
    described as {
      |The model's side of the EventIngestionService conversation.
    }
  }
  adaptor ToEventIngestionService to context EventIngestionService as router is { 
    outlet ToEventIngestionServiceToMeterContext is command Meter.AdjustUsage
    inlet ToEventIngestionServiceIn is type MeterEvent    inlet ToEventIngestionServiceReplies is result EventIngestionService.IngestionStatsResult    outlet ToEventIngestionServiceOut is query EventIngestionService.GetIngestionStats
    handler ToEventIngestionServiceHandler is {
      on batchEventsRecorded: event Meter.BatchEventsRecorded is {
        let askAnswer: type EventIngestionService.IngestionStatsResult = ask query EventIngestionService.GetIngestionStats of context EventIngestionService
        do "a batch recorded needing ingestion's own count to reconcile against"
        when prompt("askAnswer.successCount differs from batchEventsRecorded.eventCount") then
          send command Meter.AdjustUsage(meterId = batchEventsRecorded.meterId, metricType = prompt("the batch's metric"), adjustment = prompt("the difference between the batch and askAnswer.successCount"), reason = prompt("reconciled to ingestion"), adjustedBy = prompt("the ingestion service")) to outlet MeterContext.ToEventIngestionService.ToEventIngestionServiceToMeterContext
        end
      }
      on other is {
        error "Unexpected message for adaptor ToEventIngestionService"
      }
    } with {
      briefly "to event ingestion service"
      described as {
        |Carries messages to the external EventIngestionService.
      }
    }
  } with {
    briefly "EventIngestionService integration"
    described as {
      |The model's side of the EventIngestionService conversation.
    }
  }
  adaptor ToNotificationService to context NotificationService as flow is { 
    inlet ToNotificationServiceIn is type MeterEvent    outlet ToNotificationServiceOut is type NotificationService.NotificationServiceCommand
    handler ToNotificationServiceHandler is {
      on usageAlertTriggered: event UsageAlertTriggered is {
        let msg: type NotificationService.SendUsageAlert = prompt("translate the usageAlertTriggered event into a SendUsageAlert for its recipient")
        send msg to outlet MeterContext.ToNotificationService.ToNotificationServiceOut
      }
      on billingRecordGenerated: event BillingRecordGenerated is {
        let notice: type NotificationService.SendUsageSummary = prompt("translate the billingRecordGenerated event into a SendUsageSummary giving the customer the usage behind the charge they are about to see, so the invoice is not their first sight of it")
        send notice to outlet MeterContext.ToNotificationService.ToNotificationServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToNotificationService"
      }
    } with {
      briefly "to notification service"
      described as {
        |Carries messages to the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor FromSubscriptionService from context SubscriptionService as sink is { 
    inlet FromSubscriptionServiceIn is event SubscriptionService.SubscriptionChanged    handler FromSubscriptionServiceHandler is {
      on subscriptionChanged: event SubscriptionService.SubscriptionChanged is {
        do "the subscription changed; which meters to open or close is a configuration decision"
      }
      on other is {
        error "Unexpected message for adaptor FromSubscriptionService"
      }
    } with {
      briefly "from subscription service"
      described as {
        |Carries messages from the external SubscriptionService.
      }
    }
  } with {
    briefly "SubscriptionService integration"
    described as {
      |The model's side of the SubscriptionService conversation.
    }
  }
  adaptor ToSubscriptionService to context SubscriptionService as router is { 
    outlet ToSubscriptionServiceToMeterContext is command Meter.PauseMeter
    inlet ToSubscriptionServiceIn is type MeterEvent    inlet ToSubscriptionServiceReplies is result SubscriptionService.SubscriptionDetailsResult    outlet ToSubscriptionServiceOut is query SubscriptionService.GetSubscriptionDetails
    handler ToSubscriptionServiceHandler is {
      on meterCreated: event Meter.MeterCreated is {
        let askAnswer: type SubscriptionService.SubscriptionDetailsResult = ask query SubscriptionService.GetSubscriptionDetails of context SubscriptionService
        do "a new meter needing the subscription that decides what it may measure"
        when prompt("askAnswer.subscriptionStatus is not active") then
          send command Meter.PauseMeter(meterId = meterCreated.meterId, reason = prompt("subscription askAnswer.subscriptionStatus is not active")) to outlet MeterContext.ToSubscriptionService.ToSubscriptionServiceToMeterContext
        end
      }
      on other is {
        error "Unexpected message for adaptor ToSubscriptionService"
      }
    } with {
      briefly "to subscription service"
      described as {
        |Carries messages to the external SubscriptionService.
      }
    }
  } with {
    briefly "SubscriptionService integration"
    described as {
      |The model's side of the SubscriptionService conversation.
    }
  }
  include "types.riddl"
  include "Meter.riddl"
  // Repository
  type MeterRepositoryCommand is one of {
    MeterContext.MeterRepository.PersistAlertThresholdRemoved or MeterContext.MeterRepository.PersistAlertThresholdSet or MeterContext.MeterRepository.PersistBatchEventsRecorded or MeterContext.MeterRepository.PersistBillingRecordGenerated or MeterContext.MeterRepository.PersistMeterClosed or MeterContext.MeterRepository.PersistMeterCreated or MeterContext.MeterRepository.PersistMeterPaused or MeterContext.MeterRepository.PersistMeterResumed or MeterContext.MeterRepository.PersistUsageAdjusted or MeterContext.MeterRepository.PersistUsageAggregated or MeterContext.MeterRepository.PersistUsageAlertTriggered or MeterContext.MeterRepository.PersistUsageEventRecorded
  } with {
    briefly "Anything that writes MeterRepository"
    described as {
      |The persistence commands MeterRepository accepts. Entity events never
      |reach it directly; a projector turns each into one of these.
    }
  }
  repository MeterRepository as flow is {
    inlet MeterRepositoryFromUsageAnalytics is type MeterRepositoryCommand    command PersistAlertThresholdRemoved is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row AlertThresholdRemoved applies to.
        }
      }
    } with {
      briefly "Persist AlertThresholdRemoved"
      described as {
        |Instructs this repository to apply AlertThresholdRemoved to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistAlertThresholdSet is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row AlertThresholdSet applies to.
        }
      }
    } with {
      briefly "Persist AlertThresholdSet"
      described as {
        |Instructs this repository to apply AlertThresholdSet to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistBatchEventsRecorded is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row BatchEventsRecorded applies to.
        }
      }
    } with {
      briefly "Persist BatchEventsRecorded"
      described as {
        |Instructs this repository to apply BatchEventsRecorded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistMeterClosed is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row MeterClosed applies to.
        }
      }
    } with {
      briefly "Persist MeterClosed"
      described as {
        |Instructs this repository to apply MeterClosed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistMeterCreated is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row MeterCreated applies to.
        }
      }
    } with {
      briefly "Persist MeterCreated"
      described as {
        |Instructs this repository to apply MeterCreated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistMeterPaused is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row MeterPaused applies to.
        }
      }
    } with {
      briefly "Persist MeterPaused"
      described as {
        |Instructs this repository to apply MeterPaused to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistMeterResumed is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row MeterResumed applies to.
        }
      }
    } with {
      briefly "Persist MeterResumed"
      described as {
        |Instructs this repository to apply MeterResumed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistUsageAdjusted is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row UsageAdjusted applies to.
        }
      }
    } with {
      briefly "Persist UsageAdjusted"
      described as {
        |Instructs this repository to apply UsageAdjusted to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistBillingRecordGenerated is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row BillingRecordGenerated applies to.
        }
      }
    } with {
      briefly "Persist BillingRecordGenerated"
      described as {
        |Instructs this repository to apply BillingRecordGenerated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistUsageAggregated is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row UsageAggregated applies to.
        }
      }
    } with {
      briefly "Persist UsageAggregated"
      described as {
        |Instructs this repository to apply UsageAggregated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistUsageAlertTriggered is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row UsageAlertTriggered applies to.
        }
      }
    } with {
      briefly "Persist UsageAlertTriggered"
      described as {
        |Instructs this repository to apply UsageAlertTriggered to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistUsageEventRecorded is {
      meterId: MeterId with {
        briefly "Row addressed"
        described as {
          |Which stored row UsageEventRecorded applies to.
        }
      }
    } with {
      briefly "Persist UsageEventRecorded"
      described as {
        |Instructs this repository to apply UsageEventRecorded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    record StoredMeter is {
      meterId: MeterId with {
        briefly "meterId"
        described as {
          |The meterId column of the stored meter row.
        }
      }
    } with {
      briefly "Stored meter row"
      described as {
        |The persistence shape of meter data as this repository stores it.
      }
    }
    schema MeterData is relational
      of meters as record StoredMeter
        index on field StoredMeter.meterId
 with {
      briefly "Meter data schema"
      described as {
        |The relational schema persisting meter records for this repository.
      }
    }
    outlet MeterRepositoryResponses is result Meter.MeterQueryResult
    query FindMeterById is {
      meterId: MeterId with {
        briefly "Meter"
        described as {
          |Meter identifier.
        }
      }
    } with {
      briefly "Find meter by ID"
      described as {
        |Retrieves meter by identifier.
      }
    }
    result MeterResult is {
      meterId: MeterId with {
        briefly "Meter addressed"
        described as {
          |Which meter this message concerns. Named directly rather
          |than left to a wrapped record, because a nested record cannot say
          |which instance is addressed.
        }
      }
      meter: Meter.ActiveStateData with {
        briefly "Meter"
        described as {
          |Meter data.
        }
      }
    } with {
      briefly "Meter result"
      described as {
        |Returns meter data.
      }
    }
    query FindByCustomer is {
      customerId: CustomerId with {
        briefly "Customer"
        described as {
          |Customer identifier.
        }
      }
    } with {
      briefly "Find by customer"
      described as {
        |Finds meters for customer.
      }
    }
    result MetersResult is {
      meterId: MeterId with {
        briefly "Meter addressed"
        described as {
          |Which meter this message concerns. Named directly rather
          |than left to a wrapped record, because a nested record cannot say
          |which instance is addressed.
        }
      }
      meters: Meter.ActiveStateData+ with {
        briefly "Meters"
        described as {
          |Matching meters.
        }
      }
      totalCount: Natural with {
        briefly "Count"
        described as {
          |Total matches.
        }
      }
    } with {
      briefly "Meters result"
      described as {
        |Returns matching meters.
      }
    }
    query FindBySubscription is {
      subscriptionId: SubscriptionId with {
        briefly "Subscription"
        described as {
          |Subscription identifier.
        }
      }
    } with {
      briefly "Find by subscription"
      described as {
        |Finds meters for subscription.
      }
    }
    query GetCurrentUsage is {
      meterId: MeterId with {
        briefly "Meter"
        described as {
          |Meter identifier.
        }
      }
    } with {
      briefly "Get current usage"
      described as {
        |Gets current period usage.
      }
    }
    query GetUsageHistory is {
      meterId: MeterId with {
        briefly "Meter"
        described as {
          |Meter identifier.
        }
      }
      periods: Natural with {
        briefly "Periods"
        described as {
          |Number of periods.
        }
      }
    } with {
      briefly "Get usage history"
      described as {
        |Gets historical usage.
      }
    }
    handler MeterRepositoryHandler is {
      on persistAlertThresholdRemoved: command PersistAlertThresholdRemoved is {
        do "apply AlertThresholdRemoved to the stored row"
      }
      on persistAlertThresholdSet: command PersistAlertThresholdSet is {
        do "apply AlertThresholdSet to the stored row"
      }
      on persistBatchEventsRecorded: command PersistBatchEventsRecorded is {
        do "apply BatchEventsRecorded to the stored row"
      }
      on persistMeterClosed: command PersistMeterClosed is {
        do "apply MeterClosed to the stored row"
      }
      on persistMeterCreated: command PersistMeterCreated is {
        do "apply MeterCreated to the stored row"
      }
      on persistMeterPaused: command PersistMeterPaused is {
        do "apply MeterPaused to the stored row"
      }
      on persistMeterResumed: command PersistMeterResumed is {
        do "apply MeterResumed to the stored row"
      }
      on persistUsageAdjusted: command PersistUsageAdjusted is {
        do "apply UsageAdjusted to the stored row"
      }
      on persistBillingRecordGenerated: command PersistBillingRecordGenerated is {
        do "apply BillingRecordGenerated to the stored row"
      }
      on persistUsageAggregated: command PersistUsageAggregated is {
        do "apply UsageAggregated to the stored row"
      }
      on persistUsageAlertTriggered: command PersistUsageAlertTriggered is {
        do "apply UsageAlertTriggered to the stored row"
      }
      on persistUsageEventRecorded: command PersistUsageEventRecorded is {
        do "apply UsageEventRecorded to the stored row"
      }
      on query GetAnalyticsData is {
        let analyticsDataView: type AnalyticsDataView = prompt("read the analytics data projection")
        reply analyticsDataView
      }
      on query FindMeterById is {
        let meterResult: type MeterResult = prompt("read the meter from the store")
        tell meterResult to entity MeterContext.Meter
      }
      on query FindByCustomer is {
        let metersResult: type MetersResult = prompt("read the meters that by customer asks for")
        tell metersResult to entity MeterContext.Meter
      }
      on query FindBySubscription is {
        let metersResult: type MetersResult = prompt("read the meters that by subscription asks for")
        tell metersResult to entity MeterContext.Meter
      }
      on query GetCurrentUsage is {
        let meterResult: type MeterResult = prompt("read the meter that current usage asks for")
        tell meterResult to entity MeterContext.Meter
      }
      on query GetUsageHistory is {
        let metersResult: type MetersResult = prompt("read the meters that usage history asks for")
        tell metersResult to entity MeterContext.Meter
      }
    } with {
      briefly "Handles repository queries"
      described as {
        |Processes meter queries.
      }
    }
  } with {
    briefly "Meter repository"
    described as {
      |Persistence for meter records.
    }
  }
  // Projector for usage analytics
  query GetAnalyticsData replies result AnalyticsDataView is {
    analyticsDataKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which analytics data to retrieve.
      }
    }
  } with {
    briefly "Get analytics data"
    described as {
      |Retrieves the analytics data projection.
    }
  }
  result AnalyticsDataView is {
    analyticsData: UsageAnalytics.AnalyticsData with {
      briefly "Analytics data"
      described as {
        |The analytics data being returned.
      }
    }
  } with {
    briefly "Analytics data view"
    described as {
      |Read model view carrying the analytics data projection.
    }
  }
  projector UsageAnalytics as flow is {
    updates repository MeterContext.MeterRepository
    outlet UsageAnalyticsToRepository is type MeterRepositoryCommand
    inlet UsageAnalyticsFromMeter is type MeterEvent    record AnalyticsData is {
      totalEvents: Natural with {
        briefly "Events"
        described as {
          |Total events recorded.
        }
      }
      totalCustomers: Natural with {
        briefly "Customers"
        described as {
          |Total metered customers.
        }
      }
      usageByMetric: MetricUsage+ with {
        briefly "By metric"
        described as {
          |Usage by metric type.
        }
      }
      topCustomers: CustomerUsage+ with {
        briefly "Top customers"
        described as {
          |Highest usage customers.
        }
      }
      alertsTriggered: Natural with {
        briefly "Alerts"
        described as {
          |Alerts triggered today.
        }
      }
    } with {
      briefly "Analytics data"
      described as {
        |Usage analytics.
      }
    }
    record MetricUsage is {
      metricType: MetricType with {
        briefly "Metric"
        described as {
          |Metric type.
        }
      }
      aggregatedTotalQuantity: Decimal(18,4) with {
        briefly "Total"
        described as {
          |Total usage.
        }
      }
      eventCount: Natural with {
        briefly "Events"
        described as {
          |Event count.
        }
      }
    } with {
      briefly "Metric usage"
      described as {
        |Usage for metric type.
      }
    }
    record CustomerUsage is {
      customerId: CustomerId with {
        briefly "Customer"
        described as {
          |Customer ID.
        }
      }
      totalUsage: Decimal(14,4) with {
        briefly "Usage"
        described as {
          |Total usage.
        }
      }
      totalAmount: Decimal(12,2) with {
        briefly "Amount"
        described as {
          |Billable amount.
        }
      }
    } with {
      briefly "Customer usage"
      described as {
        |Usage for customer.
      }
    }
    handler AnalyticsHandler is {
      on alertThresholdRemoved: event Meter.AlertThresholdRemoved is {
        tell command MeterContext.MeterRepository.PersistAlertThresholdRemoved(meterId = alertThresholdRemoved.meterId) to repository MeterContext.MeterRepository
        do "apply AlertThresholdRemoved to the UsageAnalytics projection"
      }
      on alertThresholdSet: event Meter.AlertThresholdSet is {
        tell command MeterContext.MeterRepository.PersistAlertThresholdSet(meterId = alertThresholdSet.meterId) to repository MeterContext.MeterRepository
        do "apply AlertThresholdSet to the UsageAnalytics projection"
      }
      on batchEventsRecorded: event Meter.BatchEventsRecorded is {
        tell command MeterContext.MeterRepository.PersistBatchEventsRecorded(meterId = batchEventsRecorded.meterId) to repository MeterContext.MeterRepository
        do "apply BatchEventsRecorded to the UsageAnalytics projection"
      }
      on meterClosed: event Meter.MeterClosed is {
        tell command MeterContext.MeterRepository.PersistMeterClosed(meterId = meterClosed.meterId) to repository MeterContext.MeterRepository
        do "apply MeterClosed to the UsageAnalytics projection"
      }
      on meterCreated: event Meter.MeterCreated is {
        tell command MeterContext.MeterRepository.PersistMeterCreated(meterId = meterCreated.meterId) to repository MeterContext.MeterRepository
        do "apply MeterCreated to the UsageAnalytics projection"
      }
      on meterPaused: event Meter.MeterPaused is {
        tell command MeterContext.MeterRepository.PersistMeterPaused(meterId = meterPaused.meterId) to repository MeterContext.MeterRepository
        do "apply MeterPaused to the UsageAnalytics projection"
      }
      on meterResumed: event Meter.MeterResumed is {
        tell command MeterContext.MeterRepository.PersistMeterResumed(meterId = meterResumed.meterId) to repository MeterContext.MeterRepository
        do "apply MeterResumed to the UsageAnalytics projection"
      }
      on usageAdjusted: event Meter.UsageAdjusted is {
        tell command MeterContext.MeterRepository.PersistUsageAdjusted(meterId = usageAdjusted.meterId) to repository MeterContext.MeterRepository
        do "apply UsageAdjusted to the UsageAnalytics projection"
      }
      on usageEventRecorded: event Meter.UsageEventRecorded is {
        tell command MeterContext.MeterRepository.PersistUsageEventRecorded(meterId = usageEventRecorded.meterId) to repository MeterContext.MeterRepository
      }
      on usageAggregated: event Meter.UsageAggregated is {
        tell command MeterContext.MeterRepository.PersistUsageAggregated(meterId = usageAggregated.meterId) to repository MeterContext.MeterRepository
      }
      on usageAlertTriggered: event Meter.UsageAlertTriggered is {
        tell command MeterContext.MeterRepository.PersistUsageAlertTriggered(meterId = usageAlertTriggered.meterId) to repository MeterContext.MeterRepository
      }
      on billingRecordGenerated: event Meter.BillingRecordGenerated is {
        tell command MeterContext.MeterRepository.PersistBillingRecordGenerated(meterId = billingRecordGenerated.meterId) to repository MeterContext.MeterRepository
      }
    } with {
      briefly "Processes analytics events"
      described as {
        |Updates usage analytics.
      }
    }
  } with {
    briefly "Usage analytics"
    described as {
      |Projects usage metrics for reporting.
    }
  }
  // Event streaming infrastructure
  type MeterEvent is one of {
    MeterContext.Meter.MeterCreated or MeterContext.Meter.UsageEventRecorded or MeterContext.Meter.UsageAggregated or MeterContext.Meter.BillingRecordGenerated or MeterContext.Meter.MeterClosed or MeterContext.Meter.AlertThresholdRemoved or MeterContext.Meter.AlertThresholdSet or MeterContext.Meter.BatchEventsRecorded or MeterContext.Meter.MeterPaused or MeterContext.Meter.MeterResumed or MeterContext.Meter.UsageAdjusted or MeterContext.Meter.UsageAlertTriggered
  } with {
    briefly "Meter events"
    described as {
      |Union of all Meter events for streaming.
    }
  }
  streamlet MeterEventSplit as split is {
    outlet MeterEventSplitToToSubscriptionService is type MeterEvent
    outlet MeterEventSplitToToNotificationService is type MeterEvent
    outlet MeterEventSplitToToEventIngestionService is type MeterEvent
    outlet MeterEventSplitToToBillingService is type MeterEvent
    outlet MeterEventSplitToToAnalyticsExportService is type MeterEvent
    inlet MeterEventSplitIn is type MeterEvent    outlet MeterEventSplitToUsageAnalytics is type MeterEvent
    handler MeterEventSplitHandler is {
      on meterCreated: event Meter.MeterCreated is {
        send meterCreated to outlet MeterEventSplitToUsageAnalytics
        send meterCreated to outlet MeterEventSplitToToSubscriptionService
        tell meterCreated to entity MeterContext.Meter
      }
      on usageEventRecorded: event Meter.UsageEventRecorded is {
        send usageEventRecorded to outlet MeterEventSplitToUsageAnalytics
        tell usageEventRecorded to entity MeterContext.Meter
      }
      on batchEventsRecorded: event Meter.BatchEventsRecorded is {
        send batchEventsRecorded to outlet MeterEventSplitToUsageAnalytics
        send batchEventsRecorded to outlet MeterEventSplitToToEventIngestionService
        tell batchEventsRecorded to entity MeterContext.Meter
      }
      on usageAggregated: event Meter.UsageAggregated is {
        send usageAggregated to outlet MeterEventSplitToUsageAnalytics
        send usageAggregated to outlet MeterEventSplitToToAnalyticsExportService
        tell usageAggregated to entity MeterContext.Meter
      }
      on alertThresholdSet: event Meter.AlertThresholdSet is {
        send alertThresholdSet to outlet MeterEventSplitToUsageAnalytics
        tell alertThresholdSet to entity MeterContext.Meter
      }
      on alertThresholdRemoved: event Meter.AlertThresholdRemoved is {
        send alertThresholdRemoved to outlet MeterEventSplitToUsageAnalytics
        tell alertThresholdRemoved to entity MeterContext.Meter
      }
      on usageAlertTriggered: event Meter.UsageAlertTriggered is {
        send usageAlertTriggered to outlet MeterEventSplitToUsageAnalytics
        send usageAlertTriggered to outlet MeterEventSplitToToNotificationService
        tell usageAlertTriggered to entity MeterContext.Meter
      }
      on billingRecordGenerated: event Meter.BillingRecordGenerated is {
        send billingRecordGenerated to outlet MeterEventSplitToUsageAnalytics
        send billingRecordGenerated to outlet MeterEventSplitToToNotificationService
        send billingRecordGenerated to outlet MeterEventSplitToToBillingService
        tell billingRecordGenerated to entity MeterContext.Meter
      }
      on usageAdjusted: event Meter.UsageAdjusted is {
        send usageAdjusted to outlet MeterEventSplitToUsageAnalytics
        tell usageAdjusted to entity MeterContext.Meter
      }
      on meterPaused: event Meter.MeterPaused is {
        send meterPaused to outlet MeterEventSplitToUsageAnalytics
        tell meterPaused to entity MeterContext.Meter
      }
      on meterResumed: event Meter.MeterResumed is {
        send meterResumed to outlet MeterEventSplitToUsageAnalytics
        tell meterResumed to entity MeterContext.Meter
      }
      on meterClosed: event Meter.MeterClosed is {
        send meterClosed to outlet MeterEventSplitToUsageAnalytics
        send meterClosed to outlet MeterEventSplitToToBillingService
        tell meterClosed to entity MeterContext.Meter
      }
      on other is {
        error "Unexpected message for processor MeterEventSplit"
      }
    } with {
      briefly "Meter event fan-out handler"
      described as {
        |Delivers each Meter event to all of its consumers.
      }
    }
  } with {
    briefly "Meter event fan-out"
    described as {
      |Fans Meter events out to the repository and projectors.
    }
  }
  connector 'ToSubscriptionService Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToToSubscriptionService to inlet MeterContext.ToSubscriptionService.ToSubscriptionServiceIn with {
    briefly "The Meter events ToSubscriptionService translates, on their way to it"
  }
  connector 'ToNotificationService Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToToNotificationService to inlet MeterContext.ToNotificationService.ToNotificationServiceIn with {
    briefly "The Meter events ToNotificationService translates, on their way to it"
  }
  connector 'ToEventIngestionService Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToToEventIngestionService to inlet MeterContext.ToEventIngestionService.ToEventIngestionServiceIn with {
    briefly "The Meter events ToEventIngestionService translates, on their way to it"
  }
  connector 'ToBillingService Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToToBillingService to inlet MeterContext.ToBillingService.ToBillingServiceIn with {
    briefly "The Meter events ToBillingService translates, on their way to it"
  }
  connector 'ToAnalyticsExportService Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToToAnalyticsExportService to inlet MeterContext.ToAnalyticsExportService.ToAnalyticsExportServiceIn with {
    briefly "The Meter events ToAnalyticsExportService translates, on their way to it"
  }
  connector 'MeterEvent Stream' is from outlet MeterContext.Meter.MeterEvents to inlet MeterContext.MeterEventSplit.MeterEventSplitIn with {
    briefly "Meter events, fanned out to storage and read models"
  }
  connector 'UsageAnalytics Feed' is from outlet MeterContext.MeterEventSplit.MeterEventSplitToUsageAnalytics to inlet MeterContext.UsageAnalytics.UsageAnalyticsFromMeter with {
    briefly "Meter events feeding the UsageAnalytics projection"
  }
  connector 'UsageAnalytics Storage' is from outlet MeterContext.UsageAnalytics.UsageAnalyticsToRepository to inlet MeterContext.MeterRepository.MeterRepositoryFromUsageAnalytics with {
    briefly "UsageAnalytics updates on their way to storage"
  }
} with {
  briefly "Meter context"
  described as {
    | The Meter context manages usage metering
    | for SaaS applications.
  }
}
