// Fund Entity
// Core entity representing an investment fund
entity Fund as flow is {
  type FundCommand is one of {
    Fund.CreateFund or Fund.CalculateNAV or Fund.RecordTransaction or Fund.UpdateHolding or Fund.ProcessSubscription or Fund.ProcessRedemption or Fund.SuspendFund or Fund.ResumeFund
  } with {
    briefly "Fund command alternation"
    described as {
      |Commands accepted by the Fund entity.
    }
  }
  inlet FundCommands is type FundCommand  outlet FundEvents2 is type FundEvent
  // === Commands ===
  command CreateFund yields event FundCreated is  {
    fundId: FundId
    name: String(1,200)
    fundType: FundType
    baseCurrency: Currency(USD)
    inceptionDate: Date
    managementFeeRate: Percentage
    performanceFeeRate: Percentage?
    minimumInvestment: Money
  } with {
    briefly "Create a new investment fund"
    described as {
      | Initializes a new fund with its core configuration including
      | name, type, fee structure, and minimum investment requirements.
      | The fund starts in Active status.
    }
  }
  command CalculateNAV yields event NAVCalculated is  {
    fundId: FundId
    valuationDate: Date
    isOfficial: Boolean
  } with {
    briefly "Calculate the Net Asset Value for a fund"
    described as {
      | Triggers the NAV calculation process for the specified date.
      | Market data and holdings are used to compute total assets,
      | liabilities, and the resulting per-share NAV.
    }
  }
  command RecordTransaction yields event TransactionRecorded is  {
    transactionId: TransactionId
    fundId: FundId
    transactionType: TransactionType
    tradeSecurityId: SecurityId?
    tradeQuantity: Quantity?
    tradePrice: Price?
    grossAmount: Money
    fees: Money
    tradeDate: Date
    settlementDate: Date
    description: String(,500)
  } with {
    briefly "Record a new fund transaction"
    described as {
      | Records a transaction affecting the fund such as security
      | purchases, sales, dividend receipts, or fee payments. The
      | transaction updates holdings and affects NAV calculation.
    }
  }
  command UpdateHolding yields event HoldingUpdated is  {
    holdingId: HoldingId
    fundId: FundId
    securityId: SecurityId
    quantity: Quantity
    marketValue: Money
  } with {
    briefly "Update a portfolio holding position"
    described as {
      | Updates the quantity or market value of an existing holding.
      | Used during position reconciliation and mark-to-market
      | processes.
    }
  }
  command ProcessSubscription yields event SubscriptionProcessed is  {
    accountId: InvestorAccountId
    fundId: FundId
    investorName: String(1,200)
    subscriptionAmount: Money
    subscriptionDate: Date
  } with {
    briefly "Process an investor subscription"
    described as {
      | Records a new investment into the fund. Calculates shares
      | issued based on current NAV and updates investor account
      | and fund shares outstanding.
    }
  }
  command ProcessRedemption yields event RedemptionProcessed is  {
    accountId: InvestorAccountId
    fundId: FundId
    shares: Quantity
    redemptionDate: Date
  } with {
    briefly "Process an investor redemption"
    described as {
      | Records a redemption from the fund. Calculates redemption
      | proceeds based on current NAV and updates investor account
      | and fund shares outstanding.
    }
  }
  command SuspendFund yields event FundSuspended is  {
    fundId: FundId
    reason: String(1,500)
    suspensionDate: Date
  } with {
    briefly "Suspend fund operations"
    described as {
      | Temporarily halts fund operations including subscriptions
      | and redemptions. Used during unusual market conditions or
      | significant events affecting the fund.
    }
  }
  command ResumeFund yields event FundResumed is  {
    fundId: FundId
    resumptionDate: Date
  } with {
    briefly "Resume suspended fund operations"
    described as {
      | Resumes normal fund operations after a suspension period.
      | The fund returns to Active status and accepts new transactions.
    }
  }
  // === Events ===
  event FundCreated is  {
    fundId: FundId
    name: String(1,200)
    fundType: FundType
    inceptionDate: Date
    createdAt: TimeStamp
  } with {
    briefly "Fund has been created"
    described as {
      | Emitted when a new fund is successfully initialized and
      | ready for operations.
    }
  }
  event NAVCalculated is  {
    navId: NAVId
    fundId: FundId
    valuationDate: Date
    netAssetValue: Money
    navPerShare: Price
    sharesOutstanding: Quantity
    isOfficial: Boolean
    calculatedAt: TimeStamp
  } with {
    briefly "NAV calculation completed"
    described as {
      | Emitted when a NAV calculation completes successfully.
      | Contains the calculated values and whether this is the
      | official published NAV.
    }
  }
  event TransactionRecorded is  {
    transactionId: TransactionId
    fundId: FundId
    transactionType: TransactionType
    netAmount: Money
    tradeDate: Date
    recordedAt: TimeStamp
  } with {
    briefly "Transaction has been recorded"
    described as {
      | Emitted when a transaction is successfully recorded.
      | Triggers downstream processes such as holding updates
      | and NAV recalculation.
    }
  }
  event HoldingUpdated is  {
    holdingId: HoldingId
    fundId: FundId
    securityId: SecurityId
    quantity: Quantity
    marketValue: Money
    updatedAt: TimeStamp
  } with {
    briefly "Holding position has been updated"
    described as {
      | Emitted when a holding's quantity or value changes.
      | Triggers recalculation of portfolio allocations and
      | unrealized gains.
    }
  }
  event SubscriptionProcessed is  {
    accountId: InvestorAccountId
    fundId: FundId
    investorName: String(1,200)
    subscriptionAmount: Money
    sharesIssued: Quantity
    navPerShare: Price
    processedAt: TimeStamp
  } with {
    briefly "Investor subscription has been processed"
    described as {
      | Emitted when an investor's subscription is processed and
      | shares are issued.
    }
  }
  event RedemptionProcessed is  {
    accountId: InvestorAccountId
    fundId: FundId
    sharesRedeemed: Quantity
    redemptionAmount: Money
    navPerShare: Price
    processedAt: TimeStamp
  } with {
    briefly "Investor redemption has been processed"
    described as {
      | Emitted when an investor's redemption is processed and
      | funds are distributed.
    }
  }
  event FundSuspended is  {
    fundId: FundId
    reason: String(1,500)
    suspensionDate: Date
    suspendedAt: TimeStamp
  } with {
    briefly "Fund operations have been suspended"
    described as {
      | Emitted when a fund enters suspended status. New
      | transactions are blocked until the fund is resumed.
    }
  }
  event FundResumed is  {
    fundId: FundId
    resumptionDate: Date
    resumedAt: TimeStamp
  } with {
    briefly "Fund operations have resumed"
    described as {
      | Emitted when a suspended fund returns to active status.
    }
  }
  // === Queries ===
  query GetFundInfo replies result FundInfoResult is { fundId: FundId } with {
    briefly "Retrieve fund information"
    described as {
      | Queries the current fund configuration and status information.
    }
  }
  result FundInfoResult is  {
    fundId: FundId with {
      briefly "Fund addressed"
      described as {
        |Which fund this message concerns.
      }
    }
    fundInfo: FundInfo
  } with {
    briefly "Fund information query result"
    described as {
      | Returns the fund's current configuration and status.
    }
  }
  query GetNAVHistory is  {
    fundId: FundId
    startDate: Date
    endDate: Date
  } with {
    briefly "Retrieve NAV history for a date range"
    described as {
      | Queries historical NAV values for the specified period.
    }
  }
  result NAVHistoryResult is  {
    fundId: FundId with {
      briefly "Fund addressed"
      described as {
        |Which fund this message concerns.
      }
    }
    navRecords: NAVRecord+
  } with {
    briefly "NAV history query result"
    described as {
      | Returns a list of NAV records for the requested date range.
    }
  }
  query GetHoldings is  {
    fundId: FundId
    asOfDate: Date
  } with {
    briefly "Retrieve fund holdings"
    described as {
      | Queries the fund's portfolio holdings as of a specific date.
    }
  }
  result HoldingsResult is  {
    fundId: FundId with {
      briefly "Fund addressed"
      described as {
        |Which fund this message concerns.
      }
    }
    holdings: HoldingRecord+
  } with {
    briefly "Holdings query result"
    described as {
      | Returns the list of current portfolio holdings with
      | positions and valuations.
    }
  }
  query GetPerformance is  {
    fundId: FundId
    asOfDate: Date
  } with {
    briefly "Retrieve fund performance metrics"
    described as {
      | Queries calculated performance returns for various periods.
    }
  }
  result PerformanceResult is  {
    fundId: FundId with {
      briefly "Fund addressed"
      described as {
        |Which fund this message concerns.
      }
    }
    performance: FundPerformance
  } with {
    briefly "Performance query result"
    described as {
      | Returns the fund's performance metrics for standard periods.
    }
  }
  // === States ===
  initial state ActiveFund of record FundInfo is {
    initial handler ActiveFundInitHandler is {
      on init is {
        set state ActiveFund to "initialize from prior event"
      }
    } with {
      briefly "Active fund init handler"
      described as {
        |Initializes the Active fund state.
      }
    }
  } with {
    briefly "Active fund state"
    described as {
      |Fund in active operational status.
    }
  }
  state SuspendedFund of record FundInfo is {
    initial handler SuspendedFundInitHandler is {
      on init is {
        set state SuspendedFund to "initialize from prior event"
      }
    } with {
      briefly "Suspended fund init handler"
      described as {
        |Initializes the Suspended fund state.
      }
    }
  } with {
    briefly "Suspended fund state"
    described as {
      |Fund in suspended status, transactions blocked.
    }
  }
  // === Handler ===
  handler FundCommandHandler is {
    on createFund: command CreateFund is {
      do "Validate fund configuration and initialize fund state"
      morph entity Fund to state ActiveFund with record FundInfo(fundId = createFund.fundId, name = createFund.name, fundType = createFund.fundType, inceptionDate = createFund.inceptionDate, baseCurrency = createFund.baseCurrency, managementFeeRate = createFund.managementFeeRate, performanceFeeRate = createFund.performanceFeeRate, minimumInvestment = createFund.minimumInvestment, status = prompt("the status of this fund info"))
      yield event FundCreated(fundId = createFund.fundId, name = createFund.name, fundType = createFund.fundType, inceptionDate = createFund.inceptionDate, createdAt = prompt("the created at of this fund created"))
    } with {
      briefly "Handles fund creation"
      described as {
        | Validates the fund configuration and creates a new fund
        | in active state.
      }
    }
    on calculateNAV: command CalculateNAV is {
      do "Retrieve current holdings and market prices"
      do "Calculate total assets by summing holding market values"
      do "Calculate total liabilities including accrued fees"
      do "Compute net asset value and per-share NAV"
      yield event NAVCalculated(fundId = calculateNAV.fundId, valuationDate = calculateNAV.valuationDate, isOfficial = calculateNAV.isOfficial, navId = prompt("the nav id of this n a v calculated"), netAssetValue = prompt("the net asset value of this n a v calculated"), navPerShare = prompt("the nav per share of this n a v calculated"), sharesOutstanding = prompt("the shares outstanding of this n a v calculated"), calculatedAt = prompt("the calculated at of this n a v calculated"))
    } with {
      briefly "Handles NAV calculation requests"
      described as {
        | Processes the NAV calculation by aggregating holdings,
        | applying market prices, and computing the per-share value.
      }
    }
    on recordTransaction: command RecordTransaction is {
      when prompt("transactionType is Buy or Sell") then
        do "Update security holding position"
        do "Calculate realized gain/loss for sells"
      end
      do "Record transaction with pending status"
      do "Update fund cash position"
      yield event TransactionRecorded(transactionId = recordTransaction.transactionId, fundId = recordTransaction.fundId, transactionType = recordTransaction.transactionType, tradeDate = recordTransaction.tradeDate, netAmount = prompt("the net amount of this transaction recorded"), recordedAt = prompt("the recorded at of this transaction recorded"))
    } with {
      briefly "Handles transaction recording"
      described as {
        | Records the transaction and updates affected holdings
        | and positions.
      }
    }
    on updateHolding: command UpdateHolding is {
      do "Recalculate holding cost basis and unrealized gain/loss"
      do "Update portfolio allocation percentages"
      yield event HoldingUpdated(holdingId = updateHolding.holdingId, fundId = updateHolding.fundId, securityId = updateHolding.securityId, quantity = updateHolding.quantity, marketValue = updateHolding.marketValue, updatedAt = prompt("the updated at of this holding updated"))
    } with {
      briefly "Handles holding updates"
      described as {
        | Updates holding position and recalculates related metrics.
      }
    }
    on processSubscription: command ProcessSubscription is {
      do "Retrieve current NAV per share"
      do "Calculate shares to issue based on investment amount"
      do "Create or update investor account"
      do "Increase fund shares outstanding"
      yield event SubscriptionProcessed(accountId = processSubscription.accountId, fundId = processSubscription.fundId, investorName = processSubscription.investorName, subscriptionAmount = processSubscription.subscriptionAmount, sharesIssued = prompt("the shares issued of this subscription processed"), navPerShare = prompt("the nav per share of this subscription processed"), processedAt = prompt("the processed at of this subscription processed"))
    } with {
      briefly "Handles investor subscriptions"
      described as {
        | Processes new investments by calculating shares to issue
        | and updating investor accounts.
      }
    }
    on processRedemption: command ProcessRedemption is {
      do "Validate sufficient shares in investor account"
      do "Calculate redemption amount based on current NAV"
      do "Reduce investor account balance"
      do "Reduce fund shares outstanding"
      yield event RedemptionProcessed(accountId = processRedemption.accountId, fundId = processRedemption.fundId, sharesRedeemed = prompt("the shares redeemed of this redemption processed"), redemptionAmount = prompt("the redemption amount of this redemption processed"), navPerShare = prompt("the nav per share of this redemption processed"), processedAt = prompt("the processed at of this redemption processed"))
    } with {
      briefly "Handles investor redemptions"
      described as {
        | Processes redemption requests by calculating proceeds
        | and updating accounts.
      }
    }
    on suspendFund: command SuspendFund is {
      morph entity Fund to state SuspendedFund with record FundInfo(fundId = suspendFund.fundId, name = prompt("the name of this fund info"), fundType = prompt("the fund type of this fund info"), status = prompt("the status of this fund info"), inceptionDate = prompt("the inception date of this fund info"), baseCurrency = prompt("the base currency of this fund info"), managementFeeRate = prompt("the management fee rate of this fund info"), performanceFeeRate = empty, minimumInvestment = prompt("the minimum investment of this fund info"))
      yield event FundSuspended(fundId = suspendFund.fundId, reason = suspendFund.reason, suspensionDate = suspendFund.suspensionDate, suspendedAt = prompt("the suspended at of this fund suspended"))
    } with {
      briefly "Handles fund suspension"
      described as {
        | Transitions the fund to suspended state and blocks new
        | transactions.
      }
    }
    on resumeFund: command ResumeFund is {
      morph entity Fund to state ActiveFund with record FundInfo(fundId = resumeFund.fundId, name = prompt("the name of this fund info"), fundType = prompt("the fund type of this fund info"), status = prompt("the status of this fund info"), inceptionDate = prompt("the inception date of this fund info"), baseCurrency = prompt("the base currency of this fund info"), managementFeeRate = prompt("the management fee rate of this fund info"), performanceFeeRate = empty, minimumInvestment = prompt("the minimum investment of this fund info"))
      yield event FundResumed(fundId = resumeFund.fundId, resumptionDate = resumeFund.resumptionDate, resumedAt = prompt("the resumed at of this fund resumed"))
    } with {
      briefly "Handles fund resumption"
      described as {
        | Returns a suspended fund to active operational status.
      }
    }
    on query GetFundInfo is {
      let fundInfoResult: type FundInfoResult = prompt("read the fund info from the store")
      reply fundInfoResult
    } with {
      briefly "Handles fund info queries"
      described as {
        | Returns the current fund configuration and status.
      }
    }
    on query GetNAVHistory is {
      let nAVHistoryResult: type NAVHistoryResult = prompt("read the n a v history from the store")
      tell nAVHistoryResult to entity FundContext.Fund
    } with {
      briefly "Handles NAV history queries"
      described as {
        | Returns historical NAV values for reporting and analysis.
      }
    }
    on query GetHoldings is {
      let holdingsResult: type HoldingsResult = prompt("read the holdings from the store")
      tell holdingsResult to entity FundContext.Fund
    } with {
      briefly "Handles holdings queries"
      described as {
        | Returns the fund's portfolio positions.
      }
    }
    on query GetPerformance is {
      let performanceResult: type PerformanceResult = prompt("read the performance from the store")
      tell performanceResult to entity FundContext.Fund
    } with {
      briefly "Handles performance queries"
      described as {
        | Returns calculated performance returns for standard periods.
      }
    }
      on fundCreated: event FundCreated is {
      do "apply FundCreated to the fund so it can be persisted and replayed"
    }
    on fundResumed: event FundResumed is {
      do "apply FundResumed to the fund so it can be persisted and replayed"
    }
    on fundSuspended: event FundSuspended is {
      do "apply FundSuspended to the fund so it can be persisted and replayed"
    }
    on holdingUpdated: event HoldingUpdated is {
      do "apply HoldingUpdated to the fund so it can be persisted and replayed"
    }
    on nAVCalculated: event NAVCalculated is {
      do "apply NAVCalculated to the fund so it can be persisted and replayed"
    }
    on redemptionProcessed: event RedemptionProcessed is {
      do "apply RedemptionProcessed to the fund so it can be persisted and replayed"
    }
    on subscriptionProcessed: event SubscriptionProcessed is {
      do "apply SubscriptionProcessed to the fund so it can be persisted and replayed"
    }
    on transactionRecorded: event TransactionRecorded is {
      do "apply TransactionRecorded to the fund so it can be persisted and replayed"
    }
} with {
    briefly "Primary command handler for Fund entity"
    described as {
      | Handles all commands and queries for fund operations including
      | NAV calculations, transaction processing, and investor account
      | management.
    }
  }
} with {
  briefly "Investment fund entity"
  described as {
    | The Fund entity represents an investment fund and manages its
    | lifecycle, holdings, transactions, and investor accounts. It
    | serves as the aggregate root for fund accounting operations.
  }
}
