// Permit Management - Permit Context
context PermitContext as router is {
  inlet PermitContextFromToDocumentStorageService is command Permit.AttachRetrievedDocument with {
    briefly "What ToDocumentStorageService makes of the answers it gets"
  }
  connector 'ToDocumentStorageService Intake' is from outlet PermitContext.ToDocumentStorageService.ToDocumentStorageServiceToPermitContext to inlet PermitContext.PermitContextFromToDocumentStorageService with {
    briefly "Answers translated by ToDocumentStorageService, on their way in"
  }
  type ToGISServiceCommand is one of {
    Permit.RecordParcelInfo or Permit.RecordZoningRequirements
  } with {
    briefly "What ToGISService turns its answers into"
  }
  inlet PermitContextFromToGISService is type ToGISServiceCommand with {
    briefly "What ToGISService makes of the answers it gets"
  }
  connector 'ToGISService Intake' is from outlet PermitContext.ToGISService.ToGISServiceToPermitContext to inlet PermitContext.PermitContextFromToGISService with {
    briefly "Answers translated by ToGISService, on their way in"
  }
  inlet PermitContextFromToContractorLicensingService is command Permit.RequestCorrections with {
    briefly "What ToContractorLicensingService makes of the answers it gets"
  }
  connector 'ToContractorLicensingService Intake' is from outlet PermitContext.ToContractorLicensingService.ToContractorLicensingServiceToPermitContext to inlet PermitContext.PermitContextFromToContractorLicensingService with {
    briefly "Answers translated by ToContractorLicensingService, on their way in"
  }
  inlet PermitContextFromToFeeCalculationService is command Permit.CalculateFees with {
    briefly "What ToFeeCalculationService makes of the answers it gets"
  }
  connector 'ToFeeCalculationService Intake' is from outlet PermitContext.ToFeeCalculationService.ToFeeCalculationServiceToPermitContext to inlet PermitContext.PermitContextFromToFeeCalculationService with {
    briefly "Answers translated by ToFeeCalculationService, on their way in"
  }
  inlet PermitContextFromToInspectionSchedulingService is command Permit.ScheduleInspection with {
    briefly "What ToInspectionSchedulingService makes of the answers it gets"
  }
  connector 'ToInspectionSchedulingService Intake' is from outlet PermitContext.ToInspectionSchedulingService.ToInspectionSchedulingServiceToPermitContext to inlet PermitContext.PermitContextFromToInspectionSchedulingService with {
    briefly "Answers translated by ToInspectionSchedulingService, on their way in"
  }
  inlet PermitContextFromInspectionSchedulingService is command Permit.ScheduleInspection with {
    briefly "What FromInspectionSchedulingService makes of that service's news"
  }
  connector 'FromInspectionSchedulingService Intake' is from outlet PermitContext.FromInspectionSchedulingService.FromInspectionSchedulingServiceToPermitContext to inlet PermitContext.PermitContextFromInspectionSchedulingService with {
    briefly "What FromInspectionSchedulingService makes of that news, on its way in"
  }
  inlet PermitCommandStreamIn is type PermitCommand  outlet PermitCommandStreamOut is type PermitCommand
  inlet PermitQueryResultStreamIn is result Permit.PermitQueryResult  outlet PermitQueryResultStreamOut is result Permit.PermitQueryResult
  handler PermitContextBoundary is {
    on recordZoningRequirements: command Permit.RecordZoningRequirements is {
      send recordZoningRequirements to outlet PermitContext.PermitCommandStreamOut
    }
    on recordParcelInfo: command Permit.RecordParcelInfo is {
      send recordParcelInfo to outlet PermitContext.PermitCommandStreamOut
    }
    on attachRetrievedDocument: command Permit.AttachRetrievedDocument is {
      send attachRetrievedDocument to outlet PermitContext.PermitCommandStreamOut
    }
    on submitApplication: command Permit.SubmitApplication is {
      send submitApplication to outlet PermitContext.PermitCommandStreamOut
    }
    on uploadDocument: command Permit.UploadDocument is {
      send uploadDocument to outlet PermitContext.PermitCommandStreamOut
    }
    on assignReviewer: command Permit.AssignReviewer is {
      send assignReviewer to outlet PermitContext.PermitCommandStreamOut
    }
    on submitReview: command Permit.SubmitReview is {
      send submitReview to outlet PermitContext.PermitCommandStreamOut
    }
    on requestCorrections: command Permit.RequestCorrections is {
      send requestCorrections to outlet PermitContext.PermitCommandStreamOut
    }
    on submitCorrections: command Permit.SubmitCorrections is {
      send submitCorrections to outlet PermitContext.PermitCommandStreamOut
    }
    on calculateFees: command Permit.CalculateFees is {
      send calculateFees to outlet PermitContext.PermitCommandStreamOut
    }
    on processPayment: command Permit.ProcessPayment is {
      send processPayment to outlet PermitContext.PermitCommandStreamOut
    }
    on approvePermit: command Permit.ApprovePermit is {
      send approvePermit to outlet PermitContext.PermitCommandStreamOut
    }
    on issuePermit: command Permit.IssuePermit is {
      send issuePermit to outlet PermitContext.PermitCommandStreamOut
    }
    on scheduleInspection: command Permit.ScheduleInspection is {
      send scheduleInspection to outlet PermitContext.PermitCommandStreamOut
    }
    on assignInspector: command Permit.AssignInspector is {
      send assignInspector to outlet PermitContext.PermitCommandStreamOut
    }
    on recordInspection: command Permit.RecordInspection is {
      send recordInspection to outlet PermitContext.PermitCommandStreamOut
    }
    on extendPermit: command Permit.ExtendPermit is {
      send extendPermit to outlet PermitContext.PermitCommandStreamOut
    }
    on closePermit: command Permit.ClosePermit is {
      send closePermit to outlet PermitContext.PermitCommandStreamOut
    }
    on voidPermit: command Permit.VoidPermit is {
      send voidPermit to outlet PermitContext.PermitCommandStreamOut
    }
    on revokePermit: command Permit.RevokePermit is {
      send revokePermit to outlet PermitContext.PermitCommandStreamOut
    }
    on permitQueryResult: result Permit.PermitQueryResult is {
      send permitQueryResult to outlet PermitContext.PermitQueryResultStreamOut
    }
    on other is {
      error "Unexpected message at the PermitContext boundary"
    }
  } with {
    briefly "Context boundary relay"
    described as {
      |The context is the port at its own boundary: peers connect here, never
      |to a definition inside. Inbound messages are relayed to the definition
      |that owns them; outbound ones are republished from here.
    }
  }
  connector 'PermitCommand Stream Intake' is from outlet PermitContext.PermitCommandStreamOut to inlet PermitContext.Permit.PermitCommands  connector 'PermitQueryResult Stream Egress' is from outlet PermitContext.PermitRepository.PermitRepositoryResponses to inlet PermitContext.PermitQueryResultStreamIn  adaptor ToContractorLicensingService to context ContractorLicensingService as router is { 
    outlet ToContractorLicensingServiceToPermitContext is command Permit.RequestCorrections
    inlet ToContractorLicensingServiceIn is type PermitEvent    inlet ToContractorLicensingServiceReplies is result ContractorLicensingService.LicenseVerificationResult    outlet ToContractorLicensingServiceOut is query ContractorLicensingService.VerifyLicense
    handler ToContractorLicensingServiceHandler is {
      on applicationSubmitted: event Permit.ApplicationSubmitted is {
        let askAnswer: type ContractorLicensingService.LicenseVerificationResult = ask query ContractorLicensingService.VerifyLicense of context ContractorLicensingService
        do "an application needing the contractor's licence to be current"
        when prompt("not askAnswer.isValid") then
          send command Permit.RequestCorrections(permitId = applicationSubmitted.permitId, corrections = prompt("the licence askAnswer shows lapsed or invalid"), requestedBy = prompt("the contractor licensing service")) to outlet PermitContext.ToContractorLicensingService.ToContractorLicensingServiceToPermitContext
        end
      }
      on other is {
        error "Unexpected message for adaptor ToContractorLicensingService"
      }
    } with {
      briefly "to contractor licensing service"
      described as {
        |Carries messages to the external ContractorLicensingService.
      }
    }
  } with {
    briefly "ContractorLicensingService integration"
    described as {
      |The model's side of the ContractorLicensingService conversation.
    }
  }
  adaptor FromDocumentStorageService from context DocumentStorageService as sink is { 
    inlet FromDocumentStorageServiceIn is event DocumentStorageService.DocumentStored    handler FromDocumentStorageServiceHandler is {
      on documentStored: event DocumentStorageService.DocumentStored is {
        do "the document is safe; the permit already records what it was for"
      }
      on other is {
        error "Unexpected message for adaptor FromDocumentStorageService"
      }
    } with {
      briefly "from document storage service"
      described as {
        |Carries messages from the external DocumentStorageService.
      }
    }
  } with {
    briefly "DocumentStorageService integration"
    described as {
      |The model's side of the DocumentStorageService conversation.
    }
  }
  adaptor ToDocumentStorageService to context DocumentStorageService as router is { 
    outlet ToDocumentStorageServiceToPermitContext is command Permit.AttachRetrievedDocument
    inlet ToDocumentStorageServiceIn is type PermitEvent    inlet ToDocumentStorageServiceReplies is result DocumentStorageService.DocumentResult    outlet ToDocumentStorageServiceOut is type DocumentStorageService.DocumentStorageServiceRequest
    handler ToDocumentStorageServiceHandler is {
      on documentUploaded: event DocumentUploaded is {
        let artifact: type DocumentStorageService.StoreDocument = prompt("translate the documentUploaded event into a StoreDocument putting the applicant's drawings into the permit file")
        send artifact to outlet PermitContext.ToDocumentStorageService.ToDocumentStorageServiceOut
      }
      on reviewerAssigned: event Permit.ReviewerAssigned is {
        let askAnswer: type DocumentStorageService.DocumentResult = ask query DocumentStorageService.RetrieveDocument of context DocumentStorageService
        do "a reviewer needing the documents fetched back before they can read them"
        send command Permit.AttachRetrievedDocument(permitId = reviewerAssigned.permitId, documentId = prompt("the document id of this attach retrieved document"), fileName = prompt("the file name of this attach retrieved document"), downloadUrl = prompt("the download url of this attach retrieved document")) to outlet PermitContext.ToDocumentStorageService.ToDocumentStorageServiceToPermitContext
      }
      on other is {
        error "Unexpected message for adaptor ToDocumentStorageService"
      }
    } with {
      briefly "to document storage service"
      described as {
        |Carries messages to the external DocumentStorageService.
      }
    }
  } with {
    briefly "DocumentStorageService integration"
    described as {
      |The model's side of the DocumentStorageService conversation.
    }
  }
  adaptor ToFeeCalculationService to context FeeCalculationService as router is { 
    outlet ToFeeCalculationServiceToPermitContext is command Permit.CalculateFees
    inlet ToFeeCalculationServiceIn is type PermitEvent    inlet ToFeeCalculationServiceReplies is result FeeCalculationService.PermitFeesResult    outlet ToFeeCalculationServiceOut is query FeeCalculationService.CalculatePermitFees
    handler ToFeeCalculationServiceHandler is {
      on applicationSubmitted: event Permit.ApplicationSubmitted is {
        let askAnswer: type FeeCalculationService.PermitFeesResult = ask query FeeCalculationService.CalculatePermitFees of context FeeCalculationService
        do "an application needing its fees worked out from the schedule"
        send command Permit.CalculateFees(permitId = applicationSubmitted.permitId, fees = prompt("the fee schedule askAnswer.feeLineItems gives, totalling askAnswer.totalAmount")) to outlet PermitContext.ToFeeCalculationService.ToFeeCalculationServiceToPermitContext
      }
      on other is {
        error "Unexpected message for adaptor ToFeeCalculationService"
      }
    } with {
      briefly "to fee calculation service"
      described as {
        |Carries messages to the external FeeCalculationService.
      }
    }
  } with {
    briefly "FeeCalculationService integration"
    described as {
      |The model's side of the FeeCalculationService conversation.
    }
  }
  adaptor ToGISService to context GISService as router is { 
    outlet ToGISServiceToPermitContext is type ToGISServiceCommand
    inlet ToGISServiceIn is type PermitEvent    inlet ToGISServiceReplies is type GISService.GISServiceReply    outlet ToGISServiceOut is type GISService.GISServiceRequest
    handler ToGISServiceHandler is {
      on applicationSubmitted: event Permit.ApplicationSubmitted is {
        let askAnswer: type GISService.ParcelInfoResult = ask query GISService.GetParcelInfo of context GISService
        do "an application needing the parcel it names to be real"
        send command Permit.RecordParcelInfo(permitId = applicationSubmitted.permitId, parcelId = prompt("the parcel id of this record parcel info"), zoningCode = prompt("the zoning code of this record parcel info"), historicDistrict = prompt("the historic district of this record parcel info")) to outlet PermitContext.ToGISService.ToGISServiceToPermitContext
      }
      on reviewerAssigned: event Permit.ReviewerAssigned is {
        let askAnswer: type GISService.ZoningRequirementsResult = ask query GISService.GetZoningRequirements of context GISService
        do "a reviewer needing the zoning rules that apply to the parcel"
        send command Permit.RecordZoningRequirements(permitId = reviewerAssigned.permitId, zoningCode = prompt("the zoning code of this record zoning requirements"), maxHeight = prompt("the max height of this record zoning requirements"), maxLotCoverage = prompt("the max lot coverage of this record zoning requirements")) to outlet PermitContext.ToGISService.ToGISServiceToPermitContext
      }
      on other is {
        error "Unexpected message for adaptor ToGISService"
      }
    } with {
      briefly "to gis service"
      described as {
        |Carries messages to the external GISService.
      }
    }
  } with {
    briefly "GISService integration"
    described as {
      |The model's side of the GISService conversation.
    }
  }
  adaptor FromInspectionSchedulingService from context InspectionSchedulingService as flow is { 
    inlet FromInspectionSchedulingServiceIn is event InspectionSchedulingService.InspectionBooked    outlet FromInspectionSchedulingServiceToPermitContext is command Permit.ScheduleInspection
    handler FromInspectionSchedulingServiceHandler is {
      on inspectionBooked: event InspectionSchedulingService.InspectionBooked is {
        let item0: type Permit.ScheduleInspection = prompt("translate the inspectionBooked event into a ScheduleInspection, an inspection actually booked is what the permit's schedule records")
        send item0 to outlet PermitContext.FromInspectionSchedulingService.FromInspectionSchedulingServiceToPermitContext
      }
      on other is {
        error "Unexpected message for adaptor FromInspectionSchedulingService"
      }
    } with {
      briefly "from inspection scheduling service"
      described as {
        |Carries messages from the external InspectionSchedulingService.
      }
    }
  } with {
    briefly "InspectionSchedulingService integration"
    described as {
      |The model's side of the InspectionSchedulingService conversation.
    }
  }
  adaptor ToInspectionSchedulingService to context InspectionSchedulingService as router is { 
    outlet ToInspectionSchedulingServiceToPermitContext is command Permit.ScheduleInspection
    inlet ToInspectionSchedulingServiceIn is type PermitEvent    inlet ToInspectionSchedulingServiceReplies is result InspectionSchedulingService.AvailableSlotsResult    outlet ToInspectionSchedulingServiceOut is type InspectionSchedulingService.InspectionSchedulingServiceRequest
    handler ToInspectionSchedulingServiceHandler is {
      on permitIssued: event Permit.PermitIssued is {
        let request: type InspectionSchedulingService.BookInspection = prompt("translate the permitIssued event into a BookInspection, an issued permit being what the inspection regime attaches to")
        send request to outlet PermitContext.ToInspectionSchedulingService.ToInspectionSchedulingServiceOut
      }
      on inspectorAssigned: event Permit.InspectorAssigned is {
        let askAnswer: type InspectionSchedulingService.AvailableSlotsResult = ask query InspectionSchedulingService.GetAvailableSlots of context InspectionSchedulingService
        do "an assigned inspector needing a slot to be booked into"
        send command Permit.ScheduleInspection(permitId = inspectorAssigned.permitId, inspectionType = prompt("the inspection the inspector was assigned for"), requestedDate = prompt("the earliest of askAnswer.slots")) to outlet PermitContext.ToInspectionSchedulingService.ToInspectionSchedulingServiceToPermitContext
      }
      on other is {
        error "Unexpected message for adaptor ToInspectionSchedulingService"
      }
    } with {
      briefly "to inspection scheduling service"
      described as {
        |Carries messages to the external InspectionSchedulingService.
      }
    }
  } with {
    briefly "InspectionSchedulingService integration"
    described as {
      |The model's side of the InspectionSchedulingService conversation.
    }
  }
  adaptor ToNotificationService to context NotificationService as flow is { 
    inlet ToNotificationServiceIn is type PermitEvent    outlet ToNotificationServiceOut is command NotificationService.SendNotification
    handler ToNotificationServiceHandler is {
      on permitApproved: event PermitApproved is {
        let notice: type NotificationService.SendNotification = prompt("translate the permitApproved event into a SendNotification telling the applicant the permit is issued and what it authorises")
        send notice to outlet PermitContext.ToNotificationService.ToNotificationServiceOut
      }
      on correctionsRequested: event CorrectionsRequested is {
        let notice: type NotificationService.SendNotification = prompt("translate the correctionsRequested event into a SendNotification telling the applicant what must be fixed before review can continue, since nothing proceeds until they act")
        send notice to outlet PermitContext.ToNotificationService.ToNotificationServiceOut
      }
      on inspectionScheduled: event InspectionScheduled is {
        let notice: type NotificationService.SendNotification = prompt("translate the inspectionScheduled event into a SendNotification telling the applicant when the inspector is coming, because they must be present")
        send notice to outlet PermitContext.ToNotificationService.ToNotificationServiceOut
      }
      on permitExtended: event PermitExtended is {
        let notice: type NotificationService.SendNotification = prompt("translate the permitExtended event into a SendNotification telling the applicant the permit now runs to a later date")
        send notice to outlet PermitContext.ToNotificationService.ToNotificationServiceOut
      }
      on permitClosed: event PermitClosed is {
        let notice: type NotificationService.SendNotification = prompt("translate the permitClosed event into a SendNotification telling the applicant the permit is closed and no further work is authorised under it")
        send notice to outlet PermitContext.ToNotificationService.ToNotificationServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToNotificationService"
      }
    } with {
      briefly "to notification service"
      described as {
        |Carries messages to the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor FromPaymentProcessingService from context PaymentProcessingService as sink is { 
    inlet FromPaymentProcessingServiceIn is type PaymentProcessingService.PaymentProcessingServiceEvent    handler FromPaymentProcessingServiceHandler is {
      on paymentCompleted: event PaymentProcessingService.PaymentCompleted is {
        do "the fee has cleared; the permit recorded the payment when it was taken"
      }
      on paymentFailed: event PaymentProcessingService.PaymentFailed is {
        do "a failed fee leaves the application unpaid; the applicant is asked again"
      }
      on other is {
        error "Unexpected message for adaptor FromPaymentProcessingService"
      }
    } with {
      briefly "from payment processing service"
      described as {
        |Carries messages from the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  adaptor ToPaymentProcessingService to context PaymentProcessingService as flow is { 
    inlet ToPaymentProcessingServiceIn is type PermitEvent    outlet ToPaymentProcessingServiceOut is command PaymentProcessingService.ProcessPermitPayment
    handler ToPaymentProcessingServiceHandler is {
      on feesCalculated: event FeesCalculated is {
        let notice: type PaymentProcessingService.ProcessPermitPayment = prompt("translate the feesCalculated event into a ProcessPermitPayment collecting the fee the applicant now owes for this permit")
        send notice to outlet PermitContext.ToPaymentProcessingService.ToPaymentProcessingServiceOut
      }
      on other is {
        error "Unexpected message for adaptor ToPaymentProcessingService"
      }
    } with {
      briefly "to payment processing service"
      described as {
        |Carries messages to the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  include "types.riddl"
  include "Permit.riddl"
  // Repository
  type PermitRepositoryCommand is one of {
    PermitContext.PermitRepository.PersistApplicationSubmitted or PermitContext.PermitRepository.PersistCorrectionsRequested or PermitContext.PermitRepository.PersistCorrectionsSubmitted or PermitContext.PermitRepository.PersistDocumentUploaded or PermitContext.PermitRepository.PersistFeesCalculated or PermitContext.PermitRepository.PersistInspectionRecorded or PermitContext.PermitRepository.PersistInspectionScheduled or PermitContext.PermitRepository.PersistInspectorAssigned or PermitContext.PermitRepository.PersistPaymentProcessed or PermitContext.PermitRepository.PersistPermitApproved or PermitContext.PermitRepository.PersistPermitClosed or PermitContext.PermitRepository.PersistPermitExtended or PermitContext.PermitRepository.PersistPermitIssued or PermitContext.PermitRepository.PersistPermitRevoked or PermitContext.PermitRepository.PersistPermitVoided or PermitContext.PermitRepository.PersistReviewSubmitted or PermitContext.PermitRepository.PersistReviewerAssigned
  } with {
    briefly "Anything that writes PermitRepository"
    described as {
      |The persistence commands PermitRepository accepts. Entity events never
      |reach it directly; a projector turns each into one of these.
    }
  }
  repository PermitRepository as flow is {
    inlet PermitRepositoryFromPermitAnalytics is type PermitRepositoryCommand    command PersistCorrectionsRequested is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row CorrectionsRequested applies to.
        }
      }
    } with {
      briefly "Persist CorrectionsRequested"
      described as {
        |Instructs this repository to apply CorrectionsRequested to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistCorrectionsSubmitted is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row CorrectionsSubmitted applies to.
        }
      }
    } with {
      briefly "Persist CorrectionsSubmitted"
      described as {
        |Instructs this repository to apply CorrectionsSubmitted to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistDocumentUploaded is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row DocumentUploaded applies to.
        }
      }
    } with {
      briefly "Persist DocumentUploaded"
      described as {
        |Instructs this repository to apply DocumentUploaded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistFeesCalculated is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row FeesCalculated applies to.
        }
      }
    } with {
      briefly "Persist FeesCalculated"
      described as {
        |Instructs this repository to apply FeesCalculated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistInspectionScheduled is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row InspectionScheduled applies to.
        }
      }
    } with {
      briefly "Persist InspectionScheduled"
      described as {
        |Instructs this repository to apply InspectionScheduled to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistInspectorAssigned is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row InspectorAssigned applies to.
        }
      }
    } with {
      briefly "Persist InspectorAssigned"
      described as {
        |Instructs this repository to apply InspectorAssigned to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitExtended is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitExtended applies to.
        }
      }
    } with {
      briefly "Persist PermitExtended"
      described as {
        |Instructs this repository to apply PermitExtended to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitRevoked is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitRevoked applies to.
        }
      }
    } with {
      briefly "Persist PermitRevoked"
      described as {
        |Instructs this repository to apply PermitRevoked to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitVoided is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitVoided applies to.
        }
      }
    } with {
      briefly "Persist PermitVoided"
      described as {
        |Instructs this repository to apply PermitVoided to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistReviewSubmitted is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row ReviewSubmitted applies to.
        }
      }
    } with {
      briefly "Persist ReviewSubmitted"
      described as {
        |Instructs this repository to apply ReviewSubmitted to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistReviewerAssigned is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row ReviewerAssigned applies to.
        }
      }
    } with {
      briefly "Persist ReviewerAssigned"
      described as {
        |Instructs this repository to apply ReviewerAssigned to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistApplicationSubmitted is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row ApplicationSubmitted applies to.
        }
      }
    } with {
      briefly "Persist ApplicationSubmitted"
      described as {
        |Instructs this repository to apply ApplicationSubmitted to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistInspectionRecorded is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row InspectionRecorded applies to.
        }
      }
    } with {
      briefly "Persist InspectionRecorded"
      described as {
        |Instructs this repository to apply InspectionRecorded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPaymentProcessed is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PaymentProcessed applies to.
        }
      }
    } with {
      briefly "Persist PaymentProcessed"
      described as {
        |Instructs this repository to apply PaymentProcessed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitApproved is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitApproved applies to.
        }
      }
    } with {
      briefly "Persist PermitApproved"
      described as {
        |Instructs this repository to apply PermitApproved to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitClosed is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitClosed applies to.
        }
      }
    } with {
      briefly "Persist PermitClosed"
      described as {
        |Instructs this repository to apply PermitClosed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPermitIssued is {
      permitId: PermitId with {
        briefly "Row addressed"
        described as {
          |Which stored row PermitIssued applies to.
        }
      }
    } with {
      briefly "Persist PermitIssued"
      described as {
        |Instructs this repository to apply PermitIssued to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    record StoredPermit is {
      permitId: PermitId with {
        briefly "permitId"
        described as {
          |The permitId column of the stored permit row.
        }
      }
    } with {
      briefly "Stored permit row"
      described as {
        |The persistence shape of permit data as this repository stores it.
      }
    }
    schema PermitData is relational
      of permits as record StoredPermit
        index on field StoredPermit.permitId
 with {
      briefly "Permit data schema"
      described as {
        |The relational schema persisting permit records for this repository.
      }
    }
    outlet PermitRepositoryResponses is result Permit.PermitQueryResult
    query FindPermitById replies result PermitResult is {
      permitId: PermitId with {
        briefly "Permit"
        described as {
          |Permit identifier.
        }
      }
    } with {
      briefly "Find permit by ID"
      described as {
        |Retrieves permit by identifier.
      }
    }
    result PermitResult is {
      permit: Permit.ActiveStateData with {
        briefly "Permit"
        described as {
          |Permit data.
        }
      }
    } with {
      briefly "Permit result"
      described as {
        |Returns permit data.
      }
    }
    query SearchPermits replies result PermitsResult is {
      filterPermitType: PermitType? with {
        briefly "Type"
        described as {
          |Filter by type.
        }
      }
      filterStatus: PermitStatus? with {
        briefly "Status"
        described as {
          |Filter by status.
        }
      }
      filterParcelId: ParcelId? with {
        briefly "Parcel"
        described as {
          |Filter by parcel.
        }
      }
    } with {
      briefly "Search permits"
      described as {
        |Searches permits by criteria.
      }
    }
    result PermitsResult is {
      permits: Permit.ActiveStateData+ with {
        briefly "Permits"
        described as {
          |Matching permits.
        }
      }
      totalCount: Natural with {
        briefly "Count"
        described as {
          |Total matches.
        }
      }
    } with {
      briefly "Permits result"
      described as {
        |Returns matching permits.
      }
    }
    query FindByAddress replies result PermitsResult is {
      street: String(1,200) with {
        briefly "Street"
        described as {
          |Street address.
        }
      }
      filterCity: String(1,100)? with {
        briefly "City"
        described as {
          |City.
        }
      }
    } with {
      briefly "Find by address"
      described as {
        |Finds permits at address.
      }
    }
    query FindByApplicant replies result PermitsResult is {
      applicantName: String(1,200) with {
        briefly "Name"
        described as {
          |Applicant name.
        }
      }
    } with {
      briefly "Find by applicant"
      described as {
        |Finds permits for applicant.
      }
    }
    query FindPendingInspections replies result PermitsResult is {
      filterInspectorId: InspectorId? with {
        briefly "Inspector"
        described as {
          |Filter by inspector.
        }
      }
      filterDate: Date? with {
        briefly "Date"
        described as {
          |Filter by date.
        }
      }
    } with {
      briefly "Find pending inspections"
      described as {
        |Finds scheduled inspections.
      }
    }
    query FindExpiringPermits replies result PermitResult is {
      daysUntilExpiration: Natural with {
        briefly "Days"
        described as {
          |Days until expiration.
        }
      }
    } with {
      briefly "Find expiring permits"
      described as {
        |Finds permits expiring soon.
      }
    }
    handler PermitRepositoryHandler is {
      on persistCorrectionsRequested: command PersistCorrectionsRequested is {
        do "apply CorrectionsRequested to the stored row"
      }
      on persistCorrectionsSubmitted: command PersistCorrectionsSubmitted is {
        do "apply CorrectionsSubmitted to the stored row"
      }
      on persistDocumentUploaded: command PersistDocumentUploaded is {
        do "apply DocumentUploaded to the stored row"
      }
      on persistFeesCalculated: command PersistFeesCalculated is {
        do "apply FeesCalculated to the stored row"
      }
      on persistInspectionScheduled: command PersistInspectionScheduled is {
        do "apply InspectionScheduled to the stored row"
      }
      on persistInspectorAssigned: command PersistInspectorAssigned is {
        do "apply InspectorAssigned to the stored row"
      }
      on persistPermitExtended: command PersistPermitExtended is {
        do "apply PermitExtended to the stored row"
      }
      on persistPermitRevoked: command PersistPermitRevoked is {
        do "apply PermitRevoked to the stored row"
      }
      on persistPermitVoided: command PersistPermitVoided is {
        do "apply PermitVoided to the stored row"
      }
      on persistReviewSubmitted: command PersistReviewSubmitted is {
        do "apply ReviewSubmitted to the stored row"
      }
      on persistReviewerAssigned: command PersistReviewerAssigned is {
        do "apply ReviewerAssigned to the stored row"
      }
      on persistApplicationSubmitted: command PersistApplicationSubmitted is {
        do "apply ApplicationSubmitted to the stored row"
      }
      on persistInspectionRecorded: command PersistInspectionRecorded is {
        do "apply InspectionRecorded to the stored row"
      }
      on persistPaymentProcessed: command PersistPaymentProcessed is {
        do "apply PaymentProcessed to the stored row"
      }
      on persistPermitApproved: command PersistPermitApproved is {
        do "apply PermitApproved to the stored row"
      }
      on persistPermitClosed: command PersistPermitClosed is {
        do "apply PermitClosed to the stored row"
      }
      on persistPermitIssued: command PersistPermitIssued is {
        do "apply PermitIssued to the stored row"
      }
      on query GetAnalyticsData is {
        let analyticsDataView: type AnalyticsDataView = prompt("read the analytics data projection")
        reply analyticsDataView
      }
      on query FindPermitById is {
        let permitResult: type PermitResult = prompt("read the permit from the store")
        reply permitResult
      }
      on query SearchPermits is {
        let permitsResult: type PermitsResult = prompt("read the permits from the store")
        reply permitsResult
      }
      on query FindByAddress is {
        let permitsResult: type PermitsResult = prompt("read the permits that by address asks for")
        reply permitsResult
      }
      on query FindByApplicant is {
        let permitsResult: type PermitsResult = prompt("read the permits that by applicant asks for")
        reply permitsResult
      }
      on query FindPendingInspections is {
        let permitsResult: type PermitsResult = prompt("read the permits that pending inspections asks for")
        reply permitsResult
      }
      on query FindExpiringPermits is {
        let permitResult: type PermitResult = prompt("read the permit from the store")
        reply permitResult
      }
    } with {
      briefly "Handles repository queries"
      described as {
        |Processes permit queries.
      }
    }
  } with {
    briefly "Permit repository"
    described as {
      |Persistence for permit records.
    }
  }
  // Projector for permit analytics
  query GetAnalyticsData replies result AnalyticsDataView is {
    analyticsDataKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which analytics data to retrieve.
      }
    }
  } with {
    briefly "Get analytics data"
    described as {
      |Retrieves the analytics data projection.
    }
  }
  result AnalyticsDataView is {
    analyticsData: PermitAnalytics.AnalyticsData with {
      briefly "Analytics data"
      described as {
        |The analytics data being returned.
      }
    }
  } with {
    briefly "Analytics data view"
    described as {
      |Read model view carrying the analytics data projection.
    }
  }
  projector PermitAnalytics as flow is {
    updates repository PermitContext.PermitRepository
    outlet PermitAnalyticsToRepository is type PermitRepositoryCommand
    inlet PermitAnalyticsFromPermit is type PermitEvent    record AnalyticsData is {
      totalApplications: Natural with {
        briefly "Applications"
        described as {
          |Total applications.
        }
      }
      applicationsByType: TypeCount+ with {
        briefly "By type"
        described as {
          |Applications by type.
        }
      }
      averageReviewDays: Decimal(5,2) with {
        briefly "Avg review"
        described as {
          |Average review time.
        }
      }
      pendingReviews: Natural with {
        briefly "Pending"
        described as {
          |Pending reviews.
        }
      }
      inspectionPassRate: Decimal(5,2) with {
        briefly "Pass rate"
        described as {
          |Inspection pass rate.
        }
      }
      totalFeesCollected: Decimal(12,2) with {
        briefly "Fees"
        described as {
          |Total fees collected.
        }
      }
      permitsByStatus: StatusCount+ with {
        briefly "By status"
        described as {
          |Permits by status.
        }
      }
    } with {
      briefly "Analytics data"
      described as {
        |Permit analytics metrics.
      }
    }
    record TypeCount is {
      permitType: PermitType with {
        briefly "Type"
        described as {
          |Permit type.
        }
      }
      count: Natural with {
        briefly "Count"
        described as {
          |Application count.
        }
      }
    } with {
      briefly "Type count"
      described as {
        |Count by permit type.
      }
    }
    record StatusCount is {
      status: PermitStatus with {
        briefly "Status"
        described as {
          |Permit status.
        }
      }
      count: Natural with {
        briefly "Count"
        described as {
          |Permit count.
        }
      }
    } with {
      briefly "Status count"
      described as {
        |Count by status.
      }
    }
    handler AnalyticsHandler is {
      on correctionsRequested: event Permit.CorrectionsRequested is {
        tell command PermitContext.PermitRepository.PersistCorrectionsRequested(permitId = correctionsRequested.permitId) to repository PermitContext.PermitRepository
        do "apply CorrectionsRequested to the PermitAnalytics projection"
      }
      on correctionsSubmitted: event Permit.CorrectionsSubmitted is {
        tell command PermitContext.PermitRepository.PersistCorrectionsSubmitted(permitId = correctionsSubmitted.permitId) to repository PermitContext.PermitRepository
        do "apply CorrectionsSubmitted to the PermitAnalytics projection"
      }
      on documentUploaded: event Permit.DocumentUploaded is {
        tell command PermitContext.PermitRepository.PersistDocumentUploaded(permitId = documentUploaded.permitId) to repository PermitContext.PermitRepository
        do "apply DocumentUploaded to the PermitAnalytics projection"
      }
      on feesCalculated: event Permit.FeesCalculated is {
        tell command PermitContext.PermitRepository.PersistFeesCalculated(permitId = feesCalculated.permitId) to repository PermitContext.PermitRepository
        do "apply FeesCalculated to the PermitAnalytics projection"
      }
      on inspectionScheduled: event Permit.InspectionScheduled is {
        tell command PermitContext.PermitRepository.PersistInspectionScheduled(permitId = inspectionScheduled.permitId) to repository PermitContext.PermitRepository
        do "apply InspectionScheduled to the PermitAnalytics projection"
      }
      on inspectorAssigned: event Permit.InspectorAssigned is {
        tell command PermitContext.PermitRepository.PersistInspectorAssigned(permitId = inspectorAssigned.permitId) to repository PermitContext.PermitRepository
        do "apply InspectorAssigned to the PermitAnalytics projection"
      }
      on permitExtended: event Permit.PermitExtended is {
        tell command PermitContext.PermitRepository.PersistPermitExtended(permitId = permitExtended.permitId) to repository PermitContext.PermitRepository
        do "apply PermitExtended to the PermitAnalytics projection"
      }
      on permitRevoked: event Permit.PermitRevoked is {
        tell command PermitContext.PermitRepository.PersistPermitRevoked(permitId = permitRevoked.permitId) to repository PermitContext.PermitRepository
        do "apply PermitRevoked to the PermitAnalytics projection"
      }
      on permitVoided: event Permit.PermitVoided is {
        tell command PermitContext.PermitRepository.PersistPermitVoided(permitId = permitVoided.permitId) to repository PermitContext.PermitRepository
        do "apply PermitVoided to the PermitAnalytics projection"
      }
      on reviewSubmitted: event Permit.ReviewSubmitted is {
        tell command PermitContext.PermitRepository.PersistReviewSubmitted(permitId = reviewSubmitted.permitId) to repository PermitContext.PermitRepository
        do "apply ReviewSubmitted to the PermitAnalytics projection"
      }
      on reviewerAssigned: event Permit.ReviewerAssigned is {
        tell command PermitContext.PermitRepository.PersistReviewerAssigned(permitId = reviewerAssigned.permitId) to repository PermitContext.PermitRepository
        do "apply ReviewerAssigned to the PermitAnalytics projection"
      }
      on applicationSubmitted: event Permit.ApplicationSubmitted is {
        tell command PermitContext.PermitRepository.PersistApplicationSubmitted(permitId = applicationSubmitted.permitId) to repository PermitContext.PermitRepository
      }
      on permitApproved: event Permit.PermitApproved is {
        tell command PermitContext.PermitRepository.PersistPermitApproved(permitId = permitApproved.permitId) to repository PermitContext.PermitRepository
      }
      on permitIssued: event Permit.PermitIssued is {
        tell command PermitContext.PermitRepository.PersistPermitIssued(permitId = permitIssued.permitId) to repository PermitContext.PermitRepository
      }
      on inspectionRecorded: event Permit.InspectionRecorded is {
        tell command PermitContext.PermitRepository.PersistInspectionRecorded(permitId = inspectionRecorded.permitId) to repository PermitContext.PermitRepository
      }
      on paymentProcessed: event Permit.PaymentProcessed is {
        tell command PermitContext.PermitRepository.PersistPaymentProcessed(permitId = paymentProcessed.permitId) to repository PermitContext.PermitRepository
      }
      on permitClosed: event Permit.PermitClosed is {
        tell command PermitContext.PermitRepository.PersistPermitClosed(permitId = permitClosed.permitId) to repository PermitContext.PermitRepository
      }
      on parcelInfoRecorded: event Permit.ParcelInfoRecorded is {
        do "a parcel info recorded changes nothing this projector keeps"
      }
      on retrievedDocumentAttached: event Permit.RetrievedDocumentAttached is {
        do "a retrieved document attached changes nothing this projector keeps"
      }
      on zoningRequirementsRecorded: event Permit.ZoningRequirementsRecorded is {
        do "a zoning requirements recorded changes nothing this projector keeps"
      }
    } with {
      briefly "Processes analytics events"
      described as {
        |Updates permit analytics.
      }
    }
  } with {
    briefly "Permit analytics"
    described as {
      |Projects permit metrics for reporting.
    }
  }
  // ---- Event publication for Permit ----
  type PermitEvent is one of {
    PermitContext.Permit.ApplicationSubmitted or PermitContext.Permit.DocumentUploaded or PermitContext.Permit.ReviewerAssigned or PermitContext.Permit.ReviewSubmitted or PermitContext.Permit.CorrectionsRequested or PermitContext.Permit.CorrectionsSubmitted or PermitContext.Permit.FeesCalculated or PermitContext.Permit.PaymentProcessed or PermitContext.Permit.PermitApproved or PermitContext.Permit.PermitIssued or PermitContext.Permit.InspectionScheduled or PermitContext.Permit.InspectorAssigned or PermitContext.Permit.InspectionRecorded or PermitContext.Permit.PermitExtended or PermitContext.Permit.PermitClosed or PermitContext.Permit.PermitVoided or PermitContext.Permit.PermitRevoked or PermitContext.Permit.ZoningRequirementsRecorded or PermitContext.Permit.ParcelInfoRecorded or PermitContext.Permit.RetrievedDocumentAttached
  } with {
    briefly "Permit event alternation"
    described as {
      |Union of all Permit events, for streaming and event-sourced
      |persistence.
    }
  }
  streamlet PermitEventSplit as split is {
    outlet PermitEventSplitToToPaymentProcessingService is type PermitEvent
    outlet PermitEventSplitToToNotificationService is type PermitEvent
    outlet PermitEventSplitToToInspectionSchedulingService is type PermitEvent
    outlet PermitEventSplitToToGISService is type PermitEvent
    outlet PermitEventSplitToToFeeCalculationService is type PermitEvent
    outlet PermitEventSplitToToDocumentStorageService is type PermitEvent
    outlet PermitEventSplitToToContractorLicensingService is type PermitEvent
    inlet PermitEventSplitIn is type PermitEvent    outlet PermitEventSplitToPermitAnalytics is type PermitEvent
    handler PermitEventSplitHandler is {
      on applicationSubmitted: event Permit.ApplicationSubmitted is {
        send applicationSubmitted to outlet PermitEventSplitToPermitAnalytics
        send applicationSubmitted to outlet PermitEventSplitToToGISService
        send applicationSubmitted to outlet PermitEventSplitToToFeeCalculationService
        send applicationSubmitted to outlet PermitEventSplitToToContractorLicensingService
        tell applicationSubmitted to entity PermitContext.Permit
      }
      on documentUploaded: event Permit.DocumentUploaded is {
        send documentUploaded to outlet PermitEventSplitToPermitAnalytics
        send documentUploaded to outlet PermitEventSplitToToDocumentStorageService
        tell documentUploaded to entity PermitContext.Permit
      }
      on reviewerAssigned: event Permit.ReviewerAssigned is {
        send reviewerAssigned to outlet PermitEventSplitToPermitAnalytics
        send reviewerAssigned to outlet PermitEventSplitToToGISService
        send reviewerAssigned to outlet PermitEventSplitToToDocumentStorageService
        tell reviewerAssigned to entity PermitContext.Permit
      }
      on reviewSubmitted: event Permit.ReviewSubmitted is {
        send reviewSubmitted to outlet PermitEventSplitToPermitAnalytics
        tell reviewSubmitted to entity PermitContext.Permit
      }
      on correctionsRequested: event Permit.CorrectionsRequested is {
        send correctionsRequested to outlet PermitEventSplitToPermitAnalytics
        send correctionsRequested to outlet PermitEventSplitToToNotificationService
        tell correctionsRequested to entity PermitContext.Permit
      }
      on correctionsSubmitted: event Permit.CorrectionsSubmitted is {
        send correctionsSubmitted to outlet PermitEventSplitToPermitAnalytics
        tell correctionsSubmitted to entity PermitContext.Permit
      }
      on feesCalculated: event Permit.FeesCalculated is {
        send feesCalculated to outlet PermitEventSplitToPermitAnalytics
        send feesCalculated to outlet PermitEventSplitToToPaymentProcessingService
        tell feesCalculated to entity PermitContext.Permit
      }
      on paymentProcessed: event Permit.PaymentProcessed is {
        send paymentProcessed to outlet PermitEventSplitToPermitAnalytics
        tell paymentProcessed to entity PermitContext.Permit
      }
      on permitApproved: event Permit.PermitApproved is {
        send permitApproved to outlet PermitEventSplitToPermitAnalytics
        send permitApproved to outlet PermitEventSplitToToNotificationService
        tell permitApproved to entity PermitContext.Permit
      }
      on permitIssued: event Permit.PermitIssued is {
        send permitIssued to outlet PermitEventSplitToPermitAnalytics
        send permitIssued to outlet PermitEventSplitToToInspectionSchedulingService
        tell permitIssued to entity PermitContext.Permit
      }
      on inspectionScheduled: event Permit.InspectionScheduled is {
        send inspectionScheduled to outlet PermitEventSplitToPermitAnalytics
        send inspectionScheduled to outlet PermitEventSplitToToNotificationService
        tell inspectionScheduled to entity PermitContext.Permit
      }
      on inspectorAssigned: event Permit.InspectorAssigned is {
        send inspectorAssigned to outlet PermitEventSplitToPermitAnalytics
        send inspectorAssigned to outlet PermitEventSplitToToInspectionSchedulingService
        tell inspectorAssigned to entity PermitContext.Permit
      }
      on inspectionRecorded: event Permit.InspectionRecorded is {
        send inspectionRecorded to outlet PermitEventSplitToPermitAnalytics
        tell inspectionRecorded to entity PermitContext.Permit
      }
      on permitExtended: event Permit.PermitExtended is {
        send permitExtended to outlet PermitEventSplitToPermitAnalytics
        send permitExtended to outlet PermitEventSplitToToNotificationService
        tell permitExtended to entity PermitContext.Permit
      }
      on permitClosed: event Permit.PermitClosed is {
        send permitClosed to outlet PermitEventSplitToPermitAnalytics
        send permitClosed to outlet PermitEventSplitToToNotificationService
        tell permitClosed to entity PermitContext.Permit
      }
      on permitVoided: event Permit.PermitVoided is {
        send permitVoided to outlet PermitEventSplitToPermitAnalytics
        tell permitVoided to entity PermitContext.Permit
      }
      on permitRevoked: event Permit.PermitRevoked is {
        send permitRevoked to outlet PermitEventSplitToPermitAnalytics
        tell permitRevoked to entity PermitContext.Permit
      }
      on zoningRequirementsRecorded: event Permit.ZoningRequirementsRecorded is {
        send zoningRequirementsRecorded to outlet PermitEventSplitToPermitAnalytics
        tell zoningRequirementsRecorded to entity PermitContext.Permit
      }
      on parcelInfoRecorded: event Permit.ParcelInfoRecorded is {
        send parcelInfoRecorded to outlet PermitEventSplitToPermitAnalytics
        tell parcelInfoRecorded to entity PermitContext.Permit
      }
      on retrievedDocumentAttached: event Permit.RetrievedDocumentAttached is {
        send retrievedDocumentAttached to outlet PermitEventSplitToPermitAnalytics
        tell retrievedDocumentAttached to entity PermitContext.Permit
      }
      on other is {
        error "Unexpected message for processor PermitEventSplit"
      }
    } with {
      briefly "Permit event fan-out handler"
      described as {
        |Delivers each Permit event to all of its consumers.
      }
    }
  } with {
    briefly "Permit event fan-out"
    described as {
      |Fans Permit events out to the repository and projectors.
    }
  }
  connector 'ToPaymentProcessingService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToPaymentProcessingService to inlet PermitContext.ToPaymentProcessingService.ToPaymentProcessingServiceIn with {
    briefly "The Permit events ToPaymentProcessingService translates, on their way to it"
  }
  connector 'ToNotificationService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToNotificationService to inlet PermitContext.ToNotificationService.ToNotificationServiceIn with {
    briefly "The Permit events ToNotificationService translates, on their way to it"
  }
  connector 'ToInspectionSchedulingService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToInspectionSchedulingService to inlet PermitContext.ToInspectionSchedulingService.ToInspectionSchedulingServiceIn with {
    briefly "The Permit events ToInspectionSchedulingService translates, on their way to it"
  }
  connector 'ToGISService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToGISService to inlet PermitContext.ToGISService.ToGISServiceIn with {
    briefly "The Permit events ToGISService translates, on their way to it"
  }
  connector 'ToFeeCalculationService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToFeeCalculationService to inlet PermitContext.ToFeeCalculationService.ToFeeCalculationServiceIn with {
    briefly "The Permit events ToFeeCalculationService translates, on their way to it"
  }
  connector 'ToDocumentStorageService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToDocumentStorageService to inlet PermitContext.ToDocumentStorageService.ToDocumentStorageServiceIn with {
    briefly "The Permit events ToDocumentStorageService translates, on their way to it"
  }
  connector 'ToContractorLicensingService Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToToContractorLicensingService to inlet PermitContext.ToContractorLicensingService.ToContractorLicensingServiceIn with {
    briefly "The Permit events ToContractorLicensingService translates, on their way to it"
  }
  connector 'PermitEvent Stream' is from outlet PermitContext.Permit.PermitEvents2 to inlet PermitContext.PermitEventSplit.PermitEventSplitIn with {
    briefly "Permit events, fanned out to storage and read models"
  }
  connector 'PermitAnalytics Feed' is from outlet PermitContext.PermitEventSplit.PermitEventSplitToPermitAnalytics to inlet PermitContext.PermitAnalytics.PermitAnalyticsFromPermit with {
    briefly "Permit events feeding the PermitAnalytics projection"
  }
  connector 'PermitAnalytics Storage' is from outlet PermitContext.PermitAnalytics.PermitAnalyticsToRepository to inlet PermitContext.PermitRepository.PermitRepositoryFromPermitAnalytics with {
    briefly "PermitAnalytics updates on their way to storage"
  }
} with {
  briefly "Permit context"
  described as {
    | The Permit context manages building and
    | land use permits.
  }
}
