// Reporting bounded context - CQRS projectors only
context Reporting as router is {
  inlet ShiftToReportingIn is type ShiftEvent
  outlet ShiftToReportingOut is type ShiftEvent
  inlet InventoryToReportingIn is type InventoryEvent
  outlet InventoryToReportingOut is type InventoryEvent
  handler ReportingBoundary is {
    on shiftCreated: event Shift.ShiftCreated is {
      send shiftCreated to outlet Reporting.ShiftToReportingOut
    }
    on employeeAssigned: event Shift.EmployeeAssigned is {
      send employeeAssigned to outlet Reporting.ShiftToReportingOut
    }
    on shiftSwapped: event Shift.ShiftSwapped is {
      send shiftSwapped to outlet Reporting.ShiftToReportingOut
    }
    on clockedIn: event Shift.ClockedIn is {
      send clockedIn to outlet Reporting.ShiftToReportingOut
    }
    on clockedOut: event Shift.ClockedOut is {
      send clockedOut to outlet Reporting.ShiftToReportingOut
    }
    on shiftCancelled: event Shift.ShiftCancelled is {
      send shiftCancelled to outlet Reporting.ShiftToReportingOut
    }
    on stockReceived: event InventoryItem.StockReceived is {
      send stockReceived to outlet Reporting.InventoryToReportingOut
    }
    on stockConsumed: event InventoryItem.StockConsumed is {
      send stockConsumed to outlet Reporting.InventoryToReportingOut
    }
    on stockAdjusted: event InventoryItem.StockAdjusted is {
      send stockAdjusted to outlet Reporting.InventoryToReportingOut
    }
    on reorderThresholdSet: event InventoryItem.ReorderThresholdSet is {
      send reorderThresholdSet to outlet Reporting.InventoryToReportingOut
    }
    on purchaseOrderCreated: event InventoryItem.PurchaseOrderCreated is {
      send purchaseOrderCreated to outlet Reporting.InventoryToReportingOut
    }
    on other is {
      error "Unexpected message at the Reporting boundary"
    }
  } with {
    briefly "Context boundary relay"
    described as {
      |The context is the port at its own boundary: peers connect here, never
      |to a definition inside. Inbound messages are relayed to the definition
      |that owns them; outbound ones are republished from here.
    }
  }
  connector 'ShiftToReporting Intake' is from outlet Reporting.ShiftToReportingOut to inlet Reporting.ShiftEventSink.IncomingShiftEvents
  connector 'InventoryToReporting Intake' is from outlet Reporting.InventoryToReportingOut to inlet Reporting.InventoryEventSink.IncomingInventoryEvents
  query GetInventoryReport is  {
    inventoryReportKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which InventoryReport to retrieve.
      }
    }
  } with {
    briefly "Get InventoryReport"
    described as {
      |Retrieves a InventoryReport projection.
    }
  }
  result InventoryReportView is  {
    inventoryReport: InventoryReportRecord with {
      briefly "InventoryReport"
      described as {
        |The InventoryReport data being returned.
      }
    }
  } with {
    briefly "InventoryReport view"
    described as {
      |Read model view carrying a InventoryReportRecord.
    }
  }
  query GetLaborReport is  {
    laborReportKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which LaborReport to retrieve.
      }
    }
  } with {
    briefly "Get LaborReport"
    described as {
      |Retrieves a LaborReport projection.
    }
  }
  result LaborReportView is  {
    laborReport: LaborReportRecord with {
      briefly "LaborReport"
      described as {
        |The LaborReport data being returned.
      }
    }
  } with {
    briefly "LaborReport view"
    described as {
      |Read model view carrying a LaborReportRecord.
    }
  }
  query GetSalesReport is  {
    salesReportKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which SalesReport to retrieve.
      }
    }
  } with {
    briefly "Get SalesReport"
    described as {
      |Retrieves a SalesReport projection.
    }
  }
  result SalesReportView is  {
    salesReport: SalesReportRecord with {
      briefly "SalesReport"
      described as {
        |The SalesReport data being returned.
      }
    }
  } with {
    briefly "SalesReport view"
    described as {
      |Read model view carrying a SalesReportRecord.
    }
  }
  type SalesReportRecord is {
    reportDate: Date with {
      briefly "Date"
      described as {
        |The trading day being reported. A restaurant's day is not a calendar
        |day — late service is booked to the day it opened.
      }
    }
    totalRevenue: Decimal(12,2) with {
      briefly "Revenue"
      described as {
        |Takings for the day across dine-in, online and delivery. The single
        |figure an owner asks for first.
      }
    }
  } with {
    briefly "Sales report record type"
    described as {
      |Aggregate type for sales report persistence.
    }
  }
  type LaborReportRecord is {
    laborReportDate: Date with {
      briefly "Date"
      described as {
        |The trading day whose labour is being reported, aligned with the
        |sales day so cost can be read as a percentage of takings.
      }
    }
    totalHoursWorked: Decimal(8,2) with {
      briefly "Hours"
      described as {
        |Hours actually clocked, not hours rostered. The gap between the two
        |is where labour cost quietly escapes the schedule.
      }
    }
  } with {
    briefly "Labor report record type"
    described as {
      |Aggregate type for labor report persistence.
    }
  }
  type InventoryReportRecord is {
    inventoryReportDate: Date with {
      briefly "Date"
      described as {
        |The trading day whose stock position is being reported.
      }
    }
    totalStockValue: Decimal(14,2) with {
      term StockTurn is  "How many times stock is sold and replaced over a period. A low turn means cash is tied up on the shelves; a high one means the kitchen is running close to out." 
      briefly "Stock value"
      described as {
        |What is sitting in the building, valued at cost. Read against sales
        |it gives the stock turn that tells an owner if cash is tied up on
        |the shelves.
      }
    }
  } with {
    briefly "Inventory report record type"
    described as {
      |Aggregate type for inventory report persistence.
    }
  }
  type SalesReportCommand is one of {
    RecordDineInPayment or RecordOnlinePayment or RecordAccountingPosting or RecordReservationEvent or RecordTableOrderEvent
  } with {
    briefly "Sales report command alternation"
    described as {
      |Commands accepted by the SalesReportRepository.
    }
  }
  type LaborReportCommand is one of {
    RecordClockIn or RecordClockOut or RecordShiftCancellation or RecordShiftEvent
  } with {
    briefly "Labor report command alternation"
    described as {
      |Commands accepted by the LaborReportRepository.
    }
  }
  type InventoryReportCommand is one of {
    RecordStockReceived or RecordStockConsumed or RecordStockAdjusted or RecordInventoryEvent
  } with {
    briefly "Inventory report command alternation"
    described as {
      |Commands accepted by the InventoryReportRepository.
    }
  }
  repository SalesReportRepository as sink is {
    inlet SalesReportRepositoryFromSalesReport is type SalesReportCommand    schema SalesReportData is relational
      of salesReports as type SalesReportRecord
        index on field SalesReportRecord.reportDate
 with {
      briefly "Sales report schema"
      described as {
        |Relational schema for persisting sales reports.
      }
    }
    command RecordDineInPayment is  {
      paymentAmount: Decimal(10,2) with {
        briefly "Amount"
        described as {
          |Payment amount from dine-in order.
        }
      }
      tableOrderId: String(1,50) with {
        briefly "Order ID"
        described as {
          |Originating table order.
        }
      }
    } with {
      briefly "Record dine-in payment"
      described as {
        |Record a dine-in payment for sales reporting.
      }
    }
    command RecordOnlinePayment is  {
      paymentAmount: Decimal(10,2) with {
        briefly "Amount"
        described as {
          |Payment amount from online order.
        }
      }
      onlineOrderId: String(1,50) with {
        briefly "Order ID"
        described as {
          |Originating online order.
        }
      }
    } with {
      briefly "Record online payment"
      described as {
        |Record an online payment for sales reporting.
      }
    }
    command RecordAccountingPosting is  {
      transactionId: String(1,50) with {
        briefly "Transaction ID"
        described as {
          |Accounting transaction.
        }
      }
    } with {
      briefly "Record accounting posting"
      described as {
        |Record an accounting posting confirmation.
      }
    }
    command RecordReservationEvent is  {
      reservationId: String(1,50) with {
        briefly "Reservation ID"
        described as {
          |The booking the reported event concerns. Reporting keeps the id so a
          |figure can be traced back to the visit behind it.
        }
      }
      eventType: String(1,30) with {
        briefly "Event type"
        described as {
          |Type of reservation event: made or cancelled.
        }
      }
    } with {
      briefly "Record reservation event"
      described as {
        |Record a reservation event for reporting.
      }
    }
    command RecordTableOrderEvent is  {
      tableOrderId: String(1,50) with {
        briefly "Order ID"
        described as {
          |The check the reported event concerns, kept so a day's revenue can
          |be broken back down to individual tables.
        }
      }
      eventType: String(1,30) with {
        briefly "Event type"
        described as {
          |Type of order event: payment or closed.
        }
      }
      paymentAmount: Decimal(10,2)? with {
        briefly "Amount"
        described as {
          |Payment amount if applicable.
        }
      }
    } with {
      briefly "Record table order event"
      described as {
        |Record a table order event for reporting.
      }
    }
    handler SalesReportPersistence is {
      on query GetSalesReport is {
        let salesReportView: type SalesReportView = prompt("read the sales report projection")
        reply salesReportView
      }
      on query GetLaborReport is {
        let laborReportView: type LaborReportView = prompt("read the labor report projection")
        reply laborReportView
      }
      on query GetInventoryReport is {
        let inventoryReportView: type InventoryReportView = prompt("read the inventory report projection")
        reply inventoryReportView
      }
      on command RecordDineInPayment is {
        do "upsert sales report record"
      }
      on command RecordOnlinePayment is {
        do "upsert online sales report record"
      }
      on command RecordAccountingPosting is {
        do "log accounting confirmation for reconciliation"
      }
      on command RecordReservationEvent is {
        do "record reservation event for sales reporting"
      }
      on command RecordTableOrderEvent is {
        do "record table order event for sales reporting"
      }
    } with {
      briefly "Sales report persistence"
      described as {
        |Persists sales report projections.
      }
    }
  } with {
    briefly "Sales report repository"
    described as {
      |Stores sales report data.
    }
  }
  repository LaborReportRepository as sink is {
    inlet LaborReportRepositoryFromLaborReport is type LaborReportCommand    schema LaborReportData is relational
      of laborReports as type LaborReportRecord
        index on field LaborReportRecord.laborReportDate
 with {
      briefly "Labor report schema"
      described as {
        |Relational schema for persisting labor reports.
      }
    }
    command RecordClockIn is  {
      shiftId: String(1,50) with {
        briefly "Shift ID"
        described as {
          |Shift being clocked into.
        }
      }
      clockedInAt: TimeStamp with {
        briefly "Time"
        described as {
          |Clock-in time.
        }
      }
    } with {
      briefly "Record clock-in"
      described as {
        |Record an employee clock-in for labor reporting.
      }
    }
    command RecordClockOut is  {
      shiftId: String(1,50) with {
        briefly "Shift ID"
        described as {
          |Shift being clocked out of.
        }
      }
      clockedOutAt: TimeStamp with {
        briefly "Time"
        described as {
          |Clock-out time.
        }
      }
    } with {
      briefly "Record clock-out"
      described as {
        |Record an employee clock-out for labor reporting.
      }
    }
    command RecordShiftCancellation is  {
      shiftId: String(1,50) with {
        briefly "Shift ID"
        described as {
          |Cancelled shift.
        }
      }
    } with {
      briefly "Record shift cancellation"
      described as {
        |Record a shift cancellation for labor reporting.
      }
    }
    command RecordShiftEvent is  {
      shiftId: String(1,50) with {
        briefly "Shift ID"
        described as {
          |The shift the reported event concerns, kept so labour cost can be
          |attributed to a specific rota slot.
        }
      }
      eventType: String(1,30) with {
        briefly "Event type"
        described as {
          |Type: clock-in, clock-out, or cancelled.
        }
      }
      eventTime: TimeStamp? with {
        briefly "Time"
        described as {
          |Event timestamp.
        }
      }
    } with {
      briefly "Record shift event"
      described as {
        |Record a shift event for reporting.
      }
    }
    handler LaborReportPersistence is {
      on command RecordClockIn is {
        do "upsert labor report record for clock-in"
      }
      on command RecordClockOut is {
        do "upsert labor report record for clock-out"
      }
      on command RecordShiftCancellation is {
        do "upsert labor report record for cancellation"
      }
      on command RecordShiftEvent is {
        do "record shift event for labor reporting"
      }
    } with {
      briefly "Labor report persistence"
      described as {
        |Persists labor report projections.
      }
    }
  } with {
    briefly "Labor report repository"
    described as {
      |Stores labor report data.
    }
  }
  repository InventoryReportRepository as sink is {
    inlet InventoryReportRepositoryFromInventoryReport is type InventoryReportCommand    schema InventoryReportData is relational
      of inventoryReports as type InventoryReportRecord
        index on field InventoryReportRecord.inventoryReportDate
 with {
      briefly "Inventory report schema"
      described as {
        |Relational schema for persisting inventory reports.
      }
    }
    command RecordStockReceived is  {
      inventoryItemId: String(1,50) with {
        briefly "ID"
        described as {
          |The stock line whose receipt is being reported.
        }
      }
      quantity: Decimal(10,2) with {
        briefly "Qty"
        described as {
          |Quantity received.
        }
      }
    } with {
      briefly "Record stock received"
      described as {
        |Record stock receipt for inventory reporting.
      }
    }
    command RecordStockConsumed is  {
      inventoryItemId: String(1,50) with {
        briefly "ID"
        described as {
          |The stock line whose consumption is being reported.
        }
      }
      quantity: Decimal(10,2) with {
        briefly "Qty"
        described as {
          |Quantity consumed.
        }
      }
    } with {
      briefly "Record stock consumed"
      described as {
        |Record stock consumption for inventory reporting.
      }
    }
    command RecordStockAdjusted is  {
      inventoryItemId: String(1,50) with {
        briefly "ID"
        described as {
          |The stock line whose correction is being reported.
        }
      }
      quantity: Decimal(10,2) with {
        briefly "Qty"
        described as {
          |Quantity adjusted.
        }
      }
    } with {
      briefly "Record stock adjusted"
      described as {
        |Record stock adjustment for inventory reporting.
      }
    }
    command RecordInventoryEvent is  {
      inventoryItemId: String(1,50) with {
        briefly "Item ID"
        described as {
          |The stock line the reported event concerns, kept so a variance can
          |be traced to the item that caused it.
        }
      }
      eventType: String(1,30) with {
        briefly "Event type"
        described as {
          |Type: received, consumed, or adjusted.
        }
      }
      quantity: Decimal(10,2)? with {
        briefly "Quantity"
        described as {
          |Quantity affected.
        }
      }
    } with {
      briefly "Record inventory event"
      described as {
        |Record an inventory event for reporting.
      }
    }
    handler InventoryReportPersistence is {
      on command RecordStockReceived is {
        do "upsert inventory report record for stock received"
      }
      on command RecordStockConsumed is {
        do "upsert inventory report record for stock consumed"
      }
      on command RecordStockAdjusted is {
        do "upsert inventory report record for stock adjusted"
      }
      on command RecordInventoryEvent is {
        do "record inventory event for reporting"
      }
    } with {
      briefly "Inventory report persistence"
      described as {
        |Persists inventory report projections.
      }
    }
  } with {
    briefly "Inventory report repository"
    described as {
      |Stores inventory report data.
    }
  }
  projector SalesReport as source is {
    updates repository Reporting.SalesReportRepository
    outlet SalesReportOut is type SalesReportCommand
    record SalesReportEntry is  {
      reportDate: Date with {
        briefly "Date"
        described as {
          |The trading day this row summarises. One row per day is what makes
          |the projection cheap to read.
        }
      }
      totalRevenue: Decimal(12,2) with {
        briefly "Revenue"
        described as {
          |Total revenue for the period.
        }
      }
      dineInRevenue: Decimal(12,2) with {
        briefly "Dine-in revenue"
        described as {
          |Revenue from dine-in orders.
        }
      }
      onlineRevenue: Decimal(12,2) with {
        briefly "Online revenue"
        described as {
          |Revenue from online orders.
        }
      }
      orderCount: Natural with {
        briefly "Orders"
        described as {
          |Total number of orders.
        }
      }
      averageOrderValue: Decimal(10,2) with {
        term Cover is  "One guest served in a service period. Capacity, staffing and spend-per-head are all counted in covers rather than in tables or orders." 
        term Check is  "The bill for one party, presented before payment. Counting checks rather than covers answers a different question: how many groups, not how many people." 
        briefly "Average order"
        described as {
          |Takings divided by covers. Moves for reasons revenue alone hides — a
          |busy night of small checks and a quiet night of large ones can total
          |the same.
        }
      }
    } with {
      briefly "Sales report entry"
      described as {
        |A single sales report data point.
      }
    }
    handler SalesReportHandler is {
      on paymentProcessed: event Restaurant.FrontOfHouse.TableOrder.PaymentProcessed is {
        let entry: type SalesReportEntry = "new SalesReportEntry from event"
        tell command RecordDineInPayment(tableOrderId = paymentProcessed.tableOrderId, paymentAmount = prompt("the payment amount of this record dine in payment")) to repository Reporting.SalesReportRepository
        do "upsert SalesReportEntry for current reportDate:
          increment orderCount, add PaymentProcessed
          .orderPayment amount to totalRevenue and
          dineInRevenue, recalculate averageOrderValue"
        set field SalesReportEntry.totalRevenue to "totalRevenue + payment amount"
      }
      on onlinePaymentProcessed: event Restaurant.OnlineOrdering.OnlineOrder.OnlinePaymentProcessed is {
        tell command RecordOnlinePayment(onlineOrderId = onlinePaymentProcessed.onlineOrderId, paymentAmount = prompt("the payment amount of this record online payment")) to repository Reporting.SalesReportRepository
        do "upsert SalesReportEntry for current reportDate:
          increment orderCount, add OnlinePaymentProcessed
          .onlinePayment amount to totalRevenue and
          onlineRevenue, recalculate averageOrderValue"
        set field SalesReportEntry.onlineRevenue to "onlineRevenue + payment amount"
      }
    } with {
      briefly "Sales report handler"
      described as {
        |Projects payment events into sales reports.
      }
    }
  } with {
    briefly "Sales report projector"
    described as {
      | Builds sales reports from payment events across
      | dine-in and online channels. Isolated from production
      | so report generation never impacts peak-hour performance.
    }
  }
  projector LaborReport as source is {
    updates repository Reporting.LaborReportRepository
    outlet LaborReportOut is type LaborReportCommand
    record LaborReportEntry is  {
      laborReportDate: Date with {
        briefly "Date"
        described as {
          |The trading day this labour row summarises.
        }
      }
      totalHoursWorked: Decimal(8,2) with {
        briefly "Hours"
        described as {
          |Hours clocked that day, accumulated as clock-outs arrive rather than
          |computed at close.
        }
      }
      shiftsCompleted: Natural with {
        briefly "Shifts"
        described as {
          |Number of completed shifts.
        }
      }
      shiftsCancelled: Natural with {
        briefly "Cancelled"
        described as {
          |Number of cancelled shifts.
        }
      }
      averageShiftDuration: Decimal(6,2) with {
        briefly "Average duration"
        described as {
          |Average shift duration in hours.
        }
      }
    } with {
      briefly "Labor report entry"
      described as {
        |A single labor report data point.
      }
    }
    handler LaborReportHandler is {
      on clockedIn: event BackOffice.Scheduling.Shift.ClockedIn is {
        let entry: type LaborReportEntry = "new LaborReportEntry from event"
        tell command RecordClockIn(shiftId = clockedIn.shiftId, clockedInAt = clockedIn.clockedInAt) to repository Reporting.LaborReportRepository
        do "upsert LaborReportEntry for current
          laborReportDate: record shift start from
          ClockedIn.clockedInAt"
        set field LaborReportEntry.totalHoursWorked to "updated hours"
      }
      on clockedOut: event BackOffice.Scheduling.Shift.ClockedOut is {
        tell command RecordClockOut(shiftId = clockedOut.shiftId, clockedOutAt = clockedOut.clockedOutAt) to repository Reporting.LaborReportRepository
        do "upsert LaborReportEntry for current
          laborReportDate: increment shiftsCompleted, add
          hours between ClockedIn and ClockedOut
          .clockedOutAt to totalHoursWorked, recalculate
          averageShiftDuration"
        set field LaborReportEntry.shiftsCompleted to "shiftsCompleted + 1"
      }
      on shiftCancelled: event BackOffice.Scheduling.Shift.ShiftCancelled is {
        tell command RecordShiftCancellation(shiftId = shiftCancelled.shiftId) to repository Reporting.LaborReportRepository
        do "upsert LaborReportEntry for current
          laborReportDate: increment shiftsCancelled"
        set field LaborReportEntry.shiftsCancelled to "shiftsCancelled + 1"
      }
    } with {
      briefly "Labor report handler"
      described as {
        |Projects scheduling events into labor reports.
      }
    }
  } with {
    briefly "Labor report projector"
    described as {
      |Builds labor reports from scheduling clock events.
    }
  }
  projector InventoryReport as source is {
    updates repository Reporting.InventoryReportRepository
    outlet InventoryReportOut is type InventoryReportCommand
    record InventoryReportEntry is  {
      inventoryReportDate: Date with {
        briefly "Date"
        described as {
          |The trading day this stock row summarises.
        }
      }
      totalItemsTracked: Natural with {
        briefly "Items tracked"
        described as {
          |Total inventory items being tracked.
        }
      }
      lowStockItems: Natural with {
        briefly "Low stock"
        described as {
          |Number of items below reorder threshold.
        }
      }
      outOfStockItems: Natural with {
        briefly "Out of stock"
        described as {
          |Number of out-of-stock items.
        }
      }
      totalStockValue: Decimal(14,2) with {
        briefly "Stock value"
        described as {
          |Estimated total stock value.
        }
      }
    } with {
      briefly "Inventory report entry"
      described as {
        |A single inventory report data point.
      }
    }
    handler InventoryReportHandler is {
      on stockReceived: event BackOffice.Inventory.InventoryItem.StockReceived is {
        let entry: type InventoryReportEntry = "new InventoryReportEntry from event"
        tell command RecordStockReceived(inventoryItemId = stockReceived.inventoryItemId, quantity = stockReceived.receivedQuantity) to repository Reporting.InventoryReportRepository
        do "upsert InventoryReportEntry for current
          inventoryReportDate: update totalItemsTracked,
          recalculate totalStockValue from
          StockReceived.newStockLevel, update lowStockItems
          and outOfStockItems counts"
        set field InventoryReportEntry.totalStockValue to "recalculated from newStockLevel"
      }
      on stockConsumed: event BackOffice.Inventory.InventoryItem.StockConsumed is {
        tell command RecordStockConsumed(inventoryItemId = stockConsumed.inventoryItemId, quantity = stockConsumed.consumedQuantity) to repository Reporting.InventoryReportRepository
        do "upsert InventoryReportEntry for current
          inventoryReportDate: recalculate totalStockValue
          from StockConsumed.remainingStockLevel, update
          lowStockItems and outOfStockItems counts"
        set field InventoryReportEntry.totalStockValue to "recalculated from remainingStockLevel"
      }
      on stockAdjusted: event BackOffice.Inventory.InventoryItem.StockAdjusted is {
        tell command RecordStockAdjusted(inventoryItemId = stockAdjusted.inventoryItemId, quantity = stockAdjusted.adjustmentQuantity) to repository Reporting.InventoryReportRepository
        do "upsert InventoryReportEntry for current
          inventoryReportDate: recalculate totalStockValue
          from StockAdjusted.adjustedStockLevel, update
          lowStockItems and outOfStockItems counts"
        set field InventoryReportEntry.totalStockValue to "recalculated from adjustedStockLevel"
      }
    } with {
      briefly "Inventory report handler"
      described as {
        |Projects inventory events into reports.
      }
    }
  } with {
    briefly "Inventory report projector"
    described as {
      |Builds inventory reports from stock events.
    }
  }
  adaptor AccountingSystemAdapter from context AccountingSystem as flow is { 
    inlet IncomingAccountingSystemEvents is type AccountingEvent    outlet TranslatedAccountingSystemEvents is type AccountingEvent
    handler InboundAccountingSystem is {
      on transactionPosted: event AccountingSystem.TransactionPosted is {
        send transactionPosted to outlet TranslatedAccountingSystemEvents
      }
      on other is {
        error "Unexpected message for adaptor AccountingSystemAdapter"
      }
    } with {
      briefly "Inbound AccountingSystem translation"
      described as {
        |Receives AccountingSystem events and forwards them to Reporting. This is the
        |seam that keeps the accounting system plug-in replaceable: a different
        |provider is absorbed here rather than in AccountingConfirmSink.
      }
    }
  } with {
    briefly "Anti-corruption layer for AccountingSystem"
    described as {
      |Stands between the external AccountingSystem and AccountingConfirmSink so the sink never names
      |the provider's wire types directly.
    }
  }
  connector 'AccountingEvent Stream' is from outlet Reporting.AccountingSystemAdapter.TranslatedAccountingSystemEvents to inlet Reporting.AccountingConfirmSink.IncomingPostingResults with {
    briefly "AccountingSystem events, translated, on their way to the sink"
    described as {
      |Carries ledger posting confirmations from the anti-corruption layer to the sink that
      |applies them inside Reporting.
    }
  }
  processor AccountingConfirmSink as sink is {
    inlet IncomingPostingResults is type AccountingEvent    handler AccountingConfirmHandler is {
      on event AccountingSystem.TransactionPosted is {
        let recordAccountingPosting: type RecordAccountingPosting = prompt("the record accounting posting that results from transaction posted")
        tell recordAccountingPosting to repository Reporting.SalesReportRepository
        do "log accounting confirmation for
          TransactionPosted.accountingTransactionId posted
          at TransactionPosted.postedAt for reconciliation"
      }
    } with {
      briefly "Accounting confirmation handler"
      described as {
        |Processes transaction posting confirmations from
        |the accounting system for reconciliation.
      }
    }
  } with {
    briefly "Accounting confirmation sink"
    described as {
      |Receives transaction posting confirmations from the
      |external accounting system.
    }
  }
  adaptor FromFrontOfHouseReservations from context Restaurant.FrontOfHouse is { 
    handler ReservationEventIntake is {
      on reservationMade: event Restaurant.FrontOfHouse.Reservation.ReservationMade is {
        tell command RecordReservationEvent(reservationId = reservationMade.reservationId, eventType = prompt("the event type of this record reservation event")) to repository Reporting.SalesReportRepository
        do "record reservation creation for sales reporting"
      }
      on reservationCancelled: event Restaurant.FrontOfHouse.Reservation.ReservationCancelled is {
        tell command RecordReservationEvent(reservationId = reservationCancelled.reservationId, eventType = prompt("the event type of this record reservation event")) to repository Reporting.SalesReportRepository
        do "record reservation cancellation for sales reporting"
      }
      on other is {
        error "Unexpected message for adaptor FromFrontOfHouseReservations"
      }
    } with {
      briefly "Reservation event intake"
      described as {
        |Processes incoming reservation events for sales
        |reporting.
      }
    }
    handler TableOrderEventIntake is {
      on paymentProcessed: event Restaurant.FrontOfHouse.TableOrder.PaymentProcessed is {
        tell command RecordTableOrderEvent(tableOrderId = paymentProcessed.tableOrderId, eventType = prompt("the event type of this record table order event"), paymentAmount = prompt("the payment amount of this record table order event")) to repository Reporting.SalesReportRepository
        do "record payment for sales reporting"
      }
      on orderClosed: event Restaurant.FrontOfHouse.TableOrder.OrderClosed is {
        tell command RecordTableOrderEvent(tableOrderId = orderClosed.tableOrderId, eventType = prompt("the event type of this record table order event"), paymentAmount = prompt("the payment amount of this record table order event")) to repository Reporting.SalesReportRepository
        do "record order closure for sales reporting"
      }
      on other is {
        error "Unexpected message for adaptor FromFrontOfHouseOrders"
      }
    } with {
      briefly "Table order event intake"
      described as {
        |Processes incoming table order events for sales
        |reporting.
      }
    }
  } with {
    briefly "Reservation event sink"
    described as {
      |Receives reservation events from the streaming
      |topology for sales and reservation reports.
    }
  }
  processor ShiftEventSink as sink is {
    inlet IncomingShiftEvents is type ShiftEvent    handler ShiftEventIntake is {
      on clockedIn: event BackOffice.Scheduling.Shift.ClockedIn is {
        tell command RecordShiftEvent(shiftId = clockedIn.shiftId, eventType = prompt("the event type of this record shift event"), eventTime = prompt("the event time of this record shift event")) to repository Reporting.LaborReportRepository
        do "record clock-in for labor reporting"
      }
      on clockedOut: event BackOffice.Scheduling.Shift.ClockedOut is {
        tell command RecordShiftEvent(shiftId = clockedOut.shiftId, eventType = prompt("the event type of this record shift event"), eventTime = prompt("the event time of this record shift event")) to repository Reporting.LaborReportRepository
        do "record clock-out for labor reporting"
      }
      on shiftCancelled: event BackOffice.Scheduling.Shift.ShiftCancelled is {
        tell command RecordShiftEvent(shiftId = shiftCancelled.shiftId, eventType = prompt("the event type of this record shift event"), eventTime = prompt("the event time of this record shift event")) to repository Reporting.LaborReportRepository
        do "record cancellation for labor reporting"
      }
    } with {
      briefly "Shift event intake"
      described as {
        |Processes incoming shift events for labor reporting.
      }
    }
  } with {
    briefly "Shift event sink"
    described as {
      |Receives shift events from the streaming topology
      |for labor reports.
    }
  }
  processor InventoryEventSink as sink is {
    inlet IncomingInventoryEvents is type InventoryEvent    handler InventoryEventIntake is {
      on stockReceived: event BackOffice.Inventory.InventoryItem.StockReceived is {
        tell command RecordInventoryEvent(inventoryItemId = stockReceived.inventoryItemId, eventType = prompt("the event type of this record inventory event"), quantity = stockReceived.receivedQuantity) to repository Reporting.InventoryReportRepository
        do "record stock received for inventory reporting"
      }
      on stockConsumed: event BackOffice.Inventory.InventoryItem.StockConsumed is {
        tell command RecordInventoryEvent(inventoryItemId = stockConsumed.inventoryItemId, eventType = prompt("the event type of this record inventory event"), quantity = stockConsumed.consumedQuantity) to repository Reporting.InventoryReportRepository
        do "record stock consumed for inventory reporting"
      }
      on stockAdjusted: event BackOffice.Inventory.InventoryItem.StockAdjusted is {
        tell command RecordInventoryEvent(inventoryItemId = stockAdjusted.inventoryItemId, eventType = prompt("the event type of this record inventory event"), quantity = stockAdjusted.adjustmentQuantity) to repository Reporting.InventoryReportRepository
        do "record stock adjusted for inventory reporting"
      }
    } with {
      briefly "Inventory event intake"
      described as {
        |Processes incoming inventory events for inventory
        |reporting.
      }
    }
  } with {
    briefly "Inventory event sink"
    described as {
      |Receives inventory events from the streaming topology
      |for inventory reports.
    }
  }
  processor NightlyCloseOut as void is {
    handler NightlyCloseOutRun is {
      on init is {
        do "roll the trading day forward: freeze yesterday's sales, labor and
          inventory rows so late arrivals cannot silently restate a day the
          owner has already read"
      }
      on other is {
        error "NightlyCloseOut takes no messages; it wakes on the clock"
      }
    } with {
      briefly "Nightly close-out run"
      described as {
        |Everything it needs is already in the three report repositories, so
        |it reads and writes them directly rather than through the stream.
      }
    }
  } with {
    briefly "Nightly close-out"
    described as {
      |The one processor here with no ports at all, and `as void` says so.
      |Nothing pushes it: it wakes on a clock at the end of trading, which is
      |exactly what having neither an inlet nor an outlet means.
      |
      |Giving it ports to look like its neighbours would be a lie about how
      |it runs — there is no upstream that could trigger a day boundary, and
      |a day boundary is not an event any part of the restaurant emits.
    }
  }
  connector 'SalesReportCommand Storage' is from outlet Reporting.SalesReport.SalesReportOut to inlet Reporting.SalesReportRepository.SalesReportRepositoryFromSalesReport with {
    briefly "SalesReport events on their way to storage"
  }
  connector 'LaborReportCommand Storage' is from outlet Reporting.LaborReport.LaborReportOut to inlet Reporting.LaborReportRepository.LaborReportRepositoryFromLaborReport with {
    briefly "LaborReport events on their way to storage"
  }
  connector 'InventoryReportCommand Storage' is from outlet Reporting.InventoryReport.InventoryReportOut to inlet Reporting.InventoryReportRepository.InventoryReportRepositoryFromInventoryReport with {
    briefly "InventoryReport events on their way to storage"
  }
} with {
  briefly "Reporting context"
  described as {
    | CQRS read-model context containing projectors for
    | sales, labor, and inventory reports. Completely
    | isolated from production contexts so that report
    | generation never degrades peak-hour restaurant
    | performance. Addresses the CEO's concern about
    | reports slowing down operations.
  }
}
