// Restaurant Operations - Order Context
context OrderContext as router is {
  inlet TableOrderCommandStreamIn is type TableOrderCommand  outlet TableOrderCommandStreamOut is type TableOrderCommand
  inlet MarketplaceOrderResultStreamIn is result TableOrder.TableOrderResult  outlet MarketplaceOrderResultStreamOut is result TableOrder.TableOrderResult
  handler OrderContextBoundary is {
    on createOrder: command TableOrder.CreateOrder is {
      send createOrder to outlet OrderContext.TableOrderCommandStreamOut
    }
    on addItem: command TableOrder.AddItem is {
      send addItem to outlet OrderContext.TableOrderCommandStreamOut
    }
    on modifyItem: command TableOrder.ModifyItem is {
      send modifyItem to outlet OrderContext.TableOrderCommandStreamOut
    }
    on voidItem: command TableOrder.VoidItem is {
      send voidItem to outlet OrderContext.TableOrderCommandStreamOut
    }
    on submitOrder: command TableOrder.SubmitOrder is {
      send submitOrder to outlet OrderContext.TableOrderCommandStreamOut
    }
    on markItemPreparing: command TableOrder.MarkItemPreparing is {
      send markItemPreparing to outlet OrderContext.TableOrderCommandStreamOut
    }
    on markItemReady: command TableOrder.MarkItemReady is {
      send markItemReady to outlet OrderContext.TableOrderCommandStreamOut
    }
    on serveItem: command TableOrder.ServeItem is {
      send serveItem to outlet OrderContext.TableOrderCommandStreamOut
    }
    on requestBill: command TableOrder.RequestBill is {
      send requestBill to outlet OrderContext.TableOrderCommandStreamOut
    }
    on applyDiscount: command TableOrder.ApplyDiscount is {
      send applyDiscount to outlet OrderContext.TableOrderCommandStreamOut
    }
    on processPayment: command TableOrder.ProcessPayment is {
      send processPayment to outlet OrderContext.TableOrderCommandStreamOut
    }
    on splitBill: command TableOrder.SplitBill is {
      send splitBill to outlet OrderContext.TableOrderCommandStreamOut
    }
    on closeOrder: command TableOrder.CloseOrder is {
      send closeOrder to outlet OrderContext.TableOrderCommandStreamOut
    }
    on cancelOrder: command TableOrder.CancelOrder is {
      send cancelOrder to outlet OrderContext.TableOrderCommandStreamOut
    }
    on tableOrderResult: result TableOrder.TableOrderResult is {
      send tableOrderResult to outlet OrderContext.MarketplaceOrderResultStreamOut
    }
    on other is {
      error "Unexpected message at the OrderContext boundary"
    }
  } with {
    briefly "Context boundary relay"
    described as {
      |The context is the port at its own boundary: peers connect here, never
      |to a definition inside. Inbound messages are relayed to the definition
      |that owns them; outbound ones are republished from here.
    }
  }
  connector 'TableOrderCommand Stream Intake' is from outlet OrderContext.TableOrderCommandStreamOut to inlet OrderContext.TableOrder.TableOrderCommands  connector 'MarketplaceOrderResult Stream Egress' is from outlet OrderContext.OrderRepository.OrderRepositoryResponses to inlet OrderContext.MarketplaceOrderResultStreamIn  adaptor FromKitchenDisplayService from context KitchenDisplayService is { 
    handler FromKitchenDisplayServiceHandler is {
      on event KitchenDisplayService.TicketBumped is {
        do "the model receives ticket bumped"
      }
      on event KitchenDisplayService.TicketDisplayed is {
        do "the model receives ticket displayed"
      }
      on other is {
        error "Unexpected message for adaptor FromKitchenDisplayService"
      }
    } with {
      briefly "from kitchen display service"
      described as {
        |Carries messages from the external KitchenDisplayService.
      }
    }
  } with {
    briefly "KitchenDisplayService integration"
    described as {
      |The model's side of the KitchenDisplayService conversation.
    }
  }
  adaptor ToKitchenDisplayService to context KitchenDisplayService is { 
    handler ToKitchenDisplayServiceHandler is {
      on command KitchenDisplayService.BumpTicket is {
        do "the model sends bump ticket"
      }
      on command KitchenDisplayService.SendTicket is {
        do "the model sends send ticket"
      }
      on other is {
        error "Unexpected message for adaptor ToKitchenDisplayService"
      }
    } with {
      briefly "to kitchen display service"
      described as {
        |Carries messages to the external KitchenDisplayService.
      }
    }
  } with {
    briefly "KitchenDisplayService integration"
    described as {
      |The model's side of the KitchenDisplayService conversation.
    }
  }
  adaptor FromMenuService from context MenuService is { 
    handler FromMenuServiceHandler is {
      on event MenuService.AvailabilityUpdated is {
        do "the model receives availability updated"
      }
      on result MenuService.ItemDetails is {
        do "the model receives item details"
      }
      on result MenuService.MenuItems is {
        do "the model receives menu items"
      }
      on other is {
        error "Unexpected message for adaptor FromMenuService"
      }
    } with {
      briefly "from menu service"
      described as {
        |Carries messages from the external MenuService.
      }
    }
  } with {
    briefly "MenuService integration"
    described as {
      |The model's side of the MenuService conversation.
    }
  }
  adaptor ToMenuService to context MenuService is { 
    handler ToMenuServiceHandler is {
      on query MenuService.GetItemDetails is {
        do "the model sends get item details"
      }
      on query MenuService.GetMenuItems is {
        do "the model sends get menu items"
      }
      on command MenuService.UpdateAvailability is {
        do "the model sends update availability"
      }
      on other is {
        error "Unexpected message for adaptor ToMenuService"
      }
    } with {
      briefly "to menu service"
      described as {
        |Carries messages to the external MenuService.
      }
    }
  } with {
    briefly "MenuService integration"
    described as {
      |The model's side of the MenuService conversation.
    }
  }
  adaptor FromPaymentProcessingService from context PaymentProcessingService is { 
    handler FromPaymentProcessingServiceHandler is {
      on event PaymentProcessingService.CardPaymentFailed is {
        do "the model receives card payment failed"
      }
      on event PaymentProcessingService.CardPaymentProcessed is {
        do "the model receives card payment processed"
      }
      on event PaymentProcessingService.RefundProcessed is {
        do "the model receives refund processed"
      }
      on other is {
        error "Unexpected message for adaptor FromPaymentProcessingService"
      }
    } with {
      briefly "from payment processing service"
      described as {
        |Carries messages from the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  adaptor ToPaymentProcessingService to context PaymentProcessingService is { 
    handler ToPaymentProcessingServiceHandler is {
      on command PaymentProcessingService.ProcessCardPayment is {
        do "the model sends process card payment"
      }
      on command PaymentProcessingService.ProcessRefund is {
        do "the model sends process refund"
      }
      on other is {
        error "Unexpected message for adaptor ToPaymentProcessingService"
      }
    } with {
      briefly "to payment processing service"
      described as {
        |Carries messages to the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  adaptor FromReceiptPrintingService from context ReceiptPrintingService is { 
    handler FromReceiptPrintingServiceHandler is {
      on event ReceiptPrintingService.KitchenTicketPrinted is {
        do "the model receives kitchen ticket printed"
      }
      on event ReceiptPrintingService.ReceiptPrinted is {
        do "the model receives receipt printed"
      }
      on other is {
        error "Unexpected message for adaptor FromReceiptPrintingService"
      }
    } with {
      briefly "from receipt printing service"
      described as {
        |Carries messages from the external ReceiptPrintingService.
      }
    }
  } with {
    briefly "ReceiptPrintingService integration"
    described as {
      |The model's side of the ReceiptPrintingService conversation.
    }
  }
  adaptor ToReceiptPrintingService to context ReceiptPrintingService is { 
    handler ToReceiptPrintingServiceHandler is {
      on command ReceiptPrintingService.PrintKitchenTicket is {
        do "the model sends print kitchen ticket"
      }
      on command ReceiptPrintingService.PrintReceipt is {
        do "the model sends print receipt"
      }
      on other is {
        error "Unexpected message for adaptor ToReceiptPrintingService"
      }
    } with {
      briefly "to receipt printing service"
      described as {
        |Carries messages to the external ReceiptPrintingService.
      }
    }
  } with {
    briefly "ReceiptPrintingService integration"
    described as {
      |The model's side of the ReceiptPrintingService conversation.
    }
  }
  adaptor FromReservationService from context ReservationService is { 
    handler FromReservationServiceHandler is {
      on result ReservationService.ReservationDetails is {
        do "the model receives reservation details"
      }
      on event ReservationService.ReservationSeated is {
        do "the model receives reservation seated"
      }
      on other is {
        error "Unexpected message for adaptor FromReservationService"
      }
    } with {
      briefly "from reservation service"
      described as {
        |Carries messages from the external ReservationService.
      }
    }
  } with {
    briefly "ReservationService integration"
    described as {
      |The model's side of the ReservationService conversation.
    }
  }
  adaptor ToReservationService to context ReservationService is { 
    handler ToReservationServiceHandler is {
      on query ReservationService.GetReservation is {
        do "the model sends get reservation"
      }
      on command ReservationService.MarkSeated is {
        do "the model sends mark seated"
      }
      on other is {
        error "Unexpected message for adaptor ToReservationService"
      }
    } with {
      briefly "to reservation service"
      described as {
        |Carries messages to the external ReservationService.
      }
    }
  } with {
    briefly "ReservationService integration"
    described as {
      |The model's side of the ReservationService conversation.
    }
  }
  adaptor FromTableManagementService from context TableManagementService is { 
    handler FromTableManagementServiceHandler is {
      on result TableManagementService.AvailableTables is {
        do "the model receives available tables"
      }
      on event TableManagementService.TableAssigned is {
        do "the model receives table assigned"
      }
      on event TableManagementService.TableReleased is {
        do "the model receives table released"
      }
      on other is {
        error "Unexpected message for adaptor FromTableManagementService"
      }
    } with {
      briefly "from table management service"
      described as {
        |Carries messages from the external TableManagementService.
      }
    }
  } with {
    briefly "TableManagementService integration"
    described as {
      |The model's side of the TableManagementService conversation.
    }
  }
  adaptor ToTableManagementService to context TableManagementService is { 
    handler ToTableManagementServiceHandler is {
      on command TableManagementService.AssignTable is {
        do "the model sends assign table"
      }
      on query TableManagementService.GetAvailableTables is {
        do "the model sends get available tables"
      }
      on command TableManagementService.ReleaseTable is {
        do "the model sends release table"
      }
      on other is {
        error "Unexpected message for adaptor ToTableManagementService"
      }
    } with {
      briefly "to table management service"
      described as {
        |Carries messages to the external TableManagementService.
      }
    }
  } with {
    briefly "TableManagementService integration"
    described as {
      |The model's side of the TableManagementService conversation.
    }
  }
  include "types.riddl"
  include "TableOrder.riddl"
  // Repository for order persistence
  type OrderRepositoryCommand is one of {
    OrderContext.OrderRepository.PersistBillRequested or OrderContext.OrderRepository.PersistBillSplit or OrderContext.OrderRepository.PersistDiscountApplied or OrderContext.OrderRepository.PersistItemAdded or OrderContext.OrderRepository.PersistItemModified or OrderContext.OrderRepository.PersistItemPreparing or OrderContext.OrderRepository.PersistItemReady or OrderContext.OrderRepository.PersistItemServed or OrderContext.OrderRepository.PersistItemVoided or OrderContext.OrderRepository.PersistOrderCancelled or OrderContext.OrderRepository.PersistOrderClosed or OrderContext.OrderRepository.PersistOrderCreated or OrderContext.OrderRepository.PersistOrderSubmitted or OrderContext.OrderRepository.PersistPaymentProcessed
  } with {
    briefly "Anything that writes OrderRepository"
    described as {
      |The persistence commands OrderRepository accepts. Entity events never
      |reach it directly; a projector turns each into one of these.
    }
  }
  repository OrderRepository as flow is {
    inlet OrderRepositoryFromRestaurantAnalytics is type OrderRepositoryCommand    command PersistBillRequested is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row BillRequested applies to.
        }
      }
    } with {
      briefly "Persist BillRequested"
      described as {
        |Instructs this repository to apply BillRequested to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistBillSplit is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row BillSplit applies to.
        }
      }
    } with {
      briefly "Persist BillSplit"
      described as {
        |Instructs this repository to apply BillSplit to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistDiscountApplied is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row DiscountApplied applies to.
        }
      }
    } with {
      briefly "Persist DiscountApplied"
      described as {
        |Instructs this repository to apply DiscountApplied to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemAdded is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemAdded applies to.
        }
      }
    } with {
      briefly "Persist ItemAdded"
      described as {
        |Instructs this repository to apply ItemAdded to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemModified is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemModified applies to.
        }
      }
    } with {
      briefly "Persist ItemModified"
      described as {
        |Instructs this repository to apply ItemModified to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemPreparing is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemPreparing applies to.
        }
      }
    } with {
      briefly "Persist ItemPreparing"
      described as {
        |Instructs this repository to apply ItemPreparing to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemReady is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemReady applies to.
        }
      }
    } with {
      briefly "Persist ItemReady"
      described as {
        |Instructs this repository to apply ItemReady to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemServed is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemServed applies to.
        }
      }
    } with {
      briefly "Persist ItemServed"
      described as {
        |Instructs this repository to apply ItemServed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistItemVoided is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row ItemVoided applies to.
        }
      }
    } with {
      briefly "Persist ItemVoided"
      described as {
        |Instructs this repository to apply ItemVoided to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistOrderSubmitted is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row OrderSubmitted applies to.
        }
      }
    } with {
      briefly "Persist OrderSubmitted"
      described as {
        |Instructs this repository to apply OrderSubmitted to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistOrderCancelled is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row OrderCancelled applies to.
        }
      }
    } with {
      briefly "Persist OrderCancelled"
      described as {
        |Instructs this repository to apply OrderCancelled to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistOrderClosed is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row OrderClosed applies to.
        }
      }
    } with {
      briefly "Persist OrderClosed"
      described as {
        |Instructs this repository to apply OrderClosed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistOrderCreated is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row OrderCreated applies to.
        }
      }
    } with {
      briefly "Persist OrderCreated"
      described as {
        |Instructs this repository to apply OrderCreated to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    command PersistPaymentProcessed is {
      orderId: TableOrderId with {
        briefly "Row addressed"
        described as {
          |Which stored row PaymentProcessed applies to.
        }
      }
    } with {
      briefly "Persist PaymentProcessed"
      described as {
        |Instructs this repository to apply PaymentProcessed to the stored row. A
        |persistence command, not a domain command: it declares no `yields`
        |because storing a row records no new entity state.
      }
    }
    outlet OrderRepositoryResponses is result TableOrder.TableOrderResult
    record StoredTableOrder is {
      tableOrderId: TableOrderId with {
        briefly "tableOrderId"
        described as {
          |The tableOrderId column of the stored tableOrder row.
        }
      }
      orderId: TableOrderId with {
        briefly "orderId"
        described as {
          |The orderId column of the stored tableOrder row.
        }
      }
      server: ServerId with {
        briefly "server"
        described as {
          |The server column of the stored tableOrder row.
        }
      }
      table: TableInfo with {
        briefly "table"
        described as {
          |The table column of the stored tableOrder row.
        }
      }
    } with {
      briefly "Stored tableOrder row"
      described as {
        |The persistence shape of tableOrder data as this repository stores it.
      }
    }
    schema OrderData is relational
      of orders as record StoredTableOrder
        index on field StoredTableOrder.orderId
        index on field StoredTableOrder.table.tableId
        index on field StoredTableOrder.server
 with {
      briefly "Order data schema"
      described as {
        |The relational schema holding order data for the OrderRepository.
      }
    }
    handler OrderPersistence is {
      on persistBillRequested: command PersistBillRequested is {
        do "apply BillRequested to the stored row"
      }
      on persistBillSplit: command PersistBillSplit is {
        do "apply BillSplit to the stored row"
      }
      on persistDiscountApplied: command PersistDiscountApplied is {
        do "apply DiscountApplied to the stored row"
      }
      on persistItemAdded: command PersistItemAdded is {
        do "apply ItemAdded to the stored row"
      }
      on persistItemModified: command PersistItemModified is {
        do "apply ItemModified to the stored row"
      }
      on persistItemPreparing: command PersistItemPreparing is {
        do "apply ItemPreparing to the stored row"
      }
      on persistItemReady: command PersistItemReady is {
        do "apply ItemReady to the stored row"
      }
      on persistItemServed: command PersistItemServed is {
        do "apply ItemServed to the stored row"
      }
      on persistItemVoided: command PersistItemVoided is {
        do "apply ItemVoided to the stored row"
      }
      on persistOrderSubmitted: command PersistOrderSubmitted is {
        do "apply OrderSubmitted to the stored row"
      }
      on persistOrderCancelled: command PersistOrderCancelled is {
        do "apply OrderCancelled to the stored row"
      }
      on persistOrderClosed: command PersistOrderClosed is {
        do "apply OrderClosed to the stored row"
      }
      on persistOrderCreated: command PersistOrderCreated is {
        do "apply OrderCreated to the stored row"
      }
      on persistPaymentProcessed: command PersistPaymentProcessed is {
        do "apply PaymentProcessed to the stored row"
      }
      on query GetRestaurantStats is {
        let restaurantStatsView: type RestaurantStatsView = prompt("read the restaurant stats projection")
        reply restaurantStatsView
      }
    } with {
      briefly "Persists order commands"
      described as {
        |Handles command-driven persistence.
      }
    }
  } with {
    briefly "Order data persistence"
    described as {
      | The OrderRepository provides persistent storage for
      | table orders and related data.
    }
  }
  // Projector for restaurant analytics
  query GetRestaurantStats replies result RestaurantStatsView is {
    restaurantStatsKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which restaurant stats to retrieve.
      }
    }
  } with {
    briefly "Get restaurant stats"
    described as {
      |Retrieves the restaurant stats projection.
    }
  }
  result RestaurantStatsView is {
    restaurantStats: RestaurantAnalytics.RestaurantStats with {
      briefly "Restaurant stats"
      described as {
        |The restaurant stats being returned.
      }
    }
  } with {
    briefly "Restaurant stats view"
    described as {
      |Read model view carrying the restaurant stats projection.
    }
  }
  projector RestaurantAnalytics as flow is {
    updates repository OrderContext.OrderRepository
    outlet RestaurantAnalyticsToRepository is type OrderRepositoryCommand
    inlet RestaurantAnalyticsFromTableOrder is type TableOrderEvent    record RestaurantStats is {
      totalOrders: Natural with {
        briefly "Total"
        described as {
          |Total orders.
        }
      }
      openOrders: Natural with {
        briefly "Open"
        described as {
          |Open orders.
        }
      }
      completedOrders: Natural with {
        briefly "Completed"
        described as {
          |Completed orders.
        }
      }
      totalRevenue: Decimal(12,2) with {
        briefly "Revenue"
        described as {
          |Total revenue.
        }
      }
      averageOrderValue: Decimal(10,2) with {
        briefly "AOV"
        described as {
          |Average order value.
        }
      }
      averageTableTurnTime: Duration with {
        briefly "Turn time"
        described as {
          |Average table turn.
        }
      }
    } with {
      briefly "Restaurant statistics"
      described as {
        |Restaurant operations metrics.
      }
    }
    handler AnalyticsHandler is {
      on billRequested: event TableOrder.BillRequested is {
        tell command OrderContext.OrderRepository.PersistBillRequested(orderId = billRequested.orderId) to repository OrderContext.OrderRepository
        do "apply BillRequested to the RestaurantAnalytics projection"
      }
      on billSplit: event TableOrder.BillSplit is {
        tell command OrderContext.OrderRepository.PersistBillSplit(orderId = billSplit.orderId) to repository OrderContext.OrderRepository
        do "apply BillSplit to the RestaurantAnalytics projection"
      }
      on discountApplied: event TableOrder.DiscountApplied is {
        tell command OrderContext.OrderRepository.PersistDiscountApplied(orderId = discountApplied.orderId) to repository OrderContext.OrderRepository
        do "apply DiscountApplied to the RestaurantAnalytics projection"
      }
      on itemAdded: event TableOrder.ItemAdded is {
        tell command OrderContext.OrderRepository.PersistItemAdded(orderId = itemAdded.orderId) to repository OrderContext.OrderRepository
        do "apply ItemAdded to the RestaurantAnalytics projection"
      }
      on itemModified: event TableOrder.ItemModified is {
        tell command OrderContext.OrderRepository.PersistItemModified(orderId = itemModified.orderId) to repository OrderContext.OrderRepository
        do "apply ItemModified to the RestaurantAnalytics projection"
      }
      on itemPreparing: event TableOrder.ItemPreparing is {
        tell command OrderContext.OrderRepository.PersistItemPreparing(orderId = itemPreparing.orderId) to repository OrderContext.OrderRepository
        do "apply ItemPreparing to the RestaurantAnalytics projection"
      }
      on itemReady: event TableOrder.ItemReady is {
        tell command OrderContext.OrderRepository.PersistItemReady(orderId = itemReady.orderId) to repository OrderContext.OrderRepository
        do "apply ItemReady to the RestaurantAnalytics projection"
      }
      on itemServed: event TableOrder.ItemServed is {
        tell command OrderContext.OrderRepository.PersistItemServed(orderId = itemServed.orderId) to repository OrderContext.OrderRepository
        do "apply ItemServed to the RestaurantAnalytics projection"
      }
      on itemVoided: event TableOrder.ItemVoided is {
        tell command OrderContext.OrderRepository.PersistItemVoided(orderId = itemVoided.orderId) to repository OrderContext.OrderRepository
        do "apply ItemVoided to the RestaurantAnalytics projection"
      }
      on orderSubmitted: event TableOrder.OrderSubmitted is {
        tell command OrderContext.OrderRepository.PersistOrderSubmitted(orderId = orderSubmitted.orderId) to repository OrderContext.OrderRepository
        do "apply OrderSubmitted to the RestaurantAnalytics projection"
      }
      on orderCreated: event TableOrder.OrderCreated is {
        tell command OrderContext.OrderRepository.PersistOrderCreated(orderId = orderCreated.orderId) to repository OrderContext.OrderRepository
        do "Update order counts"
      }
      on orderClosed: event TableOrder.OrderClosed is {
        tell command OrderContext.OrderRepository.PersistOrderClosed(orderId = orderClosed.orderId) to repository OrderContext.OrderRepository
        do "Update completion and revenue metrics"
      }
      on paymentProcessed: event TableOrder.PaymentProcessed is {
        tell command OrderContext.OrderRepository.PersistPaymentProcessed(orderId = paymentProcessed.orderId) to repository OrderContext.OrderRepository
        do "Update payment metrics"
      }
      on orderCancelled: event TableOrder.OrderCancelled is {
        tell command OrderContext.OrderRepository.PersistOrderCancelled(orderId = orderCancelled.orderId) to repository OrderContext.OrderRepository
        do "Update cancellation metrics"
      }
    } with {
      briefly "Maintains restaurant analytics"
      described as {
        |Projects events into analytics views.
      }
    }
  } with {
    briefly "Restaurant analytics projections"
    described as {
      |Maintains restaurant operations analytics data.
    }
  }
  // ---- Event publication for TableOrder ----
  type TableOrderEvent is one of {
    OrderContext.TableOrder.OrderCreated or OrderContext.TableOrder.ItemAdded or OrderContext.TableOrder.ItemModified or OrderContext.TableOrder.ItemVoided or OrderContext.TableOrder.OrderSubmitted or OrderContext.TableOrder.ItemPreparing or OrderContext.TableOrder.ItemReady or OrderContext.TableOrder.ItemServed or OrderContext.TableOrder.BillRequested or OrderContext.TableOrder.DiscountApplied or OrderContext.TableOrder.PaymentProcessed or OrderContext.TableOrder.BillSplit or OrderContext.TableOrder.OrderClosed or OrderContext.TableOrder.OrderCancelled
  } with {
    briefly "TableOrder event alternation"
    described as {
      |Union of all TableOrder events, for streaming and event-sourced
      |persistence.
    }
  }
  streamlet TableOrderEventSplit as flow is {
    inlet TableOrderEventSplitIn is type TableOrderEvent    outlet TableOrderEventSplitToRestaurantAnalytics is type TableOrderEvent
    handler TableOrderEventSplitHandler is {
      on orderCreated: event TableOrder.OrderCreated is {
        send orderCreated to outlet TableOrderEventSplitToRestaurantAnalytics
        tell orderCreated to entity OrderContext.TableOrder
      }
      on itemAdded: event TableOrder.ItemAdded is {
        send itemAdded to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemAdded to entity OrderContext.TableOrder
      }
      on itemModified: event TableOrder.ItemModified is {
        send itemModified to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemModified to entity OrderContext.TableOrder
      }
      on itemVoided: event TableOrder.ItemVoided is {
        send itemVoided to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemVoided to entity OrderContext.TableOrder
      }
      on orderSubmitted: event TableOrder.OrderSubmitted is {
        send orderSubmitted to outlet TableOrderEventSplitToRestaurantAnalytics
        tell orderSubmitted to entity OrderContext.TableOrder
      }
      on itemPreparing: event TableOrder.ItemPreparing is {
        send itemPreparing to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemPreparing to entity OrderContext.TableOrder
      }
      on itemReady: event TableOrder.ItemReady is {
        send itemReady to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemReady to entity OrderContext.TableOrder
      }
      on itemServed: event TableOrder.ItemServed is {
        send itemServed to outlet TableOrderEventSplitToRestaurantAnalytics
        tell itemServed to entity OrderContext.TableOrder
      }
      on billRequested: event TableOrder.BillRequested is {
        send billRequested to outlet TableOrderEventSplitToRestaurantAnalytics
        tell billRequested to entity OrderContext.TableOrder
      }
      on discountApplied: event TableOrder.DiscountApplied is {
        send discountApplied to outlet TableOrderEventSplitToRestaurantAnalytics
        tell discountApplied to entity OrderContext.TableOrder
      }
      on paymentProcessed: event TableOrder.PaymentProcessed is {
        send paymentProcessed to outlet TableOrderEventSplitToRestaurantAnalytics
        tell paymentProcessed to entity OrderContext.TableOrder
      }
      on billSplit: event TableOrder.BillSplit is {
        send billSplit to outlet TableOrderEventSplitToRestaurantAnalytics
        tell billSplit to entity OrderContext.TableOrder
      }
      on orderClosed: event TableOrder.OrderClosed is {
        send orderClosed to outlet TableOrderEventSplitToRestaurantAnalytics
        tell orderClosed to entity OrderContext.TableOrder
      }
      on orderCancelled: event TableOrder.OrderCancelled is {
        send orderCancelled to outlet TableOrderEventSplitToRestaurantAnalytics
        tell orderCancelled to entity OrderContext.TableOrder
      }
      on other is {
        error "Unexpected message for processor TableOrderEventSplit"
      }
    } with {
      briefly "TableOrder event fan-out handler"
      described as {
        |Delivers each TableOrder event to all of its consumers.
      }
    }
  } with {
    briefly "TableOrder event fan-out"
    described as {
      |Fans TableOrder events out to the repository and projectors.
    }
  }
  connector 'TableOrderEvent Stream' is from outlet OrderContext.TableOrder.TableOrderEvents2 to inlet OrderContext.TableOrderEventSplit.TableOrderEventSplitIn with {
    briefly "TableOrder events, fanned out to storage and read models"
  }
  connector 'RestaurantAnalytics Feed' is from outlet OrderContext.TableOrderEventSplit.TableOrderEventSplitToRestaurantAnalytics to inlet OrderContext.RestaurantAnalytics.RestaurantAnalyticsFromTableOrder with {
    briefly "TableOrder events feeding the RestaurantAnalytics projection"
  }
  connector 'RestaurantAnalytics Storage' is from outlet OrderContext.RestaurantAnalytics.RestaurantAnalyticsToRepository to inlet OrderContext.OrderRepository.OrderRepositoryFromRestaurantAnalytics with {
    briefly "RestaurantAnalytics updates on their way to storage"
  }
} with {
  briefly "Bounded context for restaurant orders"
  described as {
    | The OrderContext is the bounded context for
    | table orders and restaurant service operations.
  }
}
