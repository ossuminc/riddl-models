// Airline Reservations - Reservation Context
context ReservationContext is {
  adaptor FromBoardingService from context BoardingService is { 
    handler FromBoardingServiceHandler is {
      on event BoardingService.BoardingCompleted is {
        do "the model receives boarding completed"
      }
      on event BoardingService.BoardingPassScanned is {
        do "the model receives boarding pass scanned"
      }
      on event BoardingService.BoardingStarted is {
        do "the model receives boarding started"
      }
      on other is {
        error "Unexpected message for adaptor FromBoardingService"
      }
    } with {
      briefly "from boarding service"
      described as {
        |Carries messages from the external BoardingService.
      }
    }
  } with {
    briefly "BoardingService integration"
    described as {
      |The model's side of the BoardingService conversation.
    }
  }
  adaptor ToBoardingService to context BoardingService is { 
    handler ToBoardingServiceHandler is {
      on command BoardingService.CompleteBoarding is {
        do "the model sends complete boarding"
      }
      on command BoardingService.ScanBoardingPass is {
        do "the model sends scan boarding pass"
      }
      on command BoardingService.StartBoarding is {
        do "the model sends start boarding"
      }
      on other is {
        error "Unexpected message for adaptor ToBoardingService"
      }
    } with {
      briefly "to boarding service"
      described as {
        |Carries messages to the external BoardingService.
      }
    }
  } with {
    briefly "BoardingService integration"
    described as {
      |The model's side of the BoardingService conversation.
    }
  }
  adaptor FromCheckInService from context CheckInService is { 
    handler FromCheckInServiceHandler is {
      on event CheckInService.CheckInClosed is {
        do "the model receives check in closed"
      }
      on event CheckInService.CheckInOpened is {
        do "the model receives check in opened"
      }
      on other is {
        error "Unexpected message for adaptor FromCheckInService"
      }
    } with {
      briefly "from check in service"
      described as {
        |Carries messages from the external CheckInService.
      }
    }
  } with {
    briefly "CheckInService integration"
    described as {
      |The model's side of the CheckInService conversation.
    }
  }
  adaptor ToCheckInService to context CheckInService is { 
    handler ToCheckInServiceHandler is {
      on command CheckInService.CloseCheckIn is {
        do "the model sends close check in"
      }
      on command CheckInService.OpenCheckIn is {
        do "the model sends open check in"
      }
      on other is {
        error "Unexpected message for adaptor ToCheckInService"
      }
    } with {
      briefly "to check in service"
      described as {
        |Carries messages to the external CheckInService.
      }
    }
  } with {
    briefly "CheckInService integration"
    described as {
      |The model's side of the CheckInService conversation.
    }
  }
  adaptor FromFlightSearchService from context FlightSearchService is { 
    handler FromFlightSearchServiceHandler is {
      on result FlightSearchService.FlightDetails is {
        do "the model receives flight details"
      }
      on result FlightSearchService.FlightSearchResults is {
        do "the model receives flight search results"
      }
      on other is {
        error "Unexpected message for adaptor FromFlightSearchService"
      }
    } with {
      briefly "from flight search service"
      described as {
        |Carries messages from the external FlightSearchService.
      }
    }
  } with {
    briefly "FlightSearchService integration"
    described as {
      |The model's side of the FlightSearchService conversation.
    }
  }
  adaptor ToFlightSearchService to context FlightSearchService is { 
    handler ToFlightSearchServiceHandler is {
      on query FlightSearchService.GetFlightDetails is {
        do "the model sends get flight details"
      }
      on query FlightSearchService.SearchFlights is {
        do "the model sends search flights"
      }
      on other is {
        error "Unexpected message for adaptor ToFlightSearchService"
      }
    } with {
      briefly "to flight search service"
      described as {
        |Carries messages to the external FlightSearchService.
      }
    }
  } with {
    briefly "FlightSearchService integration"
    described as {
      |The model's side of the FlightSearchService conversation.
    }
  }
  adaptor FromNotificationService from context NotificationService is { 
    handler FromNotificationServiceHandler is {
      on event NotificationService.BoardingPassSent is {
        do "the model receives boarding pass sent"
      }
      on event NotificationService.CheckInReminderSent is {
        do "the model receives check in reminder sent"
      }
      on event NotificationService.FlightStatusSent is {
        do "the model receives flight status sent"
      }
      on event NotificationService.ItinerarySent is {
        do "the model receives itinerary sent"
      }
      on other is {
        error "Unexpected message for adaptor FromNotificationService"
      }
    } with {
      briefly "from notification service"
      described as {
        |Carries messages from the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor ToNotificationService to context NotificationService is { 
    handler ToNotificationServiceHandler is {
      on command NotificationService.SendBoardingPass is {
        do "the model sends send boarding pass"
      }
      on command NotificationService.SendCheckInReminder is {
        do "the model sends send check in reminder"
      }
      on command NotificationService.SendFlightStatusUpdate is {
        do "the model sends send flight status update"
      }
      on command NotificationService.SendItineraryConfirmation is {
        do "the model sends send itinerary confirmation"
      }
      on other is {
        error "Unexpected message for adaptor ToNotificationService"
      }
    } with {
      briefly "to notification service"
      described as {
        |Carries messages to the external NotificationService.
      }
    }
  } with {
    briefly "NotificationService integration"
    described as {
      |The model's side of the NotificationService conversation.
    }
  }
  adaptor FromPaymentProcessingService from context PaymentProcessingService is { 
    handler FromPaymentProcessingServiceHandler is {
      on event PaymentProcessingService.PaymentFailed is {
        do "the model receives payment failed"
      }
      on event PaymentProcessingService.PaymentSucceeded is {
        do "the model receives payment succeeded"
      }
      on event PaymentProcessingService.RefundSucceeded is {
        do "the model receives refund succeeded"
      }
      on other is {
        error "Unexpected message for adaptor FromPaymentProcessingService"
      }
    } with {
      briefly "from payment processing service"
      described as {
        |Carries messages from the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  adaptor ToPaymentProcessingService to context PaymentProcessingService is { 
    handler ToPaymentProcessingServiceHandler is {
      on command PaymentProcessingService.ProcessReservationPayment is {
        do "the model sends process reservation payment"
      }
      on command PaymentProcessingService.ProcessReservationRefund is {
        do "the model sends process reservation refund"
      }
      on other is {
        error "Unexpected message for adaptor ToPaymentProcessingService"
      }
    } with {
      briefly "to payment processing service"
      described as {
        |Carries messages to the external PaymentProcessingService.
      }
    }
  } with {
    briefly "PaymentProcessingService integration"
    described as {
      |The model's side of the PaymentProcessingService conversation.
    }
  }
  adaptor FromPricingService from context PricingService is { 
    handler FromPricingServiceHandler is {
      on result PricingService.FareCalculation is {
        do "the model receives fare calculation"
      }
      on other is {
        error "Unexpected message for adaptor FromPricingService"
      }
    } with {
      briefly "from pricing service"
      described as {
        |Carries messages from the external PricingService.
      }
    }
  } with {
    briefly "PricingService integration"
    described as {
      |The model's side of the PricingService conversation.
    }
  }
  adaptor ToPricingService to context PricingService is { 
    handler ToPricingServiceHandler is {
      on query PricingService.CalculateFare is {
        do "the model sends calculate fare"
      }
      on other is {
        error "Unexpected message for adaptor ToPricingService"
      }
    } with {
      briefly "to pricing service"
      described as {
        |Carries messages to the external PricingService.
      }
    }
  } with {
    briefly "PricingService integration"
    described as {
      |The model's side of the PricingService conversation.
    }
  }
  adaptor FromSeatInventoryService from context SeatInventoryService is { 
    handler FromSeatInventoryServiceHandler is {
      on result SeatInventoryService.AvailableSeats is {
        do "the model receives available seats"
      }
      on event SeatInventoryService.SeatReleased is {
        do "the model receives seat released"
      }
      on event SeatInventoryService.SeatReserved is {
        do "the model receives seat reserved"
      }
      on other is {
        error "Unexpected message for adaptor FromSeatInventoryService"
      }
    } with {
      briefly "from seat inventory service"
      described as {
        |Carries messages from the external SeatInventoryService.
      }
    }
  } with {
    briefly "SeatInventoryService integration"
    described as {
      |The model's side of the SeatInventoryService conversation.
    }
  }
  adaptor ToSeatInventoryService to context SeatInventoryService is { 
    handler ToSeatInventoryServiceHandler is {
      on query SeatInventoryService.GetAvailableSeats is {
        do "the model sends get available seats"
      }
      on command SeatInventoryService.ReleaseSeat is {
        do "the model sends release seat"
      }
      on command SeatInventoryService.ReserveSeat is {
        do "the model sends reserve seat"
      }
      on other is {
        error "Unexpected message for adaptor ToSeatInventoryService"
      }
    } with {
      briefly "to seat inventory service"
      described as {
        |Carries messages to the external SeatInventoryService.
      }
    }
  } with {
    briefly "SeatInventoryService integration"
    described as {
      |The model's side of the SeatInventoryService conversation.
    }
  }
  include "types.riddl"
  include "Reservation.riddl"
  // Repository for reservation persistence
  repository ReservationRepository as merge is {
    inlet ReservationRepositoryFromReservation is type ReservationEvent    inlet ReservationRepositoryFromAirlineAnalytics is type ReservationEvent    outlet ReservationRepositoryResponses is type Reservation.ReservationResult
    record StoredReservation is  {
      reservationId: ReservationId with {
        briefly "reservationId"
        described as {
          |The reservationId column of the stored reservation row.
        }
      }
      confirmationCode: String(6,6) with {
        briefly "confirmationCode"
        described as {
          |The confirmationCode column of the stored reservation row.
        }
      }
      passenger: PassengerInfo with {
        briefly "passenger"
        described as {
          |The passenger column of the stored reservation row.
        }
      }
    } with {
      briefly "Stored reservation row"
      described as {
        |The persistence shape of reservation data as this repository stores it.
      }
    }
    schema ReservationData is relational
      of reservations as type StoredReservation
        index on field StoredReservation.reservationId
        index on field StoredReservation.passenger.passengerId
        index on field StoredReservation.confirmationCode
 with {
      briefly "Reservation data schema"
      described as {
        |The relational schema holding reservation data for the ReservationRepository.
      }
    }
    handler ReservationPersistence is {
      on query GetAirlineStats is {
        do "read the airline stats projection"
        yield result AirlineStatsView
      }
      on command Reservation.CreateReservation is {
        tell event Reservation.ReservationCreated to entity ReservationContext.Reservation
        do "Store new reservation"
      }
      on command Reservation.SelectSeat is {
        do "Update seat assignment"
      }
      on command Reservation.AddExtraBaggage is {
        tell event Reservation.ExtraBaggageAdded to entity ReservationContext.Reservation
        do "Store extra baggage"
      }
      on command Reservation.SetMealPreference is {
        tell event Reservation.MealPreferenceSet to entity ReservationContext.Reservation
        do "Store meal preference"
      }
      on command Reservation.RequestSpecialService is {
        do "Store special service"
      }
      on command Reservation.ProcessPayment is {
        tell event Reservation.PaymentProcessed to entity ReservationContext.Reservation
        do "Store payment"
      }
      on command Reservation.ConfirmReservation is {
        tell event Reservation.ReservationConfirmed to entity ReservationContext.Reservation
        do "Update to confirmed"
      }
      on command Reservation.CheckIn is {
        do "Update to checked in"
      }
      on command Reservation.IssueBoardingPass is {
        tell event Reservation.BoardingPassIssued to entity ReservationContext.Reservation
        do "Store boarding pass"
      }
      on command Reservation.BoardFlight is {
        do "Update to boarded"
      }
      on command Reservation.ChangeFlightSegment is {
        do "Update flight segment"
      }
      on command Reservation.CancelReservation is {
        tell event Reservation.ReservationCancelled to entity ReservationContext.Reservation
        do "Update to cancelled"
      }
      on command Reservation.ProcessRefund is {
        tell event Reservation.RefundProcessed to entity ReservationContext.Reservation
        do "Store refund"
      }
    } with {
      briefly "Persists reservation commands"
      described as {
        |Handles command-driven persistence.
      }
    }
  } with {
    briefly "Reservation data persistence"
    described as {
      | The ReservationRepository provides persistent storage for
      | reservations and related data.
    }
  }
  // Projector for airline analytics
  query GetAirlineStats is  {
    airlineStatsKey: String(1,100) with {
      briefly "Key"
      described as {
        |Identifies which airline stats to retrieve.
      }
    }
  } with {
    briefly "Get airline stats"
    described as {
      |Retrieves the airline stats projection.
    }
  }
  result AirlineStatsView is  {
    airlineStats: AirlineAnalytics.AirlineStats with {
      briefly "Airline stats"
      described as {
        |The airline stats being returned.
      }
    }
  } with {
    briefly "Airline stats view"
    described as {
      |Read model view carrying the airline stats projection.
    }
  }
  projector AirlineAnalytics as flow is {
    updates repository ReservationContext.ReservationRepository
    inlet AirlineAnalyticsFromReservation is type ReservationEvent    outlet AirlineAnalyticsOut is type ReservationEvent
    record AirlineStats is  {
      totalReservations: Natural with {
        briefly "Total"
        described as {
          |Total reservations.
        }
      }
      confirmedReservations: Natural with {
        briefly "Confirmed"
        described as {
          |Confirmed reservations.
        }
      }
      checkedInCount: Natural with {
        briefly "Checked in"
        described as {
          |Checked in count.
        }
      }
      cancelledReservations: Natural with {
        briefly "Cancelled"
        described as {
          |Cancelled reservations.
        }
      }
      totalRevenue: Decimal(12,2) with {
        briefly "Revenue"
        described as {
          |Total revenue.
        }
      }
      loadFactor: Decimal(5,2) with {
        briefly "Load factor"
        described as {
          |Seat occupancy rate.
        }
      }
    } with {
      briefly "Airline statistics"
      described as {
        |Airline booking metrics.
      }
    }
    handler AnalyticsHandler is {
      on event Reservation.ReservationCreated is {
        tell event Reservation.ReservationCreated to repository ReservationContext.ReservationRepository
        do "Update reservation counts"
      }
      on event Reservation.ReservationConfirmed is {
        tell event Reservation.ReservationConfirmed to repository ReservationContext.ReservationRepository
        do "Update confirmed counts"
      }
      on event Reservation.PaymentProcessed is {
        tell event Reservation.PaymentProcessed to repository ReservationContext.ReservationRepository
        do "Update revenue metrics"
      }
      on event Reservation.PassengerCheckedIn is {
        tell event Reservation.PassengerCheckedIn to repository ReservationContext.ReservationRepository
        do "Update check-in metrics"
      }
      on event Reservation.ReservationCancelled is {
        tell event Reservation.ReservationCancelled to repository ReservationContext.ReservationRepository
        do "Update cancellation metrics"
      }
    } with {
      briefly "Maintains airline analytics"
      described as {
        |Projects events into analytics views.
      }
    }
  } with {
    briefly "Airline analytics projections"
    described as {
      |Maintains airline booking analytics data.
    }
  }
  // ---- Event publication for Reservation ----
  type ReservationEvent is one of {
    ReservationContext.Reservation.ReservationCreated or ReservationContext.Reservation.SeatSelected or ReservationContext.Reservation.ExtraBaggageAdded or ReservationContext.Reservation.MealPreferenceSet or ReservationContext.Reservation.SpecialServiceRequested or ReservationContext.Reservation.PaymentProcessed or ReservationContext.Reservation.ReservationConfirmed or ReservationContext.Reservation.PassengerCheckedIn or ReservationContext.Reservation.BoardingPassIssued or ReservationContext.Reservation.PassengerBoarded or ReservationContext.Reservation.FlightSegmentChanged or ReservationContext.Reservation.ReservationCancelled or ReservationContext.Reservation.RefundProcessed
  } with {
    briefly "Reservation event alternation"
    described as {
      |Union of all Reservation events, for streaming and event-sourced
      |persistence.
    }
  }
  processor ReservationEventSource as source is {
    outlet ReservationEvents is type ReservationEvent
    handler ReservationSourceHandler is {
      on event Reservation.ReservationCreated is {
        send event Reservation.ReservationCreated to outlet ReservationEvents
      }
      on event Reservation.SeatSelected is {
        send event Reservation.SeatSelected to outlet ReservationEvents
      }
      on event Reservation.ExtraBaggageAdded is {
        send event Reservation.ExtraBaggageAdded to outlet ReservationEvents
      }
      on event Reservation.MealPreferenceSet is {
        send event Reservation.MealPreferenceSet to outlet ReservationEvents
      }
      on event Reservation.SpecialServiceRequested is {
        send event Reservation.SpecialServiceRequested to outlet ReservationEvents
      }
      on event Reservation.PaymentProcessed is {
        send event Reservation.PaymentProcessed to outlet ReservationEvents
      }
      on event Reservation.ReservationConfirmed is {
        send event Reservation.ReservationConfirmed to outlet ReservationEvents
      }
      on event Reservation.PassengerCheckedIn is {
        send event Reservation.PassengerCheckedIn to outlet ReservationEvents
      }
      on event Reservation.BoardingPassIssued is {
        send event Reservation.BoardingPassIssued to outlet ReservationEvents
      }
      on event Reservation.PassengerBoarded is {
        send event Reservation.PassengerBoarded to outlet ReservationEvents
      }
      on event Reservation.FlightSegmentChanged is {
        send event Reservation.FlightSegmentChanged to outlet ReservationEvents
      }
      on event Reservation.ReservationCancelled is {
        send event Reservation.ReservationCancelled to outlet ReservationEvents
      }
      on event Reservation.RefundProcessed is {
        send event Reservation.RefundProcessed to outlet ReservationEvents
      }
      on other is {
        error "Unexpected message for source ReservationEventSource"
      }
    } with {
      briefly "Reservation source handler"
      described as {
        |Publishes Reservation events onto the outlet.
      }
    }
  } with {
    briefly "Reservation event source"
    described as {
      |Publishes Reservation entity events into the streaming topology.
    }
  }
  processor ReservationEventSink as sink is {
    inlet ReservationInlet is type ReservationEvent    handler ReservationSinkHandler is {
      on event Reservation.ReservationCreated is {
        tell event Reservation.ReservationCreated to repository ReservationContext.ReservationRepository
      }
      on event Reservation.SeatSelected is {
        tell event Reservation.SeatSelected to repository ReservationContext.ReservationRepository
      }
      on event Reservation.ExtraBaggageAdded is {
        tell event Reservation.ExtraBaggageAdded to repository ReservationContext.ReservationRepository
      }
      on event Reservation.MealPreferenceSet is {
        tell event Reservation.MealPreferenceSet to repository ReservationContext.ReservationRepository
      }
      on event Reservation.SpecialServiceRequested is {
        tell event Reservation.SpecialServiceRequested to repository ReservationContext.ReservationRepository
      }
      on event Reservation.PaymentProcessed is {
        tell event Reservation.PaymentProcessed to repository ReservationContext.ReservationRepository
      }
      on event Reservation.ReservationConfirmed is {
        tell event Reservation.ReservationConfirmed to repository ReservationContext.ReservationRepository
      }
      on event Reservation.PassengerCheckedIn is {
        tell event Reservation.PassengerCheckedIn to repository ReservationContext.ReservationRepository
      }
      on event Reservation.BoardingPassIssued is {
        tell event Reservation.BoardingPassIssued to repository ReservationContext.ReservationRepository
      }
      on event Reservation.PassengerBoarded is {
        tell event Reservation.PassengerBoarded to repository ReservationContext.ReservationRepository
      }
      on event Reservation.FlightSegmentChanged is {
        tell event Reservation.FlightSegmentChanged to repository ReservationContext.ReservationRepository
      }
      on event Reservation.ReservationCancelled is {
        tell event Reservation.ReservationCancelled to repository ReservationContext.ReservationRepository
      }
      on event Reservation.RefundProcessed is {
        tell event Reservation.RefundProcessed to repository ReservationContext.ReservationRepository
      }
      on other is {
        error "Unexpected message for sink ReservationEventSink"
      }
    } with {
      briefly "Reservation sink handler"
      described as {
        |Persists Reservation events via the repository.
      }
    }
  } with {
    briefly "Reservation event sink"
    described as {
      |Receives Reservation events for persistence.
    }
  }
  connector ReservationEventFlow is from outlet ReservationContext.ReservationEventSource.ReservationEvents to inlet ReservationContext.ReservationEventSink.ReservationInlet with {
    briefly "Reservation event flow"
    described as {
      |Connects the Reservation source to its sink.
    }
  }
  processor ReservationEventSplit as split is {
    inlet ReservationEventSplitIn is type ReservationEvent    outlet ReservationEventSplitToReservationRepository is type ReservationEvent
    outlet ReservationEventSplitToAirlineAnalytics is type ReservationEvent
    handler ReservationEventSplitHandler is {
      on event Reservation.ReservationCreated is {
        send event Reservation.ReservationCreated to outlet ReservationEventSplitToReservationRepository
        send event Reservation.ReservationCreated to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.ReservationCreated to entity ReservationContext.Reservation
      }
      on event Reservation.SeatSelected is {
        send event Reservation.SeatSelected to outlet ReservationEventSplitToReservationRepository
        send event Reservation.SeatSelected to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.SeatSelected to entity ReservationContext.Reservation
      }
      on event Reservation.ExtraBaggageAdded is {
        send event Reservation.ExtraBaggageAdded to outlet ReservationEventSplitToReservationRepository
        send event Reservation.ExtraBaggageAdded to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.ExtraBaggageAdded to entity ReservationContext.Reservation
      }
      on event Reservation.MealPreferenceSet is {
        send event Reservation.MealPreferenceSet to outlet ReservationEventSplitToReservationRepository
        send event Reservation.MealPreferenceSet to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.MealPreferenceSet to entity ReservationContext.Reservation
      }
      on event Reservation.SpecialServiceRequested is {
        send event Reservation.SpecialServiceRequested to outlet ReservationEventSplitToReservationRepository
        send event Reservation.SpecialServiceRequested to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.SpecialServiceRequested to entity ReservationContext.Reservation
      }
      on event Reservation.PaymentProcessed is {
        send event Reservation.PaymentProcessed to outlet ReservationEventSplitToReservationRepository
        send event Reservation.PaymentProcessed to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.PaymentProcessed to entity ReservationContext.Reservation
      }
      on event Reservation.ReservationConfirmed is {
        send event Reservation.ReservationConfirmed to outlet ReservationEventSplitToReservationRepository
        send event Reservation.ReservationConfirmed to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.ReservationConfirmed to entity ReservationContext.Reservation
      }
      on event Reservation.PassengerCheckedIn is {
        send event Reservation.PassengerCheckedIn to outlet ReservationEventSplitToReservationRepository
        send event Reservation.PassengerCheckedIn to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.PassengerCheckedIn to entity ReservationContext.Reservation
      }
      on event Reservation.BoardingPassIssued is {
        send event Reservation.BoardingPassIssued to outlet ReservationEventSplitToReservationRepository
        send event Reservation.BoardingPassIssued to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.BoardingPassIssued to entity ReservationContext.Reservation
      }
      on event Reservation.PassengerBoarded is {
        send event Reservation.PassengerBoarded to outlet ReservationEventSplitToReservationRepository
        send event Reservation.PassengerBoarded to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.PassengerBoarded to entity ReservationContext.Reservation
      }
      on event Reservation.FlightSegmentChanged is {
        send event Reservation.FlightSegmentChanged to outlet ReservationEventSplitToReservationRepository
        send event Reservation.FlightSegmentChanged to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.FlightSegmentChanged to entity ReservationContext.Reservation
      }
      on event Reservation.ReservationCancelled is {
        send event Reservation.ReservationCancelled to outlet ReservationEventSplitToReservationRepository
        send event Reservation.ReservationCancelled to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.ReservationCancelled to entity ReservationContext.Reservation
      }
      on event Reservation.RefundProcessed is {
        send event Reservation.RefundProcessed to outlet ReservationEventSplitToReservationRepository
        send event Reservation.RefundProcessed to outlet ReservationEventSplitToAirlineAnalytics
        tell event Reservation.RefundProcessed to entity ReservationContext.Reservation
      }
      on other is {
        error "Unexpected message for processor ReservationEventSplit"
      }
    } with {
      briefly "Reservation event fan-out handler"
      described as {
        |Delivers each Reservation event to all of its consumers.
      }
    }
  } with {
    briefly "Reservation event fan-out"
    described as {
      |Fans Reservation events out to the repository and projectors.
    }
  }
  connector 'ReservationEvent Stream' is from outlet ReservationContext.Reservation.ReservationEvents2 to inlet ReservationContext.ReservationEventSplit.ReservationEventSplitIn with {
    briefly "Reservation events, fanned out to storage and read models"
  }
  connector 'ReservationEvent Storage' is from outlet ReservationContext.ReservationEventSplit.ReservationEventSplitToReservationRepository to inlet ReservationContext.ReservationRepository.ReservationRepositoryFromReservation with {
    briefly "Reservation events on their way to storage"
  }
  connector 'AirlineAnalytics Feed' is from outlet ReservationContext.ReservationEventSplit.ReservationEventSplitToAirlineAnalytics to inlet ReservationContext.AirlineAnalytics.AirlineAnalyticsFromReservation with {
    briefly "Reservation events feeding the AirlineAnalytics projection"
  }
  connector 'AirlineAnalytics Storage' is from outlet ReservationContext.AirlineAnalytics.AirlineAnalyticsOut to inlet ReservationContext.ReservationRepository.ReservationRepositoryFromAirlineAnalytics with {
    briefly "The AirlineAnalytics projection on its way to storage"
  }
} with {
  briefly "Bounded context for airline reservations"
  described as {
    | The ReservationContext is the bounded context for
    | flight reservations and passenger management.
  }
}
